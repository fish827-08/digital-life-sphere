# 评审 R1：云端战略转向报告 + 本地初步评价

> 评审人：内部评估专员 | 日期：2026-09-11 | 证据标准：实测优先
> **目标 commit**：`gitee/main` = `55245c1`（本地 main 已同步至同点）
> 被评对象：① `docs/决策与评审/战略转向-生态循环与食物链重构-20260911.md`（云端 v1.0，833 行，`e0648e8`，2026-09-11 17:00）
> ② `docs/决策与评审/评估-EVAL-内部评估-战略转向-20260911.md`（本地初步评价，55 行，`0e9d891`，2026-09-11 23:10）
> 关联：`评估-EVAL-可行性分析-g15高位payoff-20260911.md`（`2e63452`）、`评估-EVAL-评审审计评估报告-20260911.md`（`55245c1`）

---

## 〇、一句话结论

> **诊断成立，处方未成立，且其经验基础是病数据。**
> 信号经济学诊断（§1.1）是本项目迄今最扎实的理论工作，我完全认同；
> 但报告的**头条经验证据（D2.1 网格）在数据层面无效** —— 不是"2 seed 复现"，而是**一条轨迹**；
> 更关键的是：报告自己在 §1.2.2(3) 已指出正确的修复点（社会结构→发送者获益），却把处方放大到 6–9 周的生态重构。
> **本地初步评价的方法论意识合格（识别出 5 项风险），但它复核了 g15 数值、没有复核数据来源，因而漏掉了上述致命项。**

**Gate 判定：不通过（G1 / G2 / G3 不通过；G7 部分不通过）。**

---

## 一、报告定位（已找到，含完整链条）

| 文档 | commit | 作者 | 时间 |
|------|--------|------|------|
| 战略转向-生态循环与食物链重构 v1.0 | `e0648e8` | digital-life-sphere-dev（云端） | 09-11 17:00 |
| 可行性分析-g15高位payoff | `2e63452` | 本地 | 09-11 |
| **内部评估-战略转向 v1.0** | **`0e9d891`** | **fish827-08（本地）** | **09-11 23:10** |
| 评审审计评估报告（对本审计前身的评审） | `55245c1` | 本地 | 09-11 |
| D2 seed 修复 | `db0aef1` | 本地 | 09-11 |

⚠️ 全部位于 `gitee/*` ref，**本地 `main` 曾落后**；评估前若不 fetch 会读到旧状态。已在 `BASELINE.md` 记录该风险。

---

## 二、实测复现（命令 + 原始结果）

```powershell
cd _gitee_review
# 1) D2.1 网格 7 组 × 2 seed 的真实 MD5
git show gitee/feat/d2-grid:experiments/long_d2_<tag>_s<43|42>.csv  →  Get-FileHash -Algorithm MD5
# 2) manifest 中的 seed
git show gitee/feat/d2-grid:experiments/manifest_d2_<tag>_s<seed>.json
# 3) 轨迹逐行前缀比对
# 4) MI 复算（用项目自己的函数）
python -m experiments._dsh_mi_probe   # 见 §五
```

### 2.1 【实测】manifest 证明 seed **从未被置入配置**

```
manifest_d2_lr005_s42.json  → config.seed = 42
manifest_d2_lr005_s43.json  → config.seed = 42      ← 应为 43
manifest_d2_lr010_s42.json  → config.seed = 42
manifest_d2_lr010_s43.json  → config.seed = 42      ← 应为 43
manifest_d2_lr0125_s42/s43  → config.seed = 42
manifest_d2_m2400_s42/s43   → config.seed = 42
```

**每一个 `_s43` manifest 都记录 `seed=42`。** 这是设计性复现的直接反证。

### 2.2 【实测】7 组配对中 3 组**逐字节全等**

| tag | s42 MD5 | s43 MD5 | 判定 |
|-----|---------|---------|------|
| lr005 | `0AA97F1C…` | `0AA97F1C…` | ❌ **全等** |
| lr0075 | `B3A90AE4…` | `B3A90AE4…` | ❌ **全等** |
| lr0125 | `117A5030…` | `117A5030…` | ❌ **全等** |
| lr010 | `995BCFEA…` | `482F8FD2…` | 前缀共享 |
| lr030 | `1FFB9402…` | `F4BE8157…` | 前缀共享 |
| m2400 | `25F288A4…` | `EC6D33C5…` | 前缀共享 |
| m3600 | `B6E1AF31…` | `B5C03B9D…` | 前缀共享 |

### 2.3 【实测】非全等的 4 组，**记录序列共享前缀**

| tag | s42 行数 | s43 行数 | 公共行前缀 | 首个分歧行 |
|-----|---------|---------|-----------|-----------|
| lr010 | 29 | 29 | **4** | 第 5 行 |
| lr030 | 30 | 31 | **4** | 第 5 行 |
| m2400 | 20 | 32 | **1** | 第 2 行 |
| m3600 | 23 | 30 | **1** | 第 2 行 |

**lr010 的 s42/s43 前 4 行完全相同**（`2000:0.3674 4000:0.1482 6000:0.2062 8000:0.4124`）。
**同一模型、不同 seed，绝不可能在 8000 tick 内逐点全等。** 唯一解释：两条 CSV 源自**同一条轨迹**，因日志记录粒度不同而在后面产生了行错位。

### 2.4 【实测】报告引用的数值本身准确

| 报告引用 | 我从 CSV 复算 | 判定 |
|---------|-------------|------|
| lr010 s42 final_g15 = 0.244 | **0.2442** | ✅ 准确 |
| lr010 s43 final_g15 = 0.346 | **0.3459** | ✅ 准确 |
| lr010 峰值 0.856~0.870 @14k | **0.8556(s42) / 0.8695(s43) @14000** | ✅ 准确 |

**即：数字没有抄错，是数据本身不是它声称的东西。**

### 2.5 【实测】manifest 与实际产出**脱节**

| 项 | manifest 记录 | CSV 实际 |
|----|--------------|---------|
| ticks | **1000** | **60000** |
| 记录精度 | — | 2000~4000 不等，且**同 tick 重复行**（如 lr010 有 24000 出现 3 次） |

manifest 停留在初始 1000-tick 流水线，**续跑从未更新 manifest** → 归档**不自描述**。

---

## 三、Gate 逐条判定

| Gate | 判定 | 阻塞项与证据 |
|------|------|-------------|
| **G1 数据有效性** | ❌ **不通过** | manifest 记 `seed=42` 而文件名标 `s43`；manifest `ticks=1000` 而产出 60000；无 config 指纹与 commit 列 |
| **G2 对照有效性** | ❌ **不通过** | D2.1 的"seed 对照"不存在（同一条轨迹）；`neutral` 对照臂此前已失效（N=2~5），至今未修 |
| **G3 统计功效** | ❌ **不通过** | 报告自称"双稳态/单 seed 异常值"，实为**一条轨迹的记录错位**。按实测 σ，g15 检出 Δ=0.05 需 53–212 seed，报告与本地评价都只谈 2 seed |
| **G4 指标效度** | ⚠️ 部分 | 战略转向报告本身**未依赖**失效指标（cult_div/trust），这点做得好；但下游倘若用它做验收需先修 |
| **G5 静默错误** | ⚠️ 部分 | 本地 B2 已修（`028eccc`）；**F-V1（`step_tick` 缺失、1062 行 `tick.rs` 静默 skip）仍未修** `[实测]` 当前扩展 `step_tick present: False` |
| **G6 验证覆盖** | ❌ 不通过 | 见 F-V1；且 `feat/s1-step-tick`(+12)、`feat/s2-rayon-rng`(+19)、`feat/d2-vec`(+5) **仍未合入 main** |
| **G7 叙事强度** | ⚠️ 部分不通过 | "D2.1 实验验证了理论分析"（§1.1.3）**强于证据** —— 该数据无法验证任何事 |

**总判定：不通过。** 依据章程「任一不通过 → 整条结论链不通过」。

---

## 四、我的核心审计意见：诊断成立，处方未成立

### 4.1 强项（必须明确肯定）

1. **§1.1 信号经济学诊断是本项目迄今最扎实的理论工作。**
   Maynard Smith & Harper 四条件的映射（共同利益/累赘原理/协调均衡/操纵-对抗）**逐条对照当前引擎实现**，结论"四个条件一个都不满足"我核对了代码，成立。这是从"调参"升级到"结构性分析"的正确跃迁。

2. **§1.4 实验前可行性分析清单（5 问）是方法论上最有价值的产出。**
   "理论上可能吗 / 小规模验证过吗 / 判据明确吗 / 对照合理吗 / 统计功效够吗"——这五问如果早些存在，D1/D2.1/battle 的浪费都可以避免。**建议立即并入项目纪律（见 R-8）。**

3. **§1.3 性能优化反思方向正确。**
   "在机制不可达时优化=错误方向加速"这一判断与 S1/S2 实测改判逻辑一致；且指出"世代时间 1.8 万 tick/代 → 压缩到 600~2000 是改参数、等效 10~30 倍算力"，与我基线 B-9 的实测吻合。

### 4.2 致命问题：经验基础是病数据

**报告 §1.1.3 用 D2.1 网格"完全验证"了理论分析。该数据不能验证任何事。**

项目的既有纪律明确写了（`评估-EVAL-D2参数调优实验指南` §1.4）：

> "g15 上升判据（验收门）：… **3 seed 方向一致（排除漂变）**"

而 D2.1 的实际 seed 数是 **1**。3 组配对是同一文件的两个副本，4 组是**同一条轨迹的两次采样**。

**后果**：战略转向的"机制必然性"结论**目前只有理论支撑，没有经验支撑**。理论可能是对的（我认为大概率对），但它**尚未被数据检验**。这恰好违反了报告自己在 §1.4 提出的第一问——"理论上可能吗？"要配"小规模验证过吗？"。

### 4.3 方法论警示：一个 56% 的差异被读成了生态学现象

报告 §1.1.3 的关键观察：

> "lr030_s43 达到 0.449（最接近0.5），但这是**单 seed 异常值**，s42 仅 0.288，存在**双稳态**"

**这正是缺陷的指纹，被反向解读了。**
manifest 证明两次运行都跑在 `seed=42`。**同代码、同 seed 不可能产出 0.288 与 0.449。**
→ 正确解读不是"双稳态"，而是"**复现失败**"。

> **审计教训（建议入项目纪律）**：当同配置两次运行出现大差异时，**默认假设是复现失败，而不是发现了新现象**。
> 生态学解释（双稳态、频率依赖、混沌）**必须排在最末位**，且需先通过"同 seed 必须逐位相同"这一关。

本地初步评价**看到了这组数字**（它引用了 lr 细扫），但**没有去看 manifest**，因而错过。

### 4.4 逻辑缺口：处方比诊断大了一个数量级

报告自身的论证链存在**层级跳跃**：

| 报告位置 | 内容 |
|---------|------|
| §1.1 诊断 | 信号不满足四条件之一 → **缺的是"发送者获益途径"** |
| §1.2.2(3) | 报告自己列出正确的修复点：**"社会结构 → 信号发送者获益"**（亲缘/互惠/群体协调） |
| §执行摘要 | 处方却是：**放弃语言目标，先做 6–9 周生态循环 + 多层食物链重构** |

**缺的是 payoff 通道，给的是生态平台。**
- 生态循环（能量守恒/分解者/营养级）**不直接创造选择压**。它改变的是世界，不是适应度地形。
- 报告 §1.2.2 的 4 条论证**全是断言，无引用、无数据**（对比 §1.1 引了 Maynard Smith & Harper，标准不一）。
- **"多营养级食物链是语言涌现的必要条件"这一命题，报告未给出任何经验或文献支撑。** 已知文献（社会脑假说、马基雅维利假说）更多指向**群体规模/社会复杂度**，而非食物链深度。

→ 结论：**"诊断正确 + 处方大两级 + 缺桥接证据"**。

### 4.5 本地初步评价的审计

| 维度 | 判定 | 说明 |
|------|------|------|
| 方法意识 | ✅ 合格 | 引用了 lr 细扫**实测数据**而非仅文档；区分风险等级；给出 A/B/C 三选项而非单一建议 |
| 风险识别 | ✅ 良好 | R1–R5 五项中 **R2/R3/R5 是真实且有价值的**（见下） |
| **数据来源核验** | ❌ **缺失** | 只核 g15 数值、未核 manifest/seed → 漏掉本文 §2 全部发现 |
| 功效标准 | ⚠️ 偏弱 | 建议"2 seed×60k"，但按其自己引用的 σ，2 seed 无判别力；未引用功效计算 |
| 结论力度 | ⚠️ 偏软 | "方向认同 + 三处风险"——**在基础数据无效的前提下，不应先给"方向认同"** |

**本地评价的五项风险，我的复核**：

| # | 本地风险 | 我的判定 |
|---|---------|---------|
| R1 单一路线全押 | ✅ 成立 | 但理由应更强：不只是"回退成本高"，而是**桥接证据不存在**（§4.4） |
| R2 "生态先行"必要性未证 | ✅ **最有价值的一项** | 本地已识别文献指向"群体规模/社会结构"而非食物链。**这与我的 §4.4 独立同构——两个独立分析都指到这里，信号很强** |
| R3 砍环境规则过头 | ✅ 成立 | 光照/温度是生态位分化的天然驱动器；建议保留静态纬度梯度，成本极低 |
| R4 阶段估时乐观 | ✅ 合理 | 建议"门槛制管理，宁可停不可烂尾"正确 |
| R5 负结果解释纪律 | ✅ 成立 | "机制可行但缺 payoff 通道" vs "机制不可行"必须区分 |

**本地评价给出的最有价值建议**："先跑 1 周小规模对照（生态原型 vs R2 声誉权重）"——**方向完全正确**（低成本先证伪）。
**但有一个设计缺陷**：该对照的判据是"g15 / 物种数 / 能量守恒三方"，而**物种数与能量守恒并不检验"能否通往语言"**。它检验的是生态原型能不能跑。→ **判据与决策目标不匹配**（详见建议 R-2）。

---

## 五、独立解决 `signal_context_mi` 分歧（本地标注"分歧未解"）

本地评价把此项列为"三方对齐议题，不采信任一方"。我**实测解决**：

```python
# 用项目自己的函数，传 snapshot 里真实存在的 resource_grid
from experiments.language_analysis import signal_context_mi
d = np.load("data/snapshots/snapshot_small.npz")
signal_context_mi(d["signal_marks"], d["signal_age"], d["resource_grid"], d["flat"], 7200)
```

**原始输出：**
```
snapshot keys 含 resource_grid (7200,) range=(-0.0000, 120.0000)
marks nonzero=3993  unique=[0 12 13 14 15]
>>> PROJECT FUNCTION (as-is): {'mutual_information': 0.127534,
                               'normalized_mi': 0.086911,
                               'context_bins': 3, 'active_signals': 3993}
```

**裁定**：
1. **审计方正确**：项目自己的函数、在项目自己的快照上，返回 **MI=0.1275**。归档的 `0.0` 是**失效数据**（`active_signals` 5904≠3993，证明来自另一状态）。
2. **本地复核得 0.0 的原因**：其理由（"取决于语境通道定义、快照是否含 resource_grid"）**方向是对的**，但函数**确实接收** `resource_grid` `[实测]`。传全零/错误通道即得 `food.max()=0` → 单箱 → **MI 构造性为 0**。这是一个**调用正确性问题**，不是定义分歧。
3. **附带确认退化分箱** `[实测]`：`food/food.max()` 三等分边缘为 `bin0=3569 (89.4%) / bin1=125 (3.1%) / bin2=298 (7.5%)` —— 与审计所述 `[3570,125,298]` 一致。**该分箱方案本身必须修**，否则 MI 估计不稳。
4. **附带确认模式发射** `[实测]`：`{12:1849, 13:305, 14:1281, 15:558}`，**模式 1–11 为 0** → "16 模式符号空间"实际是 **4**。

> **结论**：该项**可以关闭分歧**（答案 0.1275），但**须同时修分箱**并把叙事改为"MI>0 是定义性的（`f_bit` 硬编码）→ 指示符而非符号"。

---

## 六、Gate 判定与处置

### 判定：**战略转向——不通过（暂缓全押）**

**阻塞项**
1. D2.1 经验基础无效（seed 从未置入；3 组全等、4 组共享前缀）→ 需在**修复后**重跑
2. "多营养级食物链是语言必要条件"**无证据**（§1.2.2 全文为断言）
3. 处方与诊断**层级不匹配**（缺 payoff 通道 vs 6–9 周生态平台）
4. 本地评价的对照判据（物种数/能量守恒）**不检验决策目标**
5. `neutral` 零模型对照仍未修（设计已写、从未执行）

**解除条件（可验证）**
- E1：D2.1/lr 在**修复后的 seed 路径**下重跑 ≥5 seed，报告 mean±sd，且**先通过"异 seed 必须不同"哨兵测试**
- E2：为"生态复杂度→语言"补**桥接论证或桥接实验**：给出可证伪的中间判据（例如"在 R2 声誉权重下 g15 稳态 >0.5"）
- E3：先做 **R2（声誉权重，1 行改动）** 的低成本证伪，再决定是否投入生态线
- E4：对照判据改为**直接检验决策目标**（g15 稳态 / 信号-语境 MI 的相对超额量），而非生态指标
- E5：修 `neutral` 为"只冻结目标位"，作为所有后续结论的零模型基线

### 建议动作

| 级 | 动作 | 理由 |
|----|------|------|
| **P0** | **撤回/重标注 D2.1 数据**，manifest 与实际产出脱节须修（写入 ticks/seed/指纹/commit） | 防止病数据继续进入决策链（已进入 3 份文档） |
| **P0** | 加**"异 seed 必须不同"哨兵测试**（本地处置清单已列，状态"待加"） | 该测试会拦住本次全部问题 |
| **P0** | **先做 R2 声誉权重 1 行证伪**（成熟做法：最小改动先验证 payoff 通道） | 成本 ~1 天，直接检验 §1.1 诊断，无需 6–9 周 |
| **P1** | 采纳本地 R3：保留**静态光照/纬度梯度**（成本 ~0.5 天），不要全砍环境 | 保生态位分化的环境底 |
| **P1** | 修 `signal_context_mi` 分箱 + 重生成分析 JSON（见 §五） | 分歧已解决，错误数据仍在库 |
| **P1** | 执行 `neutral` 修正对照（`PROGRESS.md` 待办 #16） | 所有方向性结论的前提 |
| **P2** | 把 §1.4 五问清单**并入项目纪律**（建议进 `AGENTS.md`） | 这是报告最好的产出 |
| **P2** | 合入 `feat/s1-step-tick`(+12)/`feat/s2-rayon-rng`(+19)/`feat/d2-vec`(+5)；修 F-V1（`step_tick` 缺失应硬失败） | `[实测]` 当前扩展无 `step_tick`，1062 行 `tick.rs` 仍不在覆盖内 |

### 我**不**建议的
- ❌ 不建议直接否决战略转向。**诊断是对的**，生态复杂度确实可能是长期所需。
- ❌ 不建议"再调一轮参数"（报告 §1.1.3 结论我认同：机制缺环时调参无意义）。
- ✅ 建议的是：**先花 1 天验证 payoff 通道（R2），再决定是否花 6–9 周建生态平台。** 报告与本地评价都把顺序倒置了。

---

## 七、我未验证的部分（明确列出）

1. **未重跑 D2.1**（60k tick × 7 组）。我只核验了归档数据的**内部一致性**与**manifest 对照**，未在修复后重新生成。
2. **未验证 `gitee/feat/d2-grid` 是否曾被其他工具续写**。存在一种可能：`continue_d2_experiments.py` 有意复用同一 seed 做"续跑"，则 `_s43` 文件名纯属误标。但即便如此，**报告据其做"2 seed 复现"解读仍是错的**，结论不变。
3. **未审计 `battle` 批次与 lr 细扫**（`long_bat_*`、`long_d2_lr*`）。`battle` 的 seed 多样性此前实测为"正常"（s42/s43 哈希不同），但**未核 manifest**。
4. **未评估生态循环路线的技术可行性**（能量守恒重构、分解者、营养级耦合的工作量估计），仅评其**论证充分性**。
5. **未做文献检索**验证"多营养级食物链 ↔ 语言"的关联强度；我指出的是"报告未给支撑"，而非"该命题为假"。

---

## 附：本次实测命令汇总

```powershell
cd _gitee_review
git log --oneline -1 gitee/main                        # 55245c1
git show gitee/feat/d2-grid:experiments/manifest_d2_lr010_s43.json   # config.seed = 42  ← 关键
# MD5 比对 7 组 × 2 seed → 3 组全等
# 逐行前缀比对 → lr010/lr030 共享 4 行；m2400/m3600 共享 1 行
# MI 复算 → mutual_information=0.127534, normalized_mi=0.086911, active_signals=3993
```

*临时探针脚本已删除；`git status` 复核为 0 项未跟踪。*

---

# 附录 R1b：对本地"整理+修复"汇报的核验（2026-09-11 追加）

> 本地汇报要点：①快照归集 ②纳入游离文件 ③**seed 缺陷已修（`db0aef1`）** ④**`signal_context_mi` 归档值失效** ⑤询问"是否需要进行实验 / 是否发云端重跑引导词"

## 一、汇报转述精度核验

| 本地汇报的表述 | 我的实测 | 判定 |
|---------------|---------|------|
| "快照归集 22 个 → `data/snapshots/`" | `[实测]` 工作区 `git status` = 0，npz 已不在根目录 | ✅ 属实 |
| "`--seed` 完全无效，已修复并验证" | `[实测]` `db0aef1` 只改 `run_d2_experiment.py` 一行 `SimConfig(seed=args.seed)`；修复后 seed 42/43/44 轨迹**互不相同** | ✅ 属实 |
| "lr 细扫 s42 与 s43 CSV **逐位相同**" | `[实测]` `long_d2_lr005_s42/s43` = `2EE640B0…` **相同**；`lr0075` = `9D06238B…` **相同** | ✅ 属实 |
| "**D2.1 网格 8 实验** … 全部是同一轨迹" | `[实测]` 网格实为 **7 组**；3 组逐字节全等，**4 组仅共享前缀**（lr010/lr030 共享 4 行、m2400/m3600 共享 1 行） | ⚠️ **不精确** |
| "**参数档间结论仍有效**，但 seed 维失效" | `[实测]` 不同 `lr` 产出不同轨迹（`flat_sum` 238813/261758/272312/259122/261284） → **参数维确实有效** | ✅ **正确且关键** |
| "`signal_context_mi` 需按新口径重算" | `[实测]` 已裁定 **0.1275**（见 §五）；但**须先修分箱**再重算，否则仍不稳 | ⚠️ 需补前置 |

**结论**：方向正确，**唯"全部是同一轨迹"是夸大**——失效的是 **seed 维**，不是整个网格。

## 二、一个必须避免的过度否定（F-D12）

本地汇报的影响面清单写"D2 gate 判据需要重跑才可信"，方向对；但若据此把 **D2.1 整体作废**，会连带丢掉**唯一支撑战略诊断的有效证据**。

**实测反证（诊断在参数维上依然成立）：**

| tag | final s42 | final s43 | peak s42 | peak s43 |
|-----|----------|----------|---------|---------|
| lr005 | 0.2553 | 0.2553 | 0.6122 | 0.6122 |
| lr0075 | 0.2347 | 0.2347 | 0.3319 | 0.3319 |
| lr010 | 0.2442 | 0.3459 | 0.8556 | 0.8695 |
| lr0125 | 0.2619 | 0.2619 | 0.4348 | 0.4348 |
| lr030 | 0.2876 | 0.4492 | 0.4057 | 0.4492 |
| m2400 | 0.2336 | 0.2342 | 0.3963 | 0.4207 |
| m3600 | 0.2380 | 0.2352 | 0.4256 | 0.3843 |

**14/14 终局 g15 < 0.5，且全部 peak-then-revert。**

→ 可行性分析 §三 的**可证伪判据已被证实**（"若全部 peak-revert 则证实 payoff 结构不可达"）。
→ **战略诊断是可复算、不依赖 seed 维的。这一点必须在重跑前就确认下来，否则重跑会被误读为"推翻诊断"。**

**正确表述**：不是"D2.1 作废"，而是"**D2.1 的 seed 维作废；参数维有效并已复算支持诊断；seed 维待补**"。

## 三、回答"是否需要做实验"

**需要，但不是"重跑 D2.1 全网格"那种做法。** 按性价比排序：

| 序 | 动作 | 成本 | 为什么这么做 | 不做会怎样 |
|----|------|------|-------------|-----------|
| **1** | **补 seed 哨兵测试**（异 seed 必不同） | ~30 min | 该测试若早存在，F-D1/F-D9 都不会发生；是**唯一能阻止复发**的动作 | 同类缺陷会第三次发作 |
| **2** | **P0 对照：R2 声誉权重（1 行）vs 现状** | ~1 天 | 直接检验 §1.1 诊断；可行性分析已定为候选 | 6–9 周生态线**未先证伪**就启动 |
| **3** | **修 `neutral` 零模型**（只冻结目标位） | ~1 天（写）+ 算力 | 所有方向性结论的前提 | 重跑出来的 g15 仍无法与漂变区分 |
| **4** | **D2.1 seed 维补跑**（7 组 × ≥5 seed） | 云端算力 | 把"参数维有效"升级为"参数维 + seed 维可报 CI" | 只能报单轨迹，无法做统计推断 |
| **5** | **`signal_context_mi` 修分箱 + 重算** | ~半天 | 分歧已解决，但分箱退化（`bin0=89.4%`）须先修 | 重算仍不稳，等于没重算 |

**关键顺序判断**：**2 排在 4 之前**。

理由：第 4 项（重跑网格）回答的是"**参数能否救回 g15**"；而可行性分析已经用**有效证据**（参数维）回答了这个问题——**不能**（14/14 回落）。
再花云端算力重跑网格，**不会改变诊断**，只会补上 seed 维的 CI。而第 2 项（R2 证伪）回答的是"**换机制能否救回 g15**"，这是唯一还没被回答、且决定 6–9 周路线是否该启动的问题。

> **即：先花 1 天问对的问题，再花算力把已知答案做得更精确。**

## 四、对云端重跑引导词的建议（若要发）

若决定发云端重跑，引导词应包含：

1. **基线 commit 必须写明 `db0aef1` 或之后**（否则重跑无意义）
2. **前置断言**：跑前先验证 seed 生效（同参数 seed42≠seed43，终态不相等），**验证失败即停**
3. **seed 数 ≥5**（按实测 σ，g15 检出 Δ=0.05 需 53–209 seed；若只求方向一致性，≥5 起）
4. **manifest 必须写** `ticks`（真实值）+ `seed`（真实传入值）+ config 指纹 + commit
5. **判据写清楚**：这是"补 seed 维 CI"，**不是**"重新检验诊断"——诊断已由参数维证实，避免误读
6. **对照臂**：至少含修正后的 `neutral`（只冻结 g14/g15）

## 五、我的建议（一句话）

> **先做"哨兵测试 + R2 一天证伪 + 修 neutral"三件事（约 2 天），再决定是否把算力投到 D2.1 seed 维补跑。**
> **不要把 D2.1 整体作废——先发那 1 天证伪，再动 6–9 周。**

## 六、R1b 未验证部分

1. 未重跑 D2.1（60k × 7 组），仅核验归档数据内部一致性与参数维有效性
2. 未核 `battle`/`gen`/`zero` 批次的 manifest（其 CSV 的 s42/s43 哈希**互不相同**，说明这些线未受该缺陷影响，但**未核 manifest 确认**）
3. 未评估 R2 声誉权重实现的具体副作用（需读移动得分代码与守恒口径）
4. 未验证云端算力预算与排队情况
