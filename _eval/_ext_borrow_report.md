# 外部借鉴评估报告：think-chain-life / prompt-life 对我们的真实价值

> 评估对象：issue IKFLA2（对我方：`_eval/_issue_the-world.md`）与 IKFJJ3（对 think-chain-life：`_eval/_issue_think-chain-life.md`）
> 外部仓库：`gitee.com/little-fishy/think-chain-life`、`gitee.com/little-fishy/prompt-life`（已 clone 至 `C:\Users\圣羽\Desktop\temp\extReview2\`）
> 方法：先读我方引擎代码建立事实，再读外部代码/README，最后逐条核验 issue 声称。
> 证据标签：`[实测]`（本机跑出的数字）/`[代码]`（带文件:行号）/`[文档]`（仓库内文档原文）/`[推断]`（我的推理）。
> 边界：不重复"0.463 能否复现"类数据核对（另有审查员负责）；本报告聚焦"设计是否可迁移 + 建议是否成立"。

---

## 〇、一句话结论

**该 issue 的"归因层"基本正确、且在若干处比它自己以为的更正确（我们已把它当自评读过）；但它的"解药层"是错的，且其经验前提被它自己的姊妹报告推翻。**

- 它建议的 P0（指称博弈）**不是新东西**：等于我方 `_advice/设计建议-20260912.md` §3.1 的"Lewis 化"，我方已在 **R28 明确"暂缓"**（理由：与 C3 自变量污染风险）。issue 版本比我们自己的规格**更粗糙**（缺收益量级前置判据、缺与 C3 的区分表、无生态接地）。
- 它的因果前提"think-chain-life 之所以能涌现 v3/v4 语言，本质是指称博弈给发射创造了直接选择压"**被它自己的 IKFJJ3 报告与对方代码共同证伪**：v3（其最可复现的结果）**根本没有指称博弈**（`environment.py` 零命中），v4 才引入隐式指称奖励，v5 引入显式指称博弈后**同口径指标反而下降**（0.061 < 0.183）。
- 真正值得借的是它的**方法论副产品**：结构破坏型零模型（Permuted）、Topographic Similarity、"任何语言指标必须报随机基线"的判别力检验。这三条恰好补我方 F-D3（全仓零统计推断）。
- 结论：**P0 指称博弈应拒绝（或最多降级为"仪器阳性对照"）；Permuted 零模型 + Topographic Similarity 应采纳（P1）；符号序列应延后并改写方案。** 详见 §七。

---

## 一、指称博弈 vs 我方 oracle：2×2 对比

### 1.1 核心 2×2（行 = 收益归属侧，列 = 设计）

| | **oracle**（已实现，R39/D-8） | **指称博弈**（tcl v5+；issue 建议的形态） |
|---|---|---|
| **发送者(S) 何时获益** | 仅当"某接收者踩着有信号的格**且该格有食物**"（`require_food=True`）时，按 `_id` 归因到发送者，由**接收者**转出 ≤`donation=0.05`；终身封顶 `0.1×自身累计发射次数 − 累计已获回馈` ⇒ **期望净收益 ≤ 0，只补偿不补贴** `[代码: simulation/oracle.py:26,52-93; simulation/sphere_engine.py:1362,1376-1384]` | 环境每 12 步**自动配对**同族群 S/R 并放置 3 个虚拟候选物，目标是环境随机指定的 1 个；接收者 18 步内走到目标 ⇒ **双方各 +6**；另有"S 指称过的食物被别人采到 ⇒ S 再得 0.5×奖励" `[代码: environment_v5.py:153-217,219-233,483-486,645-649]` |
| **接收者(R) 收益** | 落点有食物 ⇒ 正常进食收益（本来就有）；回馈是 **R 付给 S** 的 `[代码: sphere_engine.py:1388-1394]` | 到目标 **+6**（另加普通进食 `+0.1×gain`）`[代码: environment_v5.py:508,523]` |
| **信号是否必需**（R 有无别的信息源） | **不必需**。食物在 R 的感知邻居里直接可见（`fr = food_ratio[nb]`）；信号携带的 `f_bit` 只是同一量的**量化滞后副本** ⇒ 信号边际信息价值 ≤ 0 `[代码: sphere_engine.py:846,857-859,692-701]` | **对该 +6 通道必需**：目标是环境随机抽的，`★目标` 平面**只进发送者观测**（`if is_sender:`），R 无其他途径知道哪个是目标 `[代码: environment_v5.py:354-356,203-204]`。但**生存/觅食通道不依赖信号**（候选物是临时 dict，不是真实食物/能量实体）`[代码: environment_v5.py:183-217]` |
| **内容是否相关** | **刻意内容无关**：只看"该格有无任何 pattern"（`had_signal`），不看 pattern 值 `[代码: sphere_engine.py:774-777,1362]` | **内容相关**：+6 只在"解对了被指称的那个目标"时发放 `[代码: environment_v5.py:219-233]` |
| **生态嵌入度** | 域内、**能量守恒**（S→R 转移，`Σenergy` 不变）`[代码: oracle.py:73]` | **域外**：候选物与 +6 都是抽象奖励，不进入能量/捕食/食物网 `[代码: environment_v5.py:183-217 + 全局搜索 candidates 未被 append 进 self.food]` |
| **设计目的** | **仪器校准**（"在选择肯定存在时，我们的仪器能不能测到选择"）`[文档: simulation/oracle.py 模块 docstring]` | **任务注入**（给 RL 一个可学的通信任务）`[文档: tcl README v5 节]` |

### 1.2 关键差异 3 条（这 3 条决定了"能不能迁移"）

1. **指称对象是否对接收者不可观测**：oracle 的指称对象（食物）R 自己看得见 ⇒ 结构性零信息价值；指称博弈的指称对象（随机选中的虚拟候选）R 看不见 ⇒ 信息价值由构造保证为正。这不是"参数差异"，是**世界结构差异**（与我方 `_advice/设计建议-20260912.md` §3.3"私有信息"同一诊断）。
2. **付费挂在"发射"还是"做对"**：oracle 付费挂在"R 恰好获益"上，且**内容无关**；指称博弈付费挂在"R 解对内容"上。我方广播信道建议 §1.1 的原话是"**收益挂在'做对'上，不挂在'发射'上**"——issue 的建议方向与之一致，但它没说清"做对"需要一个**不可观测**的参考量。
3. **奖励来源**：tcl 的 +6 是 RL 的 reward，不是能量；我方引擎没有 reward 通道，任何收益必须落到能量或繁殖。tcl 式 +6 在我们要么变成"凭空注入能量"（破坏能量守恒 → 生态门风险），要么变成"S→R 转移"（那 S 净亏，退化成 oracle 的加强版，仍无正选择压）。**这是 issue 完全没提的最大工程量。**

---

## 二、把"指称博弈"接进我方引擎：代价与风险

### 2.1 新增实体 / 位置（"候选目标集合"从哪来）

| 方案 | 代价 | 结论 |
|---|---|---|
| **A. 照搬 tcl：虚拟候选物 + 外部任务** | 需要：新实体层（3 个候选物/对）、每 agent 任务状态（`sender/receiver/target/deadline/success`）、环境定时配对器、**观测掩码**（★只给 S）——但我方决策是 `score→softmax→选邻居格`，没有"候选物"概念，也没有 per-agent 观测掩码；还要一个"抽象得分"通道 | 改动量 ≈ 新增一个小型环境子系统（类比 tcl `environment_v5.py` 的 500 行级），且**必须在引擎主循环里加第二套并行状态机**。`[推断]` |
| **B. 域内版：私有信息 + 共享收益**（我方自己的 A-4/§3.1/§3.3 规格） | 只需给资源场加一个"只有站上去才感知到"的隐藏位（如斑块富集度），把该位进 `state`、进 `codebook`，并给 S 一条"R 采到该格 → S 也得分"的耦合项 | 改动面小一级，且不动能量守恒（共享收益可做成"R 采到的能量分 S 一部分"）`[文档: _advice/设计建议-广播信道与共享收益-20260913.md §1.1-1.4]` |
| **C. 用现有 patch 当候选集**（`patch_count=30, patch_radius=2`） | 我方资源斑块对 R 直接可见（`fr`），必须先让 R"看不见"才成立；否则回到 oracle 式零信息 | 不解决根本问题 `[代码: 资源场对感知可见]` |

**关键事实**：tcl 的候选物是**临时 dict**，不是食物实体（不进 `self.food`），观测里只通过"目标平面"出现且只给 sender `[代码: environment_v5.py:183-217,243,354-356]`。也就是说**它的指称对象是纯抽象标记**——这直接解释了为什么它能"干净地"制造信息不对称：因为它压根不在生态里。我们要复刻，就得同样造一个"不在生态里"的通道。

### 2.2 要不要改 `signal_field` 的数据结构？

- **不必须改 dtype/形状，但必须改"state 的定义"**。当前 `pattern` 只由发送者自身状态决定（`state = e_bin*4 + f_bit*2 + n_bit`，`e_bin = energy/max_energy*4`），**与任何外部指称对象无关** `[代码: sphere_engine.py:692-701]`。要让信号指代目标，必须把"目标索引"并入 `state` 或另开一位。这是**语义改动**，不是容器改动。
- `_marks` 是 `(n_cells,) uint8`、16 种有效 pattern（低 4 位）`[代码: world/signal_field.py:9,39,49]`；若目标数 ≤16，**容器够用**；若要"序列"，才需要扩容器（见 §四）。

### 2.3 会不会破坏"双路径逐位对拍"（Rust/Python）？

- **会，除非把新机制显式钉成 Python-only 并加"静默忽略就报错"的门槛。** 现成先例正是 oracle：`C-8` 在 `use_sim_core=True` 且 `oracle.enabled=True` 时**直接 raise**，理由是"静默忽略会重演 R14『配置看似生效实则没生效』" `[代码: sphere_engine.py:310-315]`。
- 现有 D2 的门槛是**局部的、易漏的**：发射段只在 `arbitrary_codebook` 时回退 Python `[代码: sphere_engine.py:668-670]`；移动段只在 `perception_radius==4 / noise>0 / softmax_tau>0` 时回退 Python `[代码: sphere_engine.py:729-731]`。**新增任何一条"D2 语义"都要在每一处都补上**，否则会出现"发射走 Rust 旧编码、接收走 Python 新语义"的混合路径（这正是 F-R9/R59 的同型事故：配置穿过 CLI→grid→preset→`--arm`→引擎开关→运行时路径六层，每层可静默丢弃）`[文档: _share/路线共识.md R65]`。
- Rust 侧 `signal.rs` **把编码硬写死**：`pattern = e_bin*4 + f_bit*2 + n_bit`，签名收 `(n_cells,) u8 / (n_cells,) i32` `[代码: sim_core/src/signal.rs:33-46,69-88; sim_core/src/lib.rs:856-878]`。要让 Rust 跟上，必须改内核 + 函数级对拍 + 引擎级对拍；不跟上就必须加 config 门（见上）。
- 结论：**双路径不是技术上不可保，而是"契约成本"高**：每一个新机制都要（i）写 Python-only 门 + 显式报错、（ii）写不变量测试、（iii）在 manifest 里记录开关（否则 provenance 不可解析，R31/R38 硬门不过）。

### 2.4 会不会影响生态门（刚验证 10/10 过门）？

- **会，且是硬影响。** 生态门（N 稳定 >0 持续 50k）是**当前适应度景观**的性质；指称博弈要往世界里加一个"与生存无关的第二收益通道"，等于改适应度景观 ⇒ 该批 10/10 的结论**对新配置不再适用，必须重跑**。参照 R36：生态门通过只对**那一批**保留为独立结论。
- 具体风险两条：
  1. **能量守恒 vs 能量注入**。oracle 选择 S→R 转移（守恒）`[代码: oracle.py:73]`；tcl 式"双方 +6"若要守恒，只能 S 掏钱给 R ⇒ S 净亏 ⇒ 需要额外的"共同收益"来源，否则 g15 只会更快被淘汰（我方广播信道建议 §3.4 冲突 2 已写死：**先接回报通道，再谈扩信道**，否则"成本↑+收益仍为 0 ⇒ 只会加速 g15 灭绝"）`[文档]`。
  2. **饱和混淆**。我方种群常态顶到 `max_count=5000` / 7200 格（R32 已把饱和列为受控变量，R41 把"不饱和"列为默认）。新增收益通道会**加快饱和**，从而把 g15 的判读重新推进"饱和臂 vs 捕食主导臂差一个数量级"的方差陷阱 `[文档: _share/路线共识.md R44]`。

---

## 三、最关键的批判：L1/L2 会不会变成"按构造必然成立"？

**我的判断：分三级，答案不同——L1 变成"任务成功率"，L2 变成"不可观测指称的副产品"，L3 变成"按构造必然"，而 L4 被反向阻断。合起来：这会把"涌现"问题替换成"任务求解"问题，结论可外推性大幅下降。**

### 3.1 L1（信号携带信息）——不是逻辑必然，但不再是"涌现"

- 严格说 **MI>0 不是按构造必然**：接收者可以乱猜（tcl v6 随机策略的"组合解码"达 0.85，说明指标本身可能虚高）`[文档: tcl README v6/v8 节]`。所以指称博弈**不会**让 L1 变成恒真命题。
- 但它把 L1 的语义从"信号系统自发出现并携带环境信息"改成"**被试在人为任务上达标**"。任务、目标分布、奖励全由设计者给定 ⇒ L1 的通过只证明"我方的遗传+启发式决策机制能解一个通信任务"。
- 我方 L1 的定义是"信号↔环境/上下文 MI **显著高于漂变零模型基线**"`[文档: _share/战略定位-20260912-语言涌现可达性.md L0-L4 表]`。注意后半句：**判据本来就要求零模型对照**。所以即使在指称博弈里，只要老老实实跑 `signal_disabled` / `neutral` / `permuted` 三臂，L1 仍然是一个**有判别力的测量**——它测的是"我方机制的能力上界"，不是"语言涌现"。**这是这个实验唯一站得住的用途（阳性对照/仪器校准），前提是措辞上不许写成"涌现"。**

### 3.2 L2（任意性）——会变成"按构造必然"

- 任意性判据是"能指与所指解耦、非物理相似"。若指称对象对接收者**结构性不可观测**，那么从接收者视角看，任何一致的映射**都不可能**由物理相似性推出 ⇒ **任意性由世界结构保证，而不是被选择出来的**。
- 但注意一个真实的反例：tcl v4 反而长出了**描述性/象似性编码**（sender 看得见目标颜色 ⇒ sym1 系统性编码颜色，MI 0.399）`[文档: tcl README v4/v7]`。也就是说指称博弈首先给的往往是**象似**，不是**任意**；任意性还需要**瓶颈 + MDL 紧凑性偏差**才会成为被选择项。这恰好落在我方 **W-F3（MDL 紧凑性偏差实质缺失）** 上：**先补 W-F3，L2 才可能从"不可能"变成"被选择"；不补，L2 只会变成"因为接收者看不见，所以映射必然任意"的平凡结论** `[文档: _web_eval/FINDINGS.md W-F3; _share/评估-时间表与涌现概率-20260913-2212.md L2 行]`。
- 换句话说：issue 的建议**跳过了它自己该依赖的前提**。补 W-F3 + W-F4 才是我方文档认定"唯一最大杠杆"（R70），而 issue 只字未提 W-F3/W-F4。

### 3.3 L3（组合性）——如果照 tcl v6 的"单维度不可解"设计，就是按构造必然

- tcl v6 的做法是**人工保证**"目标与至少 2 个候选同色、至少 2 个同形 ⇒ 任何单一属性维度都无法唯一确定目标，只有组合才行" `[文档: tcl README v6 节]`。这是把"组合性必须出现"写进任务约束 ⇒ 一旦达标，**组合性是任务设计的重言式**，不是涌现。
- 更强的反证来自对方自己的 v8：消息被随机 permutation 后，**组合性指标反而更高**（0.246 > final 0.054）`[文档: tcl README v8 表]`。即：他们的组合性公式对"消息-语义脱钩"不敏感 ⇒ 在"按构造必然"的场景里，连指标本身都不可信。
- 所以我方 L3 若要碰，**必须同时上结构破坏型零模型**，否则结论不可外推。

### 3.4 L4（语义接地 / 可撒谎）——指称博弈（外挂版）会**反向阻断**

- 域外虚拟目标没有生态后果，符号指向的是一个"任务索引"而非世界状态。要"可撒谎"，必须存在**利害**（撒谎有收益、被抓有代价）`[文档: L4 判据]`。
- 因此：**为了把 L1-L3 做漂亮而注入域外任务，会把 L4 推得更远**。这是权衡上的反向激励，issue 没做这个取舍分析。

### 3.5 小结（可直接写进预注册）

> 指称博弈臂的产物只能表述为：**"我方引擎在人为通信任务上的可解性上界" + "L1/L3 仪器判别力的校准"**。
> 不得表述为"语言涌现""信号演化""任意性涌现"。这与 R30 的措辞纪律同构（g15 高 ≠ 语言涌现）`[文档: _share/路线共识.md R30]`。

---

## 四、"符号序列"建议是否成立？

### 4.1 先核实 issue 的那句话

> issue 原文："信号是单格单 uint8（16 模式），多发射者覆盖写，符号串数据结构不存在"

**核实结论：准确。** 逐条给行号：

| 声称 | 代码事实 | 判定 |
|---|---|---|
| 单格单 uint8 | `self._marks = np.zeros(world.n_cells, dtype=np.uint8)` `[代码: world/signal_field.py:39]`；模块 docstring："uint8 低 4 位（bit0~bit3 对应 4 个子格）" `[代码: world/signal_field.py:9]`；写入时 `pattern & 0x0F` `[代码: world/signal_field.py:49]` | 成立（更精确说：**uint8 容器，实际只用低 4 位**） |
| 16 模式 | `write()` 只保留 4 位 ⇒ 0-15 `[代码: world/signal_field.py:44-50]` | 成立 |
| 多发射者覆盖写 | `write_many` 用花式索引赋值 `self._marks[cells] = p` ⇒ **重复索引取最后一次**，无累积/无合并 `[代码: world/signal_field.py:52-58]`；Rust 侧同语义且**注释明写**"多 emitter 同格：按循环顺序后写覆盖先写" `[代码: sim_core/src/signal.rs:13,88-90]` | 成立（且双路径一致） |
| 符号串数据结构不存在 | `_marks` 形状 `(n_cells,)`，无槽位/时间维 `[代码: world/signal_field.py:19-25,39-40]`；快照也只有 `(n_cells,)` 两份 `[代码: sphere_engine.py:1687-1688]` | 成立 |

### 4.2 但"改成序列"在我方引擎里有两个它没算到的拦路虎（都为 `[实测]`）

**(1) 信道已经饱和，"有没有信号"几乎不携带信息。**

从已完成的 60k run 快照直接量（`the-world-data/_rerun_logs/a4_fix/*.snapshot.npz`，信号场 7200 格）：

| run | 场活跃格占比 | 有 ≥1 个带信号邻居的个体占比 | 平均带信号邻居数 | 邻居 pattern 平均"唯一率" |
|---|---|---|---|---|
| `asym_on_cb0_s42`（N=4999，终局 g15 0.217） | **57.8%** | **86.1%** | 6.40 / 8 | 0.45 |
| `asym_on_cb1_s45`（N=5000，终局 g15 0.803） | **82.6%** | **100%** | 7.57 / 8 | 0.32 |

`[实测]`。含义：`sp = sig_present[nb]`（存在项）对绝大多数个体**恒为 1**，在 softmax 里贡献一个常数 ⇒ **零判别力**。真正有判别力的只剩 pattern 内容通道（`0.4*perc*interp`）。

**(2) 字母表实际塌缩到 4 个符号，且内容高度重复。**

`asym_on_cb0_s42` 快照的 pattern 分布：活跃 4164 格中，`12/13/14/15`（即 `e_bin=3` 那 4 个）合计 **3921 格 = 94.2%** `[实测]`。这与我方文档"16 模式实际只用 4 种" `[文档: _share/战略定位-20260912 §L3 行]` 完全吻合，而根因就在 `state` 的高 2 位是**发送者自身能量档** `[代码: sphere_engine.py:693-695]`。

### 4.3 最小改动面（比 issue 想的大，但有一条捷径）

| 做法 | 改动面 | 与"逐位对拍/快照"的冲突 |
|---|---|---|
| **按 issue 原意**：相邻多格串读（cell 0 → cell 1 → cell 2 当词元序列） | 发射端要**顺序写多格**；但每格都可能被第三方覆盖 ⇒ "串"会被污染。要么引入写入锁（并发语义全变），要么接受噪声 | 🔴 高：并发写入语义、RNG 顺序、快照（`_marks`/`_age` 均须扩维+版本升级）、Rust 内核重写或强制 Python-only |
| **加内槽位**：`_marks` 改 `(n_cells, L)` uint8，`_age` 同 | 容器扩维 ⇒ `sphere_engine.py:676,750,758,792,813,864,1555,1586,1687,1836` 全部读写点要改；快照 `SNAPSHOT_VERSION` 升级 + 旧档回退 | 🟡 中：若**槽位选择确定**（无新 RNG）且 Rust 侧显式门口，可保住"关掉即逐位一致" |
| **零新容器捷径**：把现有 4 个 bit 重新解释为"时间序 4 槽移位寄存器"（词内结构，docstring 已提"田字格 4 位置提供词内结构" `[代码: world/signal_field.py:7]`） | 不用改 dtype/形状/快照/Rust 签名 ⇒ **双路径与快照零冲突** | 🟢 低，但**语义冲突**：现有 4 位是"能量2+食物1+邻居1"的并发状态位，改成时间序就废掉了现在的语义，必须作为**新条件（C6 类）预注册**，不能声称"没改 C1" |
| **先解决覆盖写与饱和**（我方广播建议 §6.3 的方案）：每 pattern 一条通道 `loudness[p,c]`，接收者按 `w_p` 竞争抽样（"谁最响听谁"），归一化而非求和 | 新容器，但**确定性、counter RNG、可并行** `[文档: _advice/设计建议-广播信道与共享收益-20260913.md §6.3]` | 🟡 中，但这是**唯一不会在满员球面上饱和**的方案 |

**结论**：issue 的"P1 符号序列"在**数据结构层**成立（"没有符号串"是事实），但在**工程与生态层**把顺序搞反了——我方自己的评估已写死："广播（扩信道）改变的是'谁能收到'，不改变'发送者为什么要有回报'；先扩信道 ⇒ 成本↑收益仍 0 ⇒ **只会加速 g15 灭绝**" `[文档: 同上 §3.4 冲突 2]`。序列化是"扩信道"的一种，**必须排在"回报通道"之后**。

---

## 五、issue 逐条核对（错漏清单）

### 5.1 "D2 四机制落地后 g15 反而更低（0.23~0.45 vs off 0.51~0.79）"

- **数字来源可追**：逐字来自我方 `[文档: docs/评估-EVAL/评估-EVAL-可行性分析-g15高位payoff-20260911.md:32-33]`（D2 on `0.234~0.449`；D2 off `0.51~0.79`）。**引用本身没有编造。**
- **但该对比有 ≥3 处未受控差异，不支撑因果**：
  1. **口径/时长**：on 是 60k tick，off 是 200k tick `[文档: 同上:32-33]`；
  2. **环境参数**：off 来自 `run_long_experiment.py` 且显式传 `--rotation-period 300` + `--lifespan-mult {1.0,0.5,0.25}` `[代码: experiments/run_long_experiment.py:59-63; 文档: 评估-EVAL-云端任务包-D21网格批次-20260909.md:54-59]`，而 on 那批的 config 指纹是 `rotation_period=2400`（默认）、`lifespan_mult=1.0` `[实测: a4_fix summary.json manifest.config_fingerprint]`；
  3. **是否 D2 关闭**：`run_long_experiment.py` 全文未设 `info_structure` ⇒ off 臂确实是 D2 全关 `[代码: grep 结果]`，但 on 臂的**机制档位**并未在文档中留档。
- **"四机制"这个标签大概率是错的**：该 on 批是 **D2.1 参数网格（lr 4 档 + mat 4 档，8 实验 × 60k）** `[文档: docs/决策与评审/战略转向-生态循环与食物链重构-20260911.md:54-61]`；而 D2 调优指南把参数网格的阶段序写为"阶段1（学习瓶颈+Steels 对齐，`arbitrary_codebook=False`，`perception_radius=8`，噪声/softmax=0）" `[文档: docs/评估-EVAL/评估-EVAL-D2参数调优实验指南-20260909.md:16,53-60]`，且 on 批的观察之一是"`lc_max=60` 打满"（学习瓶颈在动）`[文档: payoff 分析:33,39]`。⇒ `[推断]` 该批是 **2/4 机制、且无信息不对称**，**不是"四机制落地"**。（顺带暴露我方一处 provenance 缺口：09-09/09-11 网格批没有 manifest 指纹可查——与 R57 同型的风险。）
- **与更新的实测冲突**：`the-world-data/_rerun_logs/a4_fix/` 的 **R19 主口径 10 臂**（修复后代码、5 seed × 60k）里，`arbitrary_codebook=True` 的 cb1 组 **3/5 臂终局 g15 = 0.6598 / 0.7710 / 0.8032（>0.5）**；cb0 组 5/5 低于 0.32 `[实测: 各 CSV 末行]`。⇒ "D2 开 ⇒ g15 反而更低"在 R19 主口径下**反向**（这是"臂间分离"的直接读数，正是 R30/R37 讨论的那批）。

**判定：数字真实，标签错误，因果不成立，且已被更新数据部分推翻。**

### 5.2 "无信息不对称（D2 前感知半径 8 > 信号半径 1）"

- **括号里的机制描述错误**：信号写在**发射者自己所在格**（`self.signals.write_many(e_flat, patterns)`，`e_flat = self._flat[emitters]`）`[代码: sphere_engine.py:687,709]`；读取发生在**接收者的整个邻居集** `nb`：`sp = sig_present[nb]`、`nb_sigs = self.signals._marks[nb]` `[代码: sphere_engine.py:847,864]`；Rust 移动路径同样收到 `nb_table` + `signal_marks` `[代码: sphere_engine.py:767-771]`。⇒ **信号的读取范围 == 食物感知范围**，不存在"信号半径 1 < 感知半径 8"。
- 更根本的是：`perception_radius` **不是欧氏半径**，而是**邻居集基数**（8=Moore 全邻，4=Von Neumann 正交邻）`[代码: simulation/config.py:264; world/sphere_world.py:255-279,281-306]`；`neighbors()` 返回 8 个相邻格、不含自身 `[代码: sphere_world.py:281-306]`。
- **但它的结论方向仍然对**，只是理由要换成更硬的三条：
  1. **内容冗余**：`state` 的 `f_bit` 是"发送者所在格资源 > 0.5×容量" `[代码: sphere_engine.py:696-699]`——而接收者在评估相邻格时**直接、当前地**看到同一个量（`fr = food_ratio[nb]`）`[代码: sphere_engine.py:846]`；
  2. **信号还更差**：信号是**滞后 ≤50 tick 的 1 bit 量化副本**（`duration=50`，`_age` 剩余寿命）`[代码: signal_field.py:27,39-40,50,72-78; sphere_engine.py:221]`，且要花 `SIGNAL_COST=0.1` `[代码: sphere_engine.py:682]` ⇒ 对 `f_bit` 这部分**信息价值 ≤0**；
  3. **唯一非冗余位（`e_bin`=他人能量档、`n_bit`=他人格的拥挤度）与接收者适应度无关**；且存在项已饱和（§4.2 实测：86-100% 个体邻居里有信号）⇒ 判别力趋零。
- **判定：机制声称错（读取范围不是 1 格），结论对（无信息价值），理由错位。**

### 5.3 "信号只是移动打分弱加分项（0.5 权重与食物并列）"

- **存在项：准确**。`score = perc*(fr*0.5 + sp*sig_weight) + soc*densities[nb]`，其中 `sig_weight = trust*(0.5 + rep_w*trust) ≤ 0.5`，即**存在项 ≤ 食物项** `[代码: sphere_engine.py:856-859]`。
- **但漏了两条信号相关通道，因此"信号只是弱加分项"是**不完整**的**：
  - **内容项** `score += 0.4*perc*interp`（`interp = _interpret[idx, pattern]`）`[代码: sphere_engine.py:864-870]` ⇒ 权重 0.4 = 食物项的 80%，且**per-pattern 内容相关**；
  - 记忆项 `0.3*perc*mem` `[代码: sphere_engine.py:860-863]`。
- 因而"无内容通道"这个推论是错的：**内容通道存在且权重不低**；真正的病根不是权重，而是**payload 冗余 + 信道饱和 + 字母表塌缩（94.2% 集中在 `e_bin=3` 的 4 个 pattern）** `[实测 §4.2]`。
- **判定：数字对，定性偏（"信号只是弱加分项"低估了 0.4 权重的解读表通道）。**

### 5.4 "千代战役……只会产出'统计显著地证明没反应'"

- **不可证伪**：这是一个没有预先定义判据的预测（我方 R4 的原判据已被实证有缺陷、R7 已作废单点阈值 `[文档: _share/路线共识.md R7]`）。
- **它以"已证明"的语气引用一个我方明确拒绝下结论的命题**：我方状态评估写得非常清楚——"剔除崩溃尾后 N≥30 的 5 个 run 中 3/5 终局 g15 > 0.5（0.563/0.551/0.887@60k），2/5 < 0.5 ⇒ **现有数据既不足以支持也不足以否证『缺 payoff 通道』，n=5 不得写成任何方向的结论**" `[文档: _eval/状态评估-20260913.md:99-106]`；且 **R8 措辞纪律**明确禁用"生态崩溃证实缺 payoff 通道"类表述 `[文档: _share/路线共识.md R8]`。issue 的"payoff 死结已证明"正落在禁用口径内。
- **它也是自我挫败的**：我方 R27 已把主线叙事转为"定界性贡献（负结果即产出）" `[文档: _share/路线共识.md R27]` ⇒ "统计显著地证明没反应"**本身就是可发表的定界结果**，不是浪费；把它当反例来说服我们放弃战役，逻辑上不成立。
- **判定：夸大 + 与 R8/R27/状态评估冲突 + 有实测反例（§5.1 的 3/5。另：本批"捕食主导 ⇒ g15 最低"确实成立——`cb0_s44=0.0251`、`cb1_s44=0.0443` 为全批最低，但本批是 **2 个**捕食主导臂，不是 3 个 `[实测]`。)**

### 5.5 其它错漏（issue 表格与建议层）

| issue 声称 | 核对结果 |
|---|---|
| 表格"发射者收益：❌ 只有成本 0.1 能量/次" | **不完整/过时**：我方已实现 oracle 发送者获益通道（`simulation/oracle.py`，R39/D-8，config `OracleConfig`，含单元测试）`[代码/文档]`。准确说法应是"**缺正收益通道**（oracle 终身封顶=保本，只能补偿不能补贴）" `[代码: oracle.py:26-28,1376-1384]` |
| "think-chain-life 之所以能涌现 v3/v4 语言，**本质是指称博弈**给'发射'创造了直接选择压" | **被对方代码与其自身报告共同证伪**：`environment.py`（v3）中 `ref_game|reference` **零命中**，`environment_v4.py` 才有 `pending_reference/_check_reference_reward`，`environment_v5.py` 才有显式配对指称博弈 `[实测: Select-String 计数]`；v3 的成功要素按其 README 是"Epoch 内冻结策略 / 极短视野（半径 2）/ 小词汇表（4 符号）/ **通信零成本**" `[文档: tcl README v3 节; 代码: train_v3.py:206-207]`。而且据其姐妹报告 IKFJJ3：加了指称博弈的 v5 同口径组合性 **0.061 < v4 0.183** `[文档: _eval/_issue_think-chain-life.md:60]`。⇒ **"指称博弈 ⇒ 语言涌现"这条因果链，在对方自己的数据里是负的。** |
| "P0 等价于你们 R1，但建议用指称博弈形态；先验 R2 再上指称博弈" | **与 R28 裁定冲突**：R28 已把 A-4（Lewis 化/私有信息+共享收益）**暂缓**，理由是与 C3 自变量污染风险，须先钉死"共享收益 vs 声誉"的区分表 `[文档: _share/路线共识.md R28; _advice/设计建议-广播信道与共享收益-20260913.md §1.3]`。issue 把"R2 声誉"与"指称博弈"当作同一条路的两个阶段，恰是 R28 要防的**解读混同**。 |
| "P1 给'状态→符号'发射加可学习组件（表格RL/小网络）" | **与引擎契约冲突**：我方是"基因=参数、无梯度"的遗传架构；双路径对拍、`validate_gene_wiring`、快照指纹都是围绕它建的。引入学习头等于换引擎，且 tcl 的最大不稳定源正是 PPO（确定性评估策略崩溃、v5 训练期尖峰不可复现）`[文档: tcl README v5 节]`。 |
| "P2 信息不对称拉到'通信必需'——感知半径 4 后仍可再降" | **当前代码不可实现**：`assert self.perception_radius in (4, 8)` `[代码: simulation/config.py:287]`；`AGENTS.md` 虽写"只支持 4/6/8"，但 6 档是已知静默 no-op（F1/R14）`[文档]`。要"再降"必须改邻居索引逻辑。另外用降半径实现"通信必需"是错的方向：**该做的是让指称对象不可观测（私有信息），而不是让整个视野变糊** `[文档: _advice/设计建议-20260912.md §3.3]`。 |
| "我们有的对方缺"三条（四臂零模型 / 多 seed+CI+Observed-Inferred-Assumed / 同 seed 逐位可复现+数据守恒审计） | **方向成立**：对方仓库**完全没有测试目录** `[实测: 无 tests/ 也未出现在文件清单]`；其 v7 才补上"5 种子×3 模式×随机基线"，v8 才补上 Permuted 零模型——**都是在我们这份 issue 之后**（v8 commit 就是"回应 issue 评论建议"）`[文档: tcl README v8 节]`。但我方必须诚实地附加限定：这批护栏**尚未产出正面结果**，且我们自己的 R19 批 provenance 只有 3/10 达标（R57/F-R9）`[文档]` ⇒ 可输出"**流程**"，不可输出"**结果优越性**"。 |

### 5.6 错漏清单（速览）

1. 【错】**"D2 四机制落地后 g15 反而更低"**：标签错（那批是 lr/mat 参数网格，非四机制）、对比未控（60k vs 200k、rp2400 vs rp300、lifespan_mult 不同）、且已被 R19 主口径 3/5 反向。
2. 【错】**"信号半径 1 < 感知半径 8"**：读取走的是接收者整邻居集；`perception_radius` 是邻居基数不是距离。结论对但理由应换成"内容冗余 + 滞后 + 饱和"。
3. 【偏】**"信号只是弱加分项（0.5 与食物并列）"**：漏掉 0.4 权重的内容通道（`interp`），低估了内容相关性。
4. 【夸】**"payoff 死结已证明"**：与我方状态评估（既不足以支持也不足以否证）与 R8 措辞纪律直接冲突。
5. 【夸】**"千代战役只会产出统计显著地证明没反应"**：不可证伪 + 与 R27"定界性贡献"定位矛盾。
6. 【错】**"tcl 语言涌现的本质是指称博弈"**：被对方代码（v3 无此机制）与其自身核验报告（v5 反降）双重证伪。
7. 【漏】**未提 W-F3 / W-F4**：我方文档认定这是 L2/L3 的**唯一最大杠杆**（R70）；不补则 L2 只会变成平凡结论（§3.2）。
8. 【漏】**未提代价**：新增实体/任务状态机、能量守恒 vs 注入、生态门重跑、双路径门槛、饱和混淆。
9. 【漏】**未提已有重复**：P0 与我方已暂缓的 A-4 重复；P1 序列化与我方广播信道评估已给出的"分通道竞争抽样"方案重复（且后者更完整）。
10. 【细】**"捕食主导 3 个臂"**：本批实测为 **2 个**（`cb0_s44`/`cb1_s44`），与 R44 记录的"捕食主导臂 0.025~0.044"一致。

---

## 六、prompt-life 是什么？值不值得看？

### 6.1 `[实测]` 结论：**它不是独立项目，是 think-chain-life 在 v7 阶段的同一份代码快照。**

- 逐文件 MD5 比对：**共同文件 45 个，44 个完全一致**；唯一不同的是 `README.md` `[实测]`。
- **只存在于 think-chain-life 的 9 个文件**全部是 v8 系列：`agent_v8.py / analyze_v8.py / config_v8.py / environment_v8.py / evaluator_v8.py / info_eval_v8.py / language_analysis_v8.py / train_v8.py / visualization_v8.py`；**prompt-life 独有文件：0 个** `[实测]`。
- README 逐行 diff：prompt-life = 308 行，think-chain-life = 344 行；**prompt-life 独有的 12 行全是旧版目录树清单**，think-chain-life 独有的 48 行全是 v8 章节 `[实测]`。⇒ 同一文档的**较早修订**。
- 同作者、同 `push.sh`（一键 push 到各自 origin）、commit 标题同为"Phase R4 最终报告（v1–v7）" `[实测/代码: push.sh]`。
- **两侧 `README.md` 都已包含对 IKFJJ3 的 2026-09-14 更正**（0.463 是 epoch 10 尖峰、确定性评估 0.061、sym2 二项检验 P≈1.0、口径混用）`[文档: 两侧 README 开头"评估口径声明"]`。

### 6.2 值不值得看

- **不值得单独投入时间。** 它不包含 think-chain-life 之外的任何信息：v1–v7 全部文件逐字节相同，v8（**唯一真正有方法论增量的一段**：Permuted 零模型 + Topographic Similarity + zero-shot holdout + 信道腐蚀）**只在 think-chain-life 里有**。
- "无人给它提 issue"不是因为"被忽视的遗珠"，而是因为它是**被取代的副本**：issue 作者既然做过 IKFJJ3 的代码级复核，本可自己发现两个仓库同源——这条也提示我们：**对方报告里的"独立验证"只覆盖了一个仓库、且没有做同源检查**，其"双向借鉴清单"的"双向"两字因此有一半是空的（它给 prompt-life 侧的"反向借鉴"没有任何新的落点）。
- 处理建议：在 `_eval` 留一行登记即可（"prompt-life = think-chain-life@v7 同源副本，唯一增量在 think-chain-life v8"），**不 clone、不排期、不列入借鉴清单**。

---

## 七、行动建议：该采纳 / 该拒绝 / 该延后

### 7.1 该采纳（优先级 P1，且都能补我方 F-D3）

| # | 条目 | 理由 | 落点 |
|---|---|---|---|
| **A1** | **结构破坏型零模型（Permuted / derangement 臂）** | tcl v8 实测：消息被随机 permutation 后组合性 **0.246 > final 0.054** ⇒ 其组合性公式对"消息-语义脱钩"完全不敏感 `[文档]`。我方 L1/L3 至今**没有漂变/结构零模型**（F-D3：全仓零 CI、零假设检验，R7 的新判据因此"不启用"）`[文档: _eval/状态评估-20260913.md 缺陷5]`。这是"按构造必然"批判（§三）唯一的解药：**没有 Permuted 臂，任何 L1/L3 读数都不可解释** | 判据工具线 P1（漂变零模型）里加一臂：保持 pattern 边际分布、打乱 pattern↔上下文配对 |
| **A2** | **Topographic Similarity 作为 L3 候选指标** | tcl 自己给出"用 TS（语义距离×消息距离的秩相关）替代 0.5×多样性+0.5×MI强度"的判断，且其 v6"功能性组合 0.92"正是被旧公式误判成伪影 `[文档]`。我方 L3 目前只有"16 模式实际只用 4 种"这种粗读数 `[文档/实测: 94.2% 塌缩到 4 pattern]` | `_share/战略定位` L3 行补一行"候选指标 + 随机基线判别力检验"，先离线在已有快照上验证判别力 |
| **A3** | **"任何语言指标必须报随机策略基线 + 判别力检验"写成硬门** | 这是对方用两次事故（v6 伪影、v8 Permuted 反超）换来的教训，且与我方 R31/R38"六门"同构，可直接合入 | 并入 R38 硬门清单；同时给 D-16 指标补"随机策略基线"列 |

### 7.2 该拒绝

| # | 条目 | 理由 |
|---|---|---|
| **R1** | **拒绝把 tcl 式"域外指称博弈（虚拟候选物 + 外部 +6 奖励）"作为主线 P0** | (a) 它要求在世界里注入**与生存无关的第二适应度通道**，把"死规则内自涌现"替换成"人为任务求解"（§3）；(b) 无生态接地 ⇒ **反向阻断 L4**（L4 要求可撒谎，必须有真实利害）；(c) 与 **R28 暂缓 A-4** 的裁定冲突，且 issue 的表述正是 R28 要防的"共享收益 vs 声誉（C3）**解读混同**"；(d) 其经验前提（"指称博弈是 tcl 语言涌现的本质"）**被 issue 自己的 IKFJJ3 报告与对方代码共同证伪**（v3 无此机制、v5 反而更差） |
| **R2** | **拒绝"P1 先做符号序列"的排序与理由** | 我方自己的评估已写死顺序：**先回报通道，再扩信道**，否则"成本↑收益仍 0 ⇒ 只会加速 g15 灭绝" `[文档: 广播建议 §3.4 冲突2]`；且实测信道已饱和（57.8–82.6% 格有标记、86–100% 个体邻居有信号）`[实测]`，此时加位宽只会加深"内容被第三方覆盖"的噪声 |
| **R3** | **拒绝"给状态→符号发射加可学习组件（表格 RL / 小网络）"** | 与我方"基因=参数、无梯度"引擎契约冲突（双路径逐位对拍、`validate_gene_wiring`、快照指纹全部围绕它建立）；且 tcl 的最大不稳定源正是 PPO（确定性评估崩溃、训练期尖峰不可复现）`[文档]` |
| **R4** | **拒绝 issue 关于"payoff 死结已证明"的既成表述** | 与 R8 措辞纪律、与我方状态评估"既不足以支持也不足以否证"直接冲突；同时拒绝"千代战役只会产出无反应"的不可证伪预测（R27 已定界性贡献为正当产出） |

### 7.3 该延后（并给出前置条件，避免"先开发后作废"）

| # | 条目 | 延后理由 | 前置条件 |
|---|---|---|---|
| **D1** | **私有信息 + 共享收益（= 我方 A-4 / §3.1 / §3.3，比 issue 的 P0 更完整）** | R28 已裁"暂缓"，理由是 C3 污染风险；且我方评估认定"不补 W-F3/W-F4，L3 在原理上长不出来"（R70）——**顺序应是先补设计缺口，再动收益通道** | ① C3 区分表**写死**（同一次交互 vs 跨回合）；② **前置判据**：先算"恒发 vs 恒不发"突变体的能量收支差 `ΔE`，要求 `ΔE > 日内能量波动 σ_E`，否则该档**不具备可测性，不必开跑** `[文档: 广播建议 §1.2]`；③ 生态门重跑口径（R32/R41：不饱和） |
| **D2** | **符号序列 / 多槽位信号** | 数据结构层可以改，但生态层顺序反了（见 R2）；且容器扩维会动快照版本与 Rust 内核 | 先落地 D3（分通道竞争抽样），再谈位宽；若做，**必须预注册为新条件（C6 传播信道/词法结构），不得声称"未改 C1"** `[文档: 广播建议 §3.4 冲突1]` |
| **D3** | **每 pattern 分通道 + 接收者竞争抽样（解覆盖写与饱和，我方建议已给规格）** | 它才是"序列/扩容"的技术前提；确定性、counter RNG、可并行，对拍友好 | 新容器 → `SNAPSHOT_VERSION` 升级 + 旧档回退；Rust 侧显式门（照 C-8 先例 `sphere_engine.py:310-315`，禁静默忽略） |
| **D4** | **把"指称对象放到感知范围外"当作独立实验（而非 oracle 的替代）** | 用"降 perception_radius"实现**当前代码不可行**（`config.py:287` 只允许 4/8；6 档静默 no-op，F1/R14） | 先补"私有状态位"这类**结构性**不可观测，而不是整体降视野 `[文档: 设计建议-20260912 §3.3]` |
| **D5** | **与 tcl 作者的进一步对拍/移植协作** | 我方当前最缺的不是机制，而是**判据工具与 provenance**（F-D3 / R57 3-of-10） | 定位为"**仪器校准的参考实现**"，不作为设计采纳；且先做 §6 提示的**同源检查**（对方报告未发现两仓同源） |

### 7.4 一句话优先级

> **先做 A1/A2/A3（判据工具，1 周量级，直接补 F-D3）→ 再补 W-F3/W-F4（R70 认定的唯一最大杠杆）→ 之后才谈收益通道（D1）；tcl 式域外指称博弈与"先做序列"一律不做。**

---

## 附：本报告用到的可复现命令

```powershell
# 1) 两仓同源判定（prompt-life = think-chain-life@v7 快照）
Get-ChildItem think-chain-life -File | Get-FileHash -Algorithm MD5   # 与 prompt-life 逐项比对：44/45 相同，9 个 v8 文件缺失，0 个独有
Compare-Object (Get-Content prompt-life\README.md) (Get-Content think-chain-life\README.md)

# 2) a4_fix 10 臂 g15 首末值（R19 主口径）
Get-ChildItem "$d\*.csv" | ForEach-Object { $r=Import-Csv $_.FullName; "$($_.Name) $($r[0].g15) -> $($r[-1].g15) N=$($r[-1].N)" }
#   cb0: 0.4873/0.4443/0.4368/0.4814/0.3959 -> 0.2166/0.3197/0.0251/0.1053/0.0847
#   cb1: 0.4989/0.4717/0.4218/0.4852/0.4436 -> 0.7710/0.6598/0.0443/0.8032/0.1713

# 3) 信号场饱和与字母表塌缩（直接量快照，不跑仿真）
#   signal_marks: cb0_s42 活跃 57.8%、pattern 12~15 占 94.2%；cb1_s45 活跃 82.6%
#   逐个体邻居集：cb0_s42 86.1% 个体 ≥1 信号邻居（均 6.40/8）；cb1_s45 100%（均 7.57/8）
```
