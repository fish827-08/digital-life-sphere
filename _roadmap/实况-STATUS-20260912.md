# 项目实况（STATUS）— digital-life-sphere 当前状态报告

> 版本：v1.0 | 建立：2026-09-12 | 维护：项目全体成员（规则见文末）
> 定位：**"此刻的快照"**——主干在哪、测试/性能几何、Gate 判定现状、实验队列、阻塞项、下一步动作。
> 真身目录：`_gitee_review/`。与本文件配套的长期视角见 `路线图-ROADMAP-20260912.md`。

---

## 〇、一句话实况

> 项目处于**"D2 机制已实现但验收门未过"的决策分岔点**：语言正选（g15）14/14 全部 peak-then-revert；
> 战略转向（生态循环）被内部评审暂缓；**当前最有证据的下一步 = R2 声誉权重 1 行证伪（约 1 天成本）**。

---

## 〇.5 实况一图速览（ASCII）

```
digital-life-sphere —— 项目实况（2026-09-12）
│
├── 主干
│   ├── 目录：_gitee_review/（唯一评估对象）
│   ├── main @ 0909b5b（本地领先）
│   ├── 测试：216 passed / 1 skip / 3 env-error（无失败）
│   └── ⚠️ Rust扩展缺 step_tick → 1062行 tick.rs 静默skip
│
├── 性能
│   ├── Rust 526t/s(N=2000) / 282t/s(N=4000) = 11.35× / 12.50×
│   └── rayon 确定性：1/2/4/8/16线程哈希全同 ✅
│
├── Gate 判定（语言涌现结论链 = ❌ 不通过）
│   ├── G1 数据有效性 ❌ （D2.1单轨迹 / MI归档失效）
│   ├── G2 对照有效性 ❌ （neutral臂崩溃N=2~5）
│   ├── G3 统计功效  ❌ （实为1 seed）
│   ├── G4 指标效度  ❌ （cult_div必然归零 / trust是食物代理）
│   ├── G5 静默错误  ⚠️ （F-B4开放）
│   ├── G6 验证覆盖  ❌ （tick.rs静默skip）
│   └── G7 叙事强度  ❌ （陈旧副本语气过强）
│
├── 战略转向（R1评审 = ❌ 不通过）
│   └── 诊断成立 / 处方未成立 / 经验基础是病数据
│
├── 关键数据
│   ├── D1四臂：g14=0.47~0.50（强正选不复现）；g15漂变
│   │   ├── sigdis臂 trust 恒0.5000（=食物代理）
│   │   └── cult_div不发射也趋同（=热扩散）
│   ├── D2.1：seed维无效（1条轨迹）/ 参数维有效
│   │   └── 14/14 终局g15<0.5 且全部 peak-then-revert
│   └── lr细扫：云端完成（d6c5fca）→ 诊断闭环 ✅
│
├── 实验队列
│   ├── lr细扫     ✅ 完成（云端）
│   ├── battle     ✅ 6个CSV已产出（待判读）
│   ├── verify_s1_long ⬜ 本地待跑（S1放行前置）
│   ├── D2.1 seed补跑 ⏸ 暂缓（排在R2之后）
│   ├── R2声誉权重 🔄 云端今晚首选（文献最扎实）
│   └── neutral修正 / MI分箱修复 🔄 待做
│
└── 评估线组织
    ├── _gitee_review/  项目主干（代码+实验+文档）
    ├── _eval/          内部评估专员（Gate G1-G7实测）
    ├── _web_eval/      评估专家·联网线（文献核查）
    │   └── V2结论：食物链↔语言无文献；C3声誉最扎实
    └── _roadmap/       本项目（路线图+实况图）
```

---

## 一、主干事实（2026-09-12 核）

| 项 | 值 | 来源 |
|----|-----|------|
| 主干目录 | `_gitee_review/` | BASELINE B-0 |
| 主分支 commit | `main` = `0909b5b`（哨兵测试；本地领先） | git log |
| 主干真身 | 第二代 latest；`digital-life-sphere/` 为陈旧副本勿评估 | BASELINE B-0 |
| 测试 | **216 passed / 1 skipped / 3 环境 error**（无失败）；审计独立复跑 214/2/3 | BASELINE B-1 |
| Rust 扩展 | 18 函数 | BASELINE B-0 |
| ⚠️ 扩展缺口 | **仍无 `step_tick`**（S1 融合路径未放行）→ 1062 行 `tick.rs` 静默 skip | BASELINE B-0/B-4 |
| 性能 | Rust 526 t/s（N=2000）、282 t/s（N=4000）；**11.35× / 12.50×** | BASELINE B-2 |
| rayon 确定性 | 1/2/4/8/16 线程哈希全同（无并行浮点归约） | BASELINE B-2 |

---

## 二、Gate 判定现状（针对"语言涌现"结论链）

> 依据：`_eval/README.md`（2026-09-11）。总判定：**语言涌现结论链不通过。**

| Gate | 判定 | 首要阻塞 |
|------|------|---------|
| G1 数据有效性 | ❌ | F-D9（D2.1 网格为单轨迹、manifest seed=42）、F-D5（MI 归档失效） |
| G2 对照有效性 | ❌ | `neutral` 臂崩溃（N=2~5）；F-D6 密度混淆 |
| G3 统计功效 | ❌ | F-D3 零推断机制、F-D9 实单 seed |
| G4 指标效度 | ❌ | F-M1（cult_div 必然归零）、F-M2（trust 是食物代理） |
| G5 静默错误 | ⚠️ | F-D1 已修；F-B4 仍开放 |
| G6 验证覆盖 | ❌ | F-V1（tick.rs 静默 skip） |
| G7 叙事强度 | ❌ | F-R4 陈旧副本肯定语气结论 |

**战略转向（R1 评审）：不通过** — 诊断成立 / 处方未成立 / D2.1 经验基础是病数据（G1/G2/G3 不通过）。

---

## 三、最近关键数据（已核实）

### 1. 零模型四臂（D1，300k tick × 3 seed）
- g14 四臂全部 0.47~0.50 → **旗舰"g14 强正选"不复现**
- g15 跨臂跨 seed 方向不一致 → 近中性漂变
- sigdis 臂 trust **恒 0.5000** → trust 是食物富余度代理
- cult_div 不发信号也趋同 → 图热扩散机械必然
- ⚠️ neutral 臂失效（全冻结→灭绝 2/5/2）；修正设计（只冻结目标位）未执行

### 2. D2.1 参数网格——**seed 维无效，参数维有效**
- 7 组配对：3 组逐字节全等，4 组共享前缀 → 实为 1 条轨迹
- **但**：不同 lr 产出不同轨迹；14/14 终局 g15 < 0.5 且全部 peak-then-revert
- → **诊断为可复算、与 seed 维无关**；"2 seed 复现"表述作废

### 3. lr 细扫（云端已完成，`d6c5fca`）
- lr={0.05,0.075,0.125}×s{42,43} 全部完成；**全部 peak-then-revert，终局 <0.5**
- 结论：**无发送者私利时稳态 g15>0.5 机制性不可达** —— 战略诊断的经验闭环

### 4. battle 参数验证
- `long_bat_rp300_*.csv`（6 个）已产出；rp=300 世代压缩 200k tick——结果待判读

---

## 四、实验与任务队列（今晚视角）

| 项 | 归属 | 状态 | 结论/动作 |
|----|------|------|----------|
| lr 细扫 | 云端 | ✅ 完成（d6c5fca） | 诊断闭环；无需重跑 |
| battle 参数 | 云端 | ✅ 产出 | 待判读 |
| verify_s1_long 10 万 tick | **本地** | ⬜ 未跑 | S1 融合路径放行前置 |
| D2.1 seed 维补跑 | 云端 | ⏸ 暂缓 | R1：排在 R2 证伪之后 |
| **R2 声誉权重实现** | **云端（开发）** | 🔄 待派 | **今晚建议首选**（文献最扎实） |
| neutral 零模型修正 | 云端/本地 | 🔄 待做 | 方向性结论前提 |
| 哨兵测试 | 本地 | ✅ 已入 main（3146c63/0909b5b） | — |
| Signal_context_mi 分箱修复 | 本地 | 🔄 待做 | MI 报告前置 |
| S1/S2/d2-vec 分支合入 | 本地审查 | 🔄 待合并 | —— |

**今晚云端建议**：做开发（R2 声誉权重 + 可选 neutral 修正），**不做实验**（lr 已闭环、seed 补跑暂缓）。

---

## 五、工作区生态（评估线组织）

| 目录 | 角色 | 侧重点 |
|------|------|--------|
| `_gitee_review/` | 项目主干（唯一评估对象） | 代码 + 实验 + 文档 |
| `_eval/` | 内部评估专员 | 实验/代码 Gate 验证（G1-G7 实测优先） |
| `_web_eval/` | 评估专家（联网线） | 公开文献/已有事实核查方向可行性 |
| `_roadmap/` | **本项目（路线图+实况）** | 发展全景 + 当前快照 |

**联网线已交付**（2026-09-12）：V1 理论引用全部真实；V2 "食物链↔语言"无文献支撑；声誉/间接互惠（C3）文献最扎实；R2 路线获独立印证。

---

## 六、实况维护规则（对外声明）

1. **本文件回答"现在怎样"**：快照型，记录可就地更新；重大发展请同步更新 `路线图-ROADMAP`。
2. **大家一起维护**：任何成员在实验判读、Gate 判定、分支合并、云端交付后，应更新对应小节并附日期与 commit。
3. **事实溯源**：数字类条目标注来源（`_eval/BASELINE.md`、git、CSV）；未核实的标 `[待核]`。
4. **Gate 现状只增不改**：判定变化通过追加记录表达（如"G1 已于 XXX 由 ❌ 转 ✅"，附证据）。
5. **更新时机**：每次 Gate 判定变化、每轮实验落地、每份关键文档发布时更新；版本号随内容推进。
6. **失效数据纪律**：已判定失效的数据（D2.1 seed 维、MI 0.0 归档）必须保留标注，不得被再次当结论引用。