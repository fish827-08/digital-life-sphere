# AI Code 开发规则（仅本地保留，不提交）
AI-CODE-DEVELOPMENT-RULES.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# Rust
target/
Cargo.lock

# 实验结果与数据（不入库）
results/
*.csv
*.json
!pyproject.toml

# 前端
node_modules/
dist/
*.map

# IDE
.idea/
.vscode/
*.iml
# 构建日志（临时产物）
build_log.txt


# Rust 编译产物
sim_core.so
sim_core/target/

# 实验输出与临时可视化
long_run_result.txt
language-path.html
long_run_l4l5_result.txt

# 快照运行时文件
snapshot.npz
long_run_*.csv
long_run_*.txt

_rerun_logs/*
experiments/local_rerun_d2x.bat

# === 2026-09-12 迁移后：本地副本/备份/独立仓库（不入主仓） ===
.git.backup-*/
_gitee_review/
_backup_full_*/
_clockcheck/
_wt_*/
digital_life/

# === 2026-09-13 修正：评估线工作区须入库（AGENT.md §一定义） ===
# _eval/（内评工作区）、_web_eval/（联网线工作区）、_roadmap/、_advice/
# 此前被误加 ignore，导致 CHARTER/GATE-CRITERIA/BASELINE/FINDINGS 未受版本控制
# ⇒ 已移出 ignore，恢复正常跟踪

# === 2026-09-13 仓库瘦身：实验数据移入独立数据仓库 the-world-data ===
data/snapshots/
*.npz

# === 2026-09-13 其他不入库项 ===
.git-rewrite/
.workbuddy/
digital-life-sphere/

the-world-data/

# 2026-09-13 恢复过程残留
_reclone_tmp/
.git.corrupt-*/

# === D-23a（F-R7）：放开"单一最新快照"目录，使断点状态可入库 ===
# 背景：R19 快照被忽略 ⇒ 本地接不上、只能从 0 重跑（白丢 ~9h）。
# ⚠️ gitignore 后写者优先 ⇒ 这段必须放在 `*.npz` 之后才生效。
# ❌ 切勿放开 *.npz 通配：120 个/run × 4 MB ≈ 480 MB/run，会把仓库再撑爆。
!_rerun_logs/snap/
!_rerun_logs/snap/*.npz
!_rerun_logs/snap/*.pkl

# === R111：写入锁（同机机制，锁文件不入库；说明见 _share/.locks/README.md）===
_share/.locks/*.lock
