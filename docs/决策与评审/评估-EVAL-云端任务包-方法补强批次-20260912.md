# 云端任务包：方法补强批次（D2 网格重跑 + 语言指标修复）（2026-09-12）

> 依据：内部评估专员 FINDINGS（R1/R1b）+ 本地可行性分析共识 + 审计评估复核。
> 核心原则：**seed 维失效、参数维保留**（F-D9 精化）——本批次不推倒诊断，只补严谨性。
> 基线：gitee main = **0909b5b**（含 run_d2 --seed 修复 db0aef1 + F-D10 哨兵测试 + np.random.seed 可复现性，22+ passed）。

---

## 前提（必做）

```
git fetch gitee && git checkout main && git pull gitee main   # main = 0909b5b
python -m pytest tests/ -q                                    # 应全绿（警告：F-V1 tick.rs skip 属已知，勿当失败）
```

**硬纪律**（沿用）：
1. **不得改动引擎机制语义**（含移动/信号/解读段数值）；只允许：实验脚本、指标脚本、manifest/指标产物
2. 数据/产物只进 gitee；结果推分支 `feat/method-fix`
3. 简报只列数字/产物；**不做科学判读**（判读留本地/内部评估）
4. 受阻停下报告，不硬绕

---

## 任务 A：D2 参数网格重跑（seed 真随机，≥5 seed）——补 F-D3 功效 + F-D11 自描述

**目的**：se editable-"seed 维 3 seed 判据"升级为 **≥5 seed 方向一致 + mean±std**；manifest 完整自描述。

**档位**（覆盖关键参数区，15 实验 + 可扩展）：
```
lr = {0.05, 0.10, 0.30} × seed = {42,43,44,45,46}   （学习率蜜月区/无蜜月区）
基准档：lr=0.15 × 同 5 seed（可选，若前 15 顺利）
```
命令模板（tag 建议 `d2x_lr010_s42` 等）：
```
python experiments/run_d2_experiment.py --ticks 60000 \
  --tag d2x_lrXXX --learning-bottleneck --steels-alignment \
  --learning-rate 0.10 --seed 42
```

**产出要求**：
1. 15 个 CSV + 15 个 manifest；**manifest 必须含**：`final_tick`（实际 60000）、`config.seed`（=num）、`commit`（main hash）、`sim_core` 指纹（若可读）、参数全镜像
2. **补 F-D11**：续跑/重跑场景必须更新 manifest（不许 ticks=1000 而产出 60000）
3. 简报：每档 5 seed 的 final_g15 **mean±std** + g15 峰值@tick 区间（只需数字）
4. 判读哨兵检查（你自己跑通验收）：同档不同 seed 的 CSV **不得**逐字节相同

---

## 任务 B：语言指标修复（signal_context_mi 分箱 + 置换检验）——补 F-D5 残余 + F-R2

**背景**：内部评估裁定 → 归档 `0.0` 失效，正确值 ≈ **0.1275**（normalized 0.0869，active 3993）；但**分箱退化**（bin0=3569/89.4% 单箱主导）导致 MI 估计不稳（不同分箱 0.30 vs 0.13）。

**任务**：
1. **修分箱**：`experiments/language_analysis.py` 的 `signal_context_mi`——语境（food 比例）与信号（16 模式）分箱改为**自适应/等频**（避免单箱 >85%）；分箱方案进函数文档与输出
2. **置换检验**：对 MI 补 **1000 次 shuffle 置换**，输出 `p_value`（零模型阈值 p95）
3. **重算全部归档快照**的语言指标 JSON：
   - `data/snapshots/snapshot_small.npz`（tick=1M）
   - 大世界 1M（若有 `snapshot_large.npz`）
   - D1 零模型快照（zero_neutral/random/sigdis，若有）
   - 每个产物 JSON 含：snapshot hash、seed/commit、`sim_core` sha256、分箱参数、p_value
4. **叙事校正**（写入 JSON note 与 README 段）：`MI>0` 是 **指示符关联（index）**，非**涌现语义（symbol）**——f_bit 硬编码进模式的定义性后果；文档措辞从"语义/符号"改为"指示符关联强度"
5. 产出：修好的 `language_analysis.py` + 全部重算 JSON 提交 `feat/method-fix`

---

## 任务 C（可选，看余量）：F-B4 就绪

- g10 觅食索引 bug（`eaters` 位置当个体索引）的**语义修复待本地决策**（涉"对拍固化 bug"纪律）；
- 你若有余量：只做**修复方案的可执行补丁**（先在 `feat/g10-index-fix` 草稿分支，不改 main），是否合入由本地拍板。

---

## 验收

- [ ] 15+ 实验 CSV/manifest 全自描述（seed/ticks/commit 一致），5 seed mean±std 简报
- [ ] `F-D11` 关闭：manifest 与实际产出一致
- [ ] `F-D5` 残余关闭：分箱修复 + 1000 置换 + 全量 JSON 重算落盘 + 叙事校正
- [ ] 全量 pytest 仍绿（除已知 F-V1 skip）
- 简报两段（A/B）仅数字/产物清单，不判读

## 参考

- 内部评估：`_eval/FINDINGS.md`（F-D3/F-D5/F-D9/F-D11）、`_eval/BASELINE.md`
- 本地可行性：`docs/决策与评审/评估-EVAL-可行性分析-g15高位payoff-20260911.md`
- 相关代码：`experiments/run_d2_experiment.py`、`experiments/language_analysis.py`