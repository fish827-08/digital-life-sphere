# A4 最小修复说明 — `nb[:4]` 方向偏置 → Von Neumann 正交 4 邻（本地开发）

> 交付角色：`[本地开发]`｜日期：**2026-09-12 12:19**（Asia/Shanghai，实测时钟）
> 基线：`gitee/main` = `a5af107`｜分支：`feat/capacity-fix`
> 上游：`docs/决策与评审/A4-崩溃溯源报告-20260912.md`（第 1 步）｜裁定依据：`[所有者]` **R12–R16**（`nb[:4]` 定性为 bug fix，不算削弱 C1）
> 验收口径（R3/R4/R6）：**≥5 seed 多数存活至 60k tick，且 N>0 稳定持续 50k tick**；**不损 D2 四机制语义**。

---

## 一、改了什么（逐项）

| # | 文件 | 变更 | 对 C1–C5 语义的影响 |
|---|------|------|--------------------|
| 1 | `world/sphere_world.py` | 新增 `neighbors_von_neumann()`：普通格取 Moore 8 邻中的 **{上,左,右,下}**（常量 `_VON_NEUMANN_IDX=(1,3,4,6)`）；极点格按经度均匀取 4 个 | 无（纯拓扑工具） |
| 2 | `simulation/sphere_engine.py` | D2 移动决策：`nb[:4]` → `world.neighbors_von_neumann(...)` | **C1 不削弱**：仍只看 4 个邻居、半径仍 4、噪声/softmax 未动，仅由"北+西"改为各向同性 |
| 3 | `experiments/run_d2_experiment.py` | `--perception-radius` 由 `[4,6,8]` 收窄为 `[4,8]`（F1：6 是静默 no-op，改硬失败） | 无（防静默降级） |
| 4 | `simulation/sphere_engine.py` | **C3**：`neutral_genes` 由"全基因冻结 0.5"改为"只冻结 g14/g15"，其余 g0–g13 照常演化 | 使零模型成为**有效**对照（原全冻结＝行为定型的假生态） |
| 5 | `simulation/config.py` | **B1**：`InfoStructureConfig.reputation_weight`（默认 `0.0`=关闭）+ `from_dict` 对未知/缺失键容错 | C3 通路**默认不变**；开启才生效 |
| 6 | `simulation/sphere_engine.py` | **B1**：移动得分信号项 `sp*0.5*trust` → `sp*trust*(0.5 + rep_w*trust)`；`rep_w>0` 时回退 Python 路径保双路径一致 | `rep_w=0` 时与原式**逐位一致** |
| 7 | `AGENTS.md` | **B6**：新增"条件表 C1–C5"章节 + 生态稳定前置门 | 纪律性，无运行影响 |
| 8 | `tests/test_s1_step_tick.py`（**在 `feat/s1-step-tick` 分支**） | **C5/F-V1**：缺扩展/缺 `step_tick` 由 `skip` 改 **`pytest.fail`** | 无（让验证盲区可见） |

### 1.1 明确**没有**动的（防"作弊式修复"）

`perception_radius`（C1 被测条件）、`perception_noise`、`softmax_tau`、`SIGNAL_COST`、资源全参数
（capacity/regrowth/patch 等）、`initial_count`、`max_count`、`use_sim_core`（仍 `False`）、
以及任何 payoff/声誉/能量回馈机制（**默认全关**）。**未加资源、未关信号成本、未恢复全知。**

---

## 二、修复正确性

### 2.1 方向审计（`[实测]` `experiments/a4_attrib_data.py` §A）
普通格 `nb[:4]` 抽样 5/5 全为"非南向"；新实现 `neighbors_von_neumann` 恒返回
`{(-1,0),(1,0),(0,-1),(0,1)}`（曼哈顿距离 1，含南与东）。

### 2.2 分层可验证：两条独立证据

**(a) 单元级**（`tests/test_a4_perception_von_neumann.py`，5 例全绿）
- 正交性：4 邻恒为曼哈顿距离 1；
- **同时含南向与北向**（旧 bug 永不出现）；
- 显式对照：旧 `nb[:4]` 不含南向（记录 bug 形态、证明测试有区分力）；
- 极点格恒返回 4 个；
- 引擎级：radius=4 下单个体 400 tick 内**南北双向移动均发生**。

**(b) 生态级**（`experiments/a4_e1_neighbor_order.py`，R4 真实口径 `uniform`/`N0=200`，5 臂×3 seed×1500 tick）：

| 臂 | N_end（s42/43/44） | 中位 |
|----|-------------------|------|
| `off_r8` 对称 | 105/116/85 | 105 |
| **`buggy_r4`（旧）** | **12/32/24** | **24** |
| **`vn_r4`（本修复）** | **94/145/120** | **120** |
| `diag_r4` 对角 4 邻 | 69/92/129 | 92 |

⇒ 修复后 **≈/优于对称基线**；**病灶确为方向偏置，而非"4 邻太少"**。

### 2.3 确定性 / 对拍
- 修复不消费任何 RNG（只改邻居选取），`rayon` 无关（D2 走 Python 路径）；
- 全量测试：**224 passed, 1 skipped, 0 failed**（基线 219 → 新增 A4 用例；`test_d1_zero_model` 按 C3 新语义更新）。

---

## 三、验收状态（≥5 seed × 60k）

`experiments/a4_verify_capacity.py`｜口径 = **与 R4 批完全一致**（`uniform`、N0=200、`use_sim_core=False`、
`learning_bottleneck+steels_alignment+info-asymmetry` 开、**`arbitrary_codebook=False`**）。
跑 5 seed（42–46）ON-fixed + 2 seed（42/43）OFF 对照，落 `_rerun_logs/a4_fix/`。

> ⚠️ **口径声明（回应 `[内评]` V12 的开口）**：R4 批本身**只开 3/4 机制**（码本关闭，`[实测]` manifest）。
> 本验收批**刻意沿用同口径**，以便与 R4 基线**逐参可比**。⇒ 本批结论 = **"3 机制 + 邻居修复"**；
> **"4 机制 + 邻居修复"**是否满足生态门**尚未测**，待 `[所有者]` 裁定 V12 后用同脚本 `arbitrary_codebook=True` 补跑。

**结果**：⏳ 运行中（12:19 起，5+2 并行）。判据：`reached_60k` 且 `tick≥10000 起 N 全程 >0`。完成后回帖 + 落 `summary.json`。

---

## 四、并行项完成情况

| 项 | 状态 | 说明 |
|----|------|------|
| **B1** R2 声誉权重 | ✅ **已开发**（`feat/reputation-weight` 内容并入本次提交） | 可开关、默认关、`rep_w=0` 逐位一致；**未跑判读**（守 R6） |
| **C3** neutral 零模型 | ✅ 只冻结 g14/g15 + 单测 | P1 |
| **B6** 条件表入 AGENTS.md | ✅ | C1–C5 + 生态门 |
| **C5** F-V1 硬失败 | ✅ 已推 `feat/s1-step-tick`（`206c83a`） | 该分支文件不在 main，用独立 worktree 改，未扰动主工作区 |
| **R11** 认领两文件 | ✅ | `run_d2x_parallel.ps1` → 入库（`eb812ce`）；根 `README.md`（pre-R9 陈旧副本，指向 `collab/main` 权威）→ **删除** |
| **F1** radius=6 静默 no-op | ✅ 修复（runner 收窄 `[4,8]`）+ **manifest 审计**：39 份 manifest radius ∈{4,8}，**无 6** ⇒ 历史无静默降级批次 |
| **F3** 探针口径 | ✅ 已按 `uniform`/`N0=200` 全部复跑 | 溯源报告 v2 |
| **F2** ON 臂非 Rust 独立验证 | ✅ 采纳 | 溯源报告 §3.1 已降级表述 |

---

## 五、复现

```bash
cd _gitee_review
./.venv/Scripts/python.exe -m pytest tests/ -q --ignore=tests/test_broker.py   # 224 passed
./.venv/Scripts/python.exe experiments/a4_e1_neighbor_order.py                 # 5 臂矩阵
./.venv/Scripts/python.exe experiments/a4_verify_capacity.py --mode on --seed 42 --ticks 60000 --out _rerun_logs/a4_fix/asym_on_s42.csv
```

*本文件为 A4 第 2 步交付。*
