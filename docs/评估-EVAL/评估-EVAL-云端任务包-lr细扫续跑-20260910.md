# 云端任务包：lr 细扫续跑交接（2026-09-10，条件触发）

> **触发条件**：local 侧实验已停止（2026-09-11，进程已 kill），本批次**由云端完整承接**。
> 本地已跑进度：lr005 跑到 ~45k/60k（s42/s43；终态前 g15≈0.35 回落），但 main 版 run_d2
> 无快照续跑 → **云端直接重跑全部 6 实验**（各 60k tick，20 核并行很快），旧 CSV 覆盖即可。
> 网格结论判读（lr 正选能否锁定）以云端完整数据为准。

## 前提

- 代码：`git fetch gitee && git checkout main && git pull gitee main`（main ≥ 028eccc，含 np.random.seed 可复现性修复 + run_d2 即时落盘）
- 快照续跑能力：你此前在 feat/d2-grid 实现过 run_d2_experiment 快照续跑 + 增量写入（619f5a6）——**在新 main 上重打同一补丁**（main 已更新该文件，注意 np.random.seed 修复必须在，勿回退）
- 参考本地已跑进度：lr005 已到 ~27k tick（若本地不再推进），CSV 会随交接带上

## 命令（6 实验，可 2 进程并行）

```
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr005  --learning-bottleneck --steels-alignment --learning-rate 0.05  --seed 42
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr005  --learning-bottleneck --steels-alignment --learning-rate 0.05  --seed 43
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr0075 --learning-bottleneck --steels-alignment --learning-rate 0.075 --seed 42
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr0075 --learning-bottleneck --steels-alignment --learning-rate 0.075 --seed 43
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr0125 --learning-bottleneck --steels-alignment --learning-rate 0.125 --seed 42
python experiments/run_d2_experiment.py --ticks 60000 --tag d2_lr0125 --learning-bottleneck --steels-alignment --learning-rate 0.125 --seed 43
```

## 产出

- 已存在 CSV/manifest 续写（增量落盘）；全部完成后 6 CSV + 6 manifest 推 **feat/d2-grid**（commit 字段应标 main hash）
- 简报仅列数字：final_tick / extinct / final_N / final_g15 / g15 峰值及 tick（lr 细扫关注点在"正选能否锁定"，峰-终对比很重要）