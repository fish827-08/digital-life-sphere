# 项目状态评估（内评线）— 2026-09-13 00:46

> 评估人：内部评估专员 | 基线：`gitee/main` = `6ba8f1c`（本地已同步，无 ahead/behind）
> 授权：`[所有者]` 2026-09-11 授予（实测优先 / 否决权 / 五项评估范围）
> 依据：`_share/路线共识.md`（R1–R23）、`派工-20260912-内部评估专员.md`、`战略定位-20260912`、仓库实测

---

## 〇、一句话结论

> **项目处在健康且罕见的自我修正轨道上，当前阶段（P0'）阻塞明确、可解，但对外可见的文档仍带着三个已被裁定作废的旧事实。**
> **Gate 判定：语言涌现结论链仍不通过（G1/G3/G4 不通过）；但 "P0' 生态稳定性" 这一子目标的判定为 —— 阻塞已定位为一个 1 处代码 bug，可从"机制攻坚"降级为"修 bug + 验证"。**

---

## 一、基线（`[实测]`）

| 项 | 值 |
|----|-----|
| 仓库 | **主工作树已迁至 `the-world/` 根**（`.git` 298 文件在根；迁移记录见 `AGENT.md` §十） |
| 分支 | `main` = `gitee/main` = `6ba8f1c`（**已同步**） |
| 扩展 | `sim_core` 导出 **18** 函数；**`step_tick` 仍缺**（F-V1 未修） |
| 测试 | `[实测]` 215 个 `test_` 函数（grep 计数，非 pytest 运行） |
| ✅ R12 修复 | `simulation/sphere_engine.py:764` 已改为 `neighbors_von_neumann()`；`:760` 留注释说明 `nb[:4]` 禁用 |
| ✅ R12 回归测试 | `tests/test_a4_perception_von_neumann.py`：5 例（含 `test_old_bug_would_have_failed`、`test_radius4_engine_can_move_south`） |
| ✅ R21 | C1 等频分箱修复已入 main（`cc7fbb8` cherry-pick `582afef`） |
| ⚠️ F-V1 | `.venv` 的 `sim_core` **无 `step_tick`** ⇒ 1062 行 `tick.rs` 在默认套件中**静默 skip** |

---

## 二、本轮（09-12 12:00 → 09-13 00:46）的实际进展

这一轮是项目启动以来**信息密度最高的一段**。按影响排序：

### ① 🟢 找到并修复了 C1 的根因 —— 一个 1 处代码 bug

`[代码]` `nb[:4]` 取 Moore 表前 4 个 = `{上左,上,上右,左}` = **半平面**（只有北+西），个体永远无法向南/向东移动。已由 `7eeaa5f`（12:21）修复。

`[实测]` E1 复跑（1500 tick × 3 seed，R4 真实口径）：

| 臂 | N_end 中位 |
|----|-----------|
| buggy `nb[:4]` | **24** |
| 修正后（真 Von Neumann） | **120** |
| 全 8 邻参照 | 105 |

⇒ **`vn_r4(120) ≥ off_r8(105)`：信息不对称本身完全不致死。** R12 定性为 **bug fix，不算削弱 C1** —— 我复核代码确认：半径仍 4、仍 4 格、noise/tau 未动。

### ② 🟢 建立了对外表述的强制锚点：L0–L4 判据阶梯

`[文档]` `战略定位-20260912` §一。这是对 09-08 "g14=0.834 被读作涌现征兆"事故的**制度化补救**：

| 级 | 判据 | 状态 |
|----|------|------|
| L0 信号稳态 | 稳态 g15>0.5 且 ≥5 seed 可复现 + 过生态门 | ❌ 未达成 |
| L1 携带信息 | 信号↔上下文 MI 显著高于漂变零模型 | ⚠️ 未在稳态信号上测到 |
| L2 任意性 | 能指-所指解耦 | ❌ 信号是自身状态编码 |
| L3 组合性 | 有限信号集表达复合意义 | ❌ 16 模式实际只用 4 种 |
| L4 语义接地 | 可撒谎 | ❌ 远超当前设计 |

### ③ 🟢 关键数据被主动降级/作废（诚实度）

| 裁定 | 内容 | 我的评价 |
|------|------|---------|
| **R19** | **R4 批整体降级（作废重跑，非修正）**——运行于 10:12–10:52，**早于** `nb[:4]` 修复（12:21）⇒ 不得作 payoff/生态/C1 依据 | ✅ **正确的严格处理**。我此前只建议"撤回 seed 维"，团队做到"整批作废"，比我更严格 |
| **R18** | M1 选型采纳 **A 强制表达** + 强制叠加学习瓶颈维 | ✅ 采纳了我的 M1 结论 |
| **R20** | F-D13：`实况.md` §三③"全部 <0.5"与自带表矛盾（实为 **3/5 > 0.5**） | ⚠️ **见 §三缺陷 2 —— 尚未修复** |
| **R21** | C1 分箱修复不在 main ⇒ "不得声称 MI 已修至 0.295"；已 cherry-pick 入 main | ✅ 已闭环 |
| **R23** | F-R5 第三起多副本事故；纪律"谁写谁负责入库" | ✅ |

### ④ 🟢 反事实沙盒已立项（R17/R18）

`mini-world` 的目的：**剥离生态竞争后，本引擎的机制栈到底能不能长出信号？**
关键设计（避免污染）：**强制 g15 常开，不得引入"发送者因信号内容获益"**——C3 是主线 R2 的待验自变量。

---

## 三、🔴 当前活跃缺陷（我实测确认仍存在）

### 缺陷 1：`AGENTS.md` 的条件表 C1 行仍是**已作废的旧事实**

`[实测]` 仓库 `AGENTS.md`（工作区指令版同）：

```
| **C1** | 信息不对称 | 感知半径 < 世界尺度 + 感知噪声
  | ✅ 已实测：**主导生态崩溃**（OFF≈N5000 vs ON≈N2~400，`_share/实况.md` §三） | ...
```

**但 R19 已作废该批数据，R12 已定性为 bug fix。** 正确状态应为 **"尚未被检验 / 待修复后重测"**（我在 `_share/路线图.md` 已更正，但工程指南未同步）。

**风险等级：高。** 理由：`AGENTS.md` 是**供 AI 代理/协作者**读的工程指南（`_gitee_review` 时期定位，现仍在根）。**新加入的 agent 会读到"C1 已实测主导崩溃"并把作废结论当作既定事实**——本项目已因"陈旧副本"出过三次事故（F-R5/R23）。

**解除条件**：`AGENTS.md` C1 行改为 `⚠️ 尚未被检验（原"主导崩溃"判定基于缺陷代码，R19 已作废）`。

### 缺陷 2：`_share/实况.md` §三③ **正文与自带表矛盾**（R20 点名的 F-D13）

`[实测]` 现存正文：

> "剔除崩溃尾，只看 N≥30 的 5 个 run：终局 g15 **全部 < 0.5**（4 个回落或低位）"

而**紧随其后的表**列出：`0.240 / 0.563 / 0.551 / 0.327 / 0.887` ⇒ **3/5 > 0.5**。

**这是自相矛盾，且方向相反**：正文读作"弱支持缺 payoff"，数据实为"**3/5 反向**"。
**风险等级：高。** 它是 `实况.md`（我建立的状态快照，被定位为取代陈旧 `PROGRESS.md` 的权威现状）。R20 已裁定更正，但**文件未改**。

**解除条件**：改为"剔除崩溃尾后 N≥30 的 5 个 run 中 **3/5 终局 g15 > 0.5**（0.563/0.551/0.887@60k），2/5 < 0.5 ⇒ **现有数据既不足以支持也不足以否证'缺 payoff 通道'**，n=5 不得写成任何方向的结论"。

### 缺陷 3：讨论板 **172 KB 已超 150 KB 归档线**，且无 `_share/archive/`

`[实测]` `_share/讨论板.md` = 172 KB；`_share/archive/` **不存在**。
`AGENT.md` §3.3 规则 1–4 要求：接近/超过 150KB 时必须归档（总结→已定结论入 `路线共识.md`、未决项**移植**到新板→原板入 `archive/` 并标注会话 id/日期/范围；**归档即知、默认不读**）。

**风险等级：中。** 后果不是数据丢失，而是**规则被静默违反**——而第 5 条"执行前查板"要求所有人先读板；172 KB 的板会实际降低"查板"的可行性。

**解除条件**：执行一次归档，`_share/archive/讨论板-<日期>-<会话id>.md`，未决项移植到新板。

### 缺陷 4（存量）：F-V1 —— `step_tick` 缺失导致**最大文件不在测试覆盖内**

`[实测]` `.venv` 的 `sim_core` 导出 18 函数、**无 `step_tick`** ⇒ `test_s1_step_tick.py` 模块级 skip ⇒ **1062 行 `tick.rs` 不在默认绿灯覆盖内**。
**风险等级：中高。** 属"验证盲区"型缺陷（不是代码错，是**不知道它对不对**）。
**解除条件**：重建扩展使 `step_tick` 可用；或在缺扩展时 `pytest.fail` 而非 skip（任务 C5）。

### 缺陷 5（存量）：F-D3 —— 全仓库零统计推断机制

`[实测]` 无 CI / 无假设检验 / 无置换检验（除 C1 单点已补）。R7 要求 R4 新判据"仅统计过生态门的 run + 相对漂变零模型的超额量"，而**漂变零模型尚未建成**（`--neutral-genes` 现为全基因冻结，非有效零模型）。
**风险等级：中。** 它是 G3 不通过的直接原因，也是 L0/L1 判据不可判定的原因。

---

## 四、Gate 判定（按 `_eval/GATE-CRITERIA.md`）

### 4.1 语言涌现结论链

| Gate | 判定 | 阻塞项 |
|------|------|--------|
| G1 数据有效性 | ❌ | R4 批已作废（R19）；`实况.md` §三③ 与自带表矛盾（缺陷 2） |
| G2 对照有效性 | ⚠️ | 生态门未过 ⇒ 对照臂不可比；漂变零模型未建 |
| G3 统计功效 | ❌ | F-D3 零推断机制；n=5 且 3/5 反向 |
| G4 指标效度 | ❌ | `cult_div` 必然归零、`trust` 是食物代理（**已登记，未修**） |
| G5 静默错误 | ⚠️ | F1 `radius=6` 静默 no-op（R14 列 P1，未修）；F-V1 |
| G6 验证覆盖 | ❌ | F-V1：`step_tick` 静默 skip |
| G7 叙事强度 | ⚠️ | L0–L4 阶梯已建立（**显著改善**）；但 AGENTS.md C1 行仍陈述作废事实（缺陷 1） |

**总判定：不通过。** 但**性质已改变**——不是"结论错了"，而是"结论尚未生成"（数据作废 + 工具未就位）。

### 4.2 P0' 生态稳定性子目标

| 项 | 判定 |
|----|------|
| 阻塞是否已定位 | ✅ **是**（1 处代码缺陷，`7eeaa5f` 已修） |
| 是否已过生态门 | ❌ **否**——R19 要求用修复后代码 **5 seed × 60k** 重跑 |
| 解除条件（可验证） | R19 验收批（5 seed × 60k，修复后代码，记录 commit + 扩展指纹 + 全部开关真实状态）中 **多数 seed 存活且 N>0 稳定持续 50k** |

---

## 五、我未验证的部分（必写）

1. **未运行 pytest**——本报告测试数字来自 `grep` 计数（215 个 `test_` 函数），**不是** pass/fail。上次数值基线为 216 passed / 1 skipped / 3 errors（09-11），**此后未复跑**。
2. **未独立复现 E1 复跑**（buggy 24 / VN 120 / off 105）——采信 `[本地开发]` 的 `[实测]`。该数字是 R12/R19 定性的**关键依据**，建议有人独立复跑（`[待核]`）。
3. **未核 `_backup_full_20260912`、`.git.backup-20260912` 内容**（工作区有备份目录，`6d1504a` 称已 gitignore）。
4. **未评估 mini-world 的 N2–N5**（M3 任务，另出评审单）。
5. **未核 `radius=6` 历史 manifest 审计结果**（R14 要求）。
6. **未评估 F-D3 的工程量**（只登记为阻塞项）。

---

## 六、结论

**做对的（应明确肯定）**：
1. **根因定位到 1 处代码 bug**，而非归因给机制——这是本项目从"机制必然"叙事中挣脱的关键一步。
2. **主动作废整批数据**（R19），比外部评估建议的更严格。
3. **建立 L0–L4 强制锚点**，堵住"任何数字都能讲成好消息"的路径。
4. **把"能不能看到语言"拆成两个可分别证伪的问题**（主线 vs mini-world）。
5. 对"多副本""静默关闭""对拍固化 bug"三类系统性风险已建立纪律（R9/R11/R14/R16/R23）。

**需要立刻处置的（按紧急度）**：
1. 🔴 **修 `实况.md` §三③ 的自相矛盾**（缺陷 2，R20 已裁定，文件未改）
2. 🔴 **修 `AGENTS.md` C1 行**（缺陷 1，会把作废结论传给新 agent）
3. 🟡 **归档讨论板**（缺陷 3，172 KB > 150 KB）
4. 🟡 跑一次 R19 验收批（P0' 的唯一解除条件）
5. 🟡 F-V1 / F-D3（验证覆盖与统计推断，是 G3/G6 的根因）
