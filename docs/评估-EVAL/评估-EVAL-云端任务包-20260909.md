# 云端任务包（2026-09-09）

> 前提：先 `git fetch origin && git checkout main && git pull origin main` 拉到最新 main（当前含 D0 修复，commit 至 b7121c3）。
> 所有实验在 main 或你自己开的新 feature 分支上进行，完成后按审查合并流程并回 main。

---

## 一、当前 main 状态（D0 止血已完成，勿再回退）

| 项目 | 状态 |
|---|---|
| sig_density bug | ✅ 已修（`e.signal_field`→`e.signals`，experiments/run_long_experiment.py:64） |
| cult_div 口径 | ✅ 已统一 `std(axis=0).mean()`；大世界重算=0.0117 |
| w_social 量纲 | ✅ 已修（`social_move_weight` 进 SimulationConfig，densities 按邻居上限归一化），双路径对拍逐位一致，164 passed |
| pred_cum/elapsed_h | ✅ pred_cum 实时取 `death_cause_totals()`；elapsed_h 跨段累计 |
| 语言综合分 | ✅ 0-100 综合分已废弃（D4），只报 7 维子分；新度量转换为相对零模型超额量（你推进 D1 时实现） |
| 脚本新参数 | `--seed / --rotation-period`（世代压缩扫描用） |

**关键实证**（你分析时引用的基线）：大世界 1M tick → signal_context_mi=0.0（信号-语境零互信息）、cult_div=0.0117、熵 0.266、Moran's I=0.21、解读一致性 0.96、g15=0.699 但 g15-信号相关=-0.014（无基因-行为关联，更像漂变）。

---

## 二、云端今晚任务（独立于后续 D2，可立即执行）

### 任务 1：世代压缩敏感性扫描（D3 数据采集，最优先）

目标：确定 rotation_period 压缩到什么档位，"世代时间"从 ~18.9k tick/代 压到 100~300 tick/代，同时生态不断裂。

批次（小世界 60×120，patchy，200k tick，每档 2 seed 起步）：
```
# rotation_period 2400（对照组，=当前行为）
python experiments/run_long_experiment.py --rows 60 --cols 120 --ticks 200000 --segment 50000 --tag gen_rp2400_s1 --seed 42
# 压缩档
python experiments/run_long_experiment.py --rows 60 --cols 120 --ticks 200000 --segment 50000 --tag gen_rp1200_s1 --seed 42 --rotation-period 1200
python experiments/run_long_experiment.py --rows 60 --cols 120 --ticks 200000 --segment 50000 --tag gen_rp600_s1  --seed 42 --rotation-period 600
python experiments/run_long_experiment.py --rows 60 --cols 120 --ticks 200000 --segment 50000 --tag gen_rp300_s1  --seed 42 --rotation-period 300
```
判读指标（每 2000 tick 已在 CSV，加打点后看 max_gen 增率 + g14/g15 曲线 + N 稳定性）：
1. **max_gen 净增**：期望 200k tick 内从 25 代级提到 500~2000 代级
2. **生态稳定性**：N 是否仍能在 50k tick 内拉满密度、老死比例不崩（换代太快不应触发大灭绝）
3. **g14/g15 方向**：压缩后选择梯度是否开始显现（对照 rp2400 的漂变样）
产出：results/gen_scan/ 汇总表（每档 max_gen/世代间隔/终局基因/trust/cult_div），回写 experiments/ 下新 CSV 并提交分支 `feat/gen-scan`。

### 任务 2（可选，若任务 1 顺利收尾）：D1 零模型三开关
在 config 加三个开关进 SimConfig + fingerprint：
- `neutral_genes`（基因断线：所有基因=0.5，仅行为不演化）
- `signal_disabled`（不发信号，g15 保持 0.5）
- `signal_mode: state|random|evolved`（信号编码模式，random 对照）
小世界 300k tick × 3 seed × {主实验, neutral, signal_disabled, random} 四臂。
预期：cult_div/trust 曲线在对照组高度复现主实验 → 主实验叙事推倒重写（这是最有价值的产出，元宝 M1 gate）。产出同样入 `feat/zero-model` 分支。

---

## 三、云端后续主任务（D2 信息结构重构，科学杠杆最高）

在 main 上新开分支 `feat/info-structure`，只做四件事（严格按评估 M2，别加别的）：

1. **学习瓶颈**：解读表不遗传；幼体从有限 (信号,后果) 观察样本归纳（Kirby 链条枢纽）
2. **任意性**：pattern = 个体可遗传码本[状态]（(16,16) 编码矩阵），映射可漂移可协商
3. **信息不对称**：感知半径 8→4 邻 + 感知噪声 `sigma_perceptual∈{0,0.05,0.2}` + 移动 softmax(tau) 替代 argmax
4. **Steels 式对齐**：同格相遇概率性解读表对齐（带噪声）

**硬门槛**（不满足不给合入）：
- Python/Rust 双路径逐位对拍（新机制同样要对拍，维持 164+ passed）
- `validate_gene_wiring` 双写一致
- 明确**推迟 L8/L9/L10**：它们依赖"信号已有语义"，修好前是成本不是收益

**唯一验收判据（元宝 M2 gate）**：g15 在无手调补贴下从 0.5 上升。

---

## 四、分支策略（再强调）

- 主开发线 = `main`（唯一主干）；你所有任务从 main 开新 feature 分支，命名 `feat/<任务名>`
- 完成标准：全量 pytest 通过 + 双路径对拍 + 报告 Observed/Inferred/Assumed 三栏
- 合入流程：分支推送 → 本地审查 → 逐位对拍复核 → 合入 main
- 存量分支处置：`l9-terrain-season` 冻结（待 M2/季节重新评估后拆三）；`language-signal` 冻结补测试后再议；`snapshot-cron`/`l7d-rayon` 已在线视为历史

---

## 五、数据与资源位置

- 大世界快照：`snapshot_large.npz`（tick=1,000,000, N=11999）
- 大世界全程数据：`experiments/long_large.csv`（已整理去重 500 行）
- 语言 6 维子分（新口径）：`experiments/state_lang_metrics_large_1M.json`
- 6 模型评估意见：`_eval_reports/` + `评估-EVAL-汇总意见-20260909.md`
- 实验产出统一进 `results/sensitivity/` 或按 tag 的 `experiments/long_<tag>.csv`