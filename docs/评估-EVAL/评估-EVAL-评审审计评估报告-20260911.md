# 评审意见：审计评估报告（the-world 多方位评估 · 2026-09-11）

> 评审对象：`评估-EVAL-审计评估报告-20260911.md`（第三方审计，60.5KB，源码通读+实测复现+并行代码审计）
> 评审内容：**审计方法可行性** + **审计结果可达性（结论是否成立/可复现）**
> 评审日期：2026-09-11 | 结论：**方法可行、结果大体可达；1 项数值需三方对齐；发现不改变战略决策方向，但须并入"方法补强"工作包**

---

## 一、审计方法可行性：可行且规范（A）

| 手段 | 评价 |
|------|------|
| 实测复现（哈希比对 MD5 / 引擎探针 / CLI 端到端） | ✅ 可复算、闭环：seed 缺陷证据链完整，与本地独立修复验证一致 |
| 并行代码审计（rayon 逐位哈希 1/2/4/8/16 线程） | ✅ 方法正确（N≥2048 强制走到并行分支），与 S2 语义一致 |
| AST/配置扫描（113 字段→8 死配置）、`grep` 统计推断机制（零命中） | ✅ 可复核、无主观性 |
| 文档-实测交叉核对（计数/口径/分支状态） | ✅ 严谨，区分 `[实测]/[文档]/[推断]` |

**审计定位准确**（`_gitee_review` 为主干、digital-life-sphere 为陈旧副本等），未把历史副本当现状。

## 二、审计结果可达性：逐级判定

### A. 已由本地独立复核确认（达成）

| # | 审计结论 | 本地复核 |
|---|---|---|
| seed 无效 | ✅ 已修复（db0aef1）+ 实测 42≠43 轨迹不同；MD5 相同的成因 = 顶层 `config.seed` 恒 42 + `simulation.seed` 无该属性 |
| B2：use_sim_core+D2 默认年龄不推进 | ✅ **已修复**（main 028eccc：捕食段分支对齐 + `_d2_repro` 补 asym + 回归 TestInfoAsymAgeRegression）——审计 HEAD(fb3b41a) 未含此修复 |
| 信号经济学诊断（发送者无私利） | ✅ 与本地可行性分析-g15 同构，且引演化理论更强 |
| 性能 11~12.5× / 双路径逐位 | ✅ 与 S1/S2 实测一致 |
| rayon 确定性与 `SyncPtr` UB 边缘 | ✅ 与 S2 交接一致；`unsafe impl Sync` 无编译器保护，已列入风险 |
| 16 模式信号实际只用 4（高 2 位=发送者能量档） | ✅ 与代码（e_bin 编码）一致 |
| 统计功效不足（seed 1/2/3 vs 需 ≥5；无 CI/检验） | ✅ 事实成立；**D2 gate"3 seed 方向一致"判据从未真实执行** |

### B. 复核有分歧 / 需对齐（1 项重要）

| 项 | 审计 | 本地复算 | 处置 |
|---|---|---|---|
| `signal_context_mi` 归档 | 0.0 失效；重算 0.1275、p≈0.000 | **当前脚本+快照（tick=1M, N=3240）复算 normalized_mi=0.0000** | **分歧未解**：0.0 与 0.1275 哪个对，取决于"语境通道"定义（快照是否含 resource_grid、分箱方案 0.2966 vs 0.1275 已示敏）。**列为三方对齐议题**，不采信任一方；归档 0.0 是否失效暂判"证据不足，待统一口径再定" |

### C. 高价值新发现（本次未处置，列入后续清单）

1. **g10 觅食索引 bug**（`eaters` 位置索引当个体索引；Rust 忠实复刻=对拍固化 bug）→ 需决策：改语义（参考与 Rust 同步重排 RNG/行为）vs 保留——**方法论警告：对拍固化参考 bug**，值得记录进纪律
2. **niche_floor/gain 死配置** → A3 敏感性部分臂空操作，结论应撤回/重标注
3. **cult_div 口径历史不一致**（axis=0 vs 池化）→ 访问 0.001 vs 0.116 对比可能部分为估计量差异
4. **max_gen 反指**（冻结臂 175 代 vs 演化臂 17 代）→ 不得再作进度/演化指标
5. trust=食物代理、16→4 信号模式；8/113 死配置（审计清单）；`tick.rs` 未入默认测试（skip→应硬失败）

## 三、对当前决策的含义

1. **不推翻战略转向/可行性分析结论**：审计的"信号经济学不可达 + 指标效度/统计功效不足"与本地可行性诊断共振，方向加固（生态/信号回馈路线必要性增强）。
2. **但强制要求工作包**：D2 重跑（修复后 seed）+ **统计功效 ≥5 seed + CI/检验** → 修改 D2 gate 判据规格（5 seed 方向一致 + 增强相对零模型超额量）；指标效度修复（cult_div 口径、trust 明确定义）。
3. 审计暴露的工程项（B2 已修、seed 已修）进入合入确认；G10/niche/A3/signal_context_mi 走向决策清单。

## 四、处置清单（按优先级）

| 级 | 项 | 归属 | 状态 |
|---|---|---|---|
| P0 | run_d2 seed 修复 + 哨兵测试（异 seed 必不同） | 本地 | seed 已修；**哨兵测试待加** |
| P0 | D2 gate 判据改为 en 5 seed + CI（重跑 D2.1/lr） | 云端重跑 | 待发引导词 |
| P1 | signal_context_mi 三方对齐（0.0 vs 0.1275），统一语境通道后重算归档 | 本地 | 复算已跑（归一化 0.0）；待定口径 |
| P1 | g10 觅食索引：语义修复决策（保参考 or 同步改） | 待决策 | 需用户拍板（涉及"对拍硬化 bug"纪律） |
| P1 | niche_floor/gain 接线或撤销 A3 相关结论 | 本地 | 待做 |
| P2 | B2 修复回归（已在 main）扩展覆盖 .venv 后 full 验证 | 本地 | 已修，待扩展覆盖后复跑 |
| P2 | npz 移出 git（LFS/外部） | 本地 | data/snapshots 已归集；git 体积另行处理 |

## 五、总体结论

- **方法**：可行、规范、可复算（多处与本地独立结论一致）。
- **结果**：除 `signal_context_mi` 一项数值待三方对齐外，其余关键结论**可达且多数已由本地进一步处置（seed / B2 已修复）**。
- **定位**：审计的质量评级（工程 A−/科学 C）与项目自评一致；其价值在"把统计功效与指标效度提到与工程纪律同档"——并入当前决策流程（战略转向评估 + D2 重跑 + 统计增强）执行，不改变方向只补方法。

---

*证据：本地复核（seed 修复 db0aef1、B2 修复 028eccc、MI 复算 0.0、MD5 一致）；审计原文入 `评估-EVAL-审计评估报告-20260911.md`。*