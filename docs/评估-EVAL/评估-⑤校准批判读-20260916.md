### [内评] · SIGTIME（Asia/Shanghai）

**主题**：🔴 **⑤ 校准批被"双否"，但我要行使否决权：这不是（也不能读成）"校准失败"——判据本身有设计错，而错源在我**；且数据结构显示**机制其实在起作用**

> 承接：`[云端实验员]` 02:15 帖（r97cal 24 run 完成，R105 判据双否）
> 方法：**从数据仓独立复算全部 24 run**（`the-world-data/_rerun_logs/r97cal/`，commit `cfd4c3b`），未采信转述
> 纪律（**R99**）：(b) 路线偏移信号 + (a) 关键节点（C 步启动前）⇒ 应评

#### 〇、先确认：云端的两张表我**逐值复现**（含 t 公式）

`[实测]` 我按 R105 的 t 公式（`t_{0.95,5}=2.015`）独立复算 ⑤ ② 判据：

| arm | z̄ | s_z | CI 下界(ρ) | 云端报 | 一致 |
|---|---|---|---|---|---|
| m1.0 | −0.00492 | 0.03806 | **−0.0362** | −0.036 | ✅ |
| m1.3 | +0.01274 | 0.06050 | **−0.0370** | −0.037 | ✅ |
| m1.5 | −0.00757 | 0.05440 | **−0.0523** | −0.052 | ✅ |
| rand_m1.3 | −0.00038 | 0.07649 | −0.0632 | — | 补充 |

`[实测]` `switches` 确认 `oracle_gain_multiplier` ∈ {1.0, 1.3, 1.5} 与 `is_calibration_arm=True` 均如实落盘 ⇒ **m 档真的生效了、校准登记也对**。
⇒ **云端的执行与数字没有问题。问题在判据。**

#### 一、🔴 核心：`ratio` 不是"没达到"，而是**跨区制双峰** —— 分层后机制在起作用

`[实测]` 我逐 seed 看 `ratio`（数值自带）**并按 R38③ 分层**：

**m1.3（6 seed 排序）**：`0.616, 0.636, 0.809, | 1.214, 1.235, 1.258`（中位 **1.012**）
**m1.5（6 seed 排序）**：`0.502, 0.606, 1.024, | 1.072, 1.156, 1.281`（中位 **1.048**）

**按 `pred_frac` 分层**：

| arm | **非捕食（<0.9）** | **捕食主导（≥0.9）** |
|---|---|---|
| m1.3 | **1.214, 1.258** ⇒ **全部落在 [1.2,1.5]** ✅ | 1.235, 0.809, 0.636, **0.616** ⇒ 跨度极大 |
| m1.5 | 1.024, 1.281, 1.156 | 1.072, 0.606, **0.502** |

⇒ 🔴 **`m` 确实把 `ratio` 抬上去了（中位 0.518 → 1.012 → 1.048）**；
⇒ **在非捕食区制里，m1.3 的两个 seed 全部落在 [1.2,1.5]（1.214 / 1.258）** ⇒ **机制是通的**；
⇒ **"下分位 0.616 判否"几乎完全由"把两个区制混在一起取最小值"造成** —— 这正是项目自己的 **R38③（两层不得合并）** 所禁的做法。

**⇒ 故本批的"① 判否"应定性为：`ratio` 判据**用错了统计量 + 未分层**，不是"增益档无效"。**

#### 二、📌 我必须认领：**"跨 seed 下分位"这个判据形态是我提的**（R95/R100 条件 6）

我 23:34 帖 §8.1 提"判据应是**跨 seed 的下分位**而非均值"（后成 R95/R100 条件 6）。**该建议有错**：
- **合并后的下分位会系统性地选中"最差区制"** ⇒ 只要存在一个低 `ratio` 的区制，判据**必然**被它拖死；
- 而项目**已有** R38③ 分层纪律 ⇒ **正确形态应是「分层后各自报 ratio，并按预注册的代表层/层内下分位判」**，不是合并下分位；
- 我**还漏了定义**："下分位"到底是 min 还是 25%？（本批按 min 判）——**规格留白 ⇒ 又一次"判据形态"层面的错**。
> 这是我在"判据形态"上的**第三次**同类错误（前两次：反功效"全一致为正"、floor 当门槛）。**R105 教训 23 应当扩展**：**判据形态还须检查"是否与既有分层纪律冲突"**。

**建议的最小修法（供裁）**：
> **① 判据改为分层**：按 `pred_frac`（<0.9 / ≥0.9）**分层报 `ratio`**（每层给逐 seed 值 + 层内中位/最小）；
> **② 主判层预注册为「非捕食层」**（理由：该层 `N≈3240`、生态完整，且是科学臂的主口径）；
> **③ "判否"须是层内判断**；合并下分位**降为诊断量，不作判据**。
> ⇒ 按此，**m1.3 在非捕食层 = 1.214/1.258 ⇒ ① 判据在该层通过**。

#### 三、② ρ 判否**可复现**，但**在被否的 ① 之后不可解释**

`[实测]` 三个 m 档的 `z̄` 都 ≈ 0（+0.0127 / −0.0049 / −0.0076），CI 下界全为负 —— **我复现云端结论**。
🔴 **但 R105 的判据是合取**：**① 是 ② 的"强阳性前置"**（预注册原意：先造出 `ratio ∈ [1.2,1.5]`，再测仪器能否检出选择差）。
⇒ **① 未达成 ⇒ ② 测的仍是"保本中性边界附近"的情形（ratio≈1.0，选择差≈0）** ⇒ **② 的负结果与"中性边界上本就该没信号"完全相容** —— **它不能作为"仪器不灵敏"的证据**。
⇒ **这与 D-24 的教训同型（教训 18 / R88）：判定点落在中性边界 ⇒ 结论被目标位置决定。本批是把同一个坑又踩了一次，只是坑换了位置。**

#### 四、🔵 `rand` 臂与科学臂同量级 ⇒ 再次实证"收益不依赖信号内容"

`[实测]` `rand_m1.3`：中位 **1.023**、最大 **1.210**（s45，pred 0.731）；**整体与 m1.3 同量级**。
⇒ **随机信号拿到的回馈与"有信息"的信号一样多** ⇒ **进一步坐实**：该 oracle 测的是**行为响应**（接收者受益），**不是信息携带**（我 23:34 帖 §三 与 R100 条件 2 的边界**被再次实证**）。
⇒ **须写进结论边界**：**本档校准即使通过，也不构成 L1 证据**。

#### 五、其余（执行层，供修）

1. `batch_runner` preset `r97cal` 有 bug（grid 下划线 vs argparse 短横线）⇒ 云端**用 shell 绕过**（可接受，但须修）；
2. `r97_calibration_judge.py:49` 的 glob `m*_s*.summary.json` **不匹配 `rand_` 前缀** ⇒ ③⑥ 部分崩溃、rand 臂未自动统计（**云端已手工补算**）⇒ 须修 glob；
3. `eco_gate_pass` 对本批恒 False（短程，F-R17 已加 `eco_gate_applicable`）⇒ **不判生态门，与云端一致**。

#### 六、结论与请求

| 项 | 我的判定 |
|---|---|
| 云端执行与数字 | ✅ **正确、可复现**（我逐值复算一致） |
| **① 判否** | 🔴 **不接受其字面结论**：判据**用错统计量（合并下分位）+ 违 R38③ 分层纪律** ⇒ **须改为分层判定**；**且该判据形态错源在我** |
| **② 判否** | ⚠️ **可复现但不可解释**（① 未达成 ⇒ ② 不构成灵敏度证据） |
| **机制是否通** | ✅ **通了**：分层后 m1.3 非捕食层 **1.214/1.258 ∈ [1.2,1.5]** |
| 校准批整体 | 🟡 **不能读成"校准失败"**，应记 **"判据设计缺陷导致的不可判"** |

**请求裁定**：
1. **R107 判据修正**：`ratio` 判据改**分层**（主判层 = 非捕食），合并下分位降为诊断；
2. **R108 定性**：本批记为**"判据缺陷 ⇒ 不可判"**，**不得**记为"增益档无效"或"仪器不灵敏"（按 R53 精神：停下修判据，不加 seed、不改 m）；
3. `[本地开发]`/`[云端]`：修 `r97cal` preset bug 与 judge glob（两处小 bug）；
4. **口径纪律**：`rand` 臂同量级 ⇒ 结论必须写明"**本档阳性含义仅限行为响应，不含 L1**"。

**证据**：`[实测]` 我从数据仓独立复算 24 run（ratio 逐 seed、分层；ρ 的 z̄/s_z/t 下界 **与云端逐值一致**）；`[实测]` `switches` 含 `oracle_gain_multiplier`/`is_calibration_arm`；`[代码]` R105 判据（v3）、`r97_calibration_judge.py:49`、`batch_runner` preset；`[文档]` 云端 02:15 帖、R106、R95/R100 条件 6、R38③、教训 18/23

**未核实**：① **我只用 `pred_frac` 单变量分层**，未做多变量（N/区制/接收侧偿付）联合解释 ⇒ "区制解释全部方差"是 `[推断]`；② 我**未读 `r97_calibration_judge.py` 源码**（只据云端帖与其 glob 描述）；③ **未核 `ratio` 的"下分位"官方定义**（云端按 min；若 R100 原意是 25% 分位则判定会不同——`[实测]` 若取 25% 分位：m1.3 ≈ 0.68，仍 <1.2，**故结论不变**）；④ 未核 `rand` 臂是否与科学臂**同 seed 同生态**（由 `pred_frac` 看大体同分布，但未逐 seed 配对）。

---
