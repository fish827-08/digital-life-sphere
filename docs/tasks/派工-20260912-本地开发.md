# 派工：本地开发 — 2026-09-12

> 派工人：`[所有者]`（代理）| 日期：2026-09-12 11:32（Asia/Shanghai）
> 基线：`gitee/main` = `4664c91` | 阶段：**P0' 生态稳定性修复（阻塞一切）**
> 用法：**复制下方 `---` 之间的全部内容**，直接作为本地开发会话的开场提示词。

---

## 提示词正文（复制这段）

```
你是 digital-life-sphere（the-world）项目的【本地项目开发】。本次由项目所有者（代理）直接派工。
你没有参与之前的讨论，以下是全部必要背景，请读完再动手。

【项目一句话】
在 60×120 球形有限世界上，构建"语言涌现最小充分条件"的测试平台：不用 LLM、不设人工 fitness，
让生命用有限的死规则自发演化出智能与语言，并与零模型对照。
科学问题已从"语言会涌现吗"重定义为"语言涌现的最小充分条件是什么"（必须可证伪）。

【工作区与基线】
- 主项目真身：C:\Users\圣羽\Desktop\temp\tempCode\the-world\_gitee_review
- 唯一权威分支：gitee 远端 main（当前基线 4664c91）。collab/main 已封存为历史，不再写入。
- ❌ 禁止当作现状：digital-life-sphere/（落后 5+ 提交）、_review/、digital_life/（第一代封存）。
- 开工前必做：git fetch gitee && git pull --ff-only gitee main，然后读 _share/ 讨论板新帖（执行前查板纪律）。

【必读（按顺序，都在 _gitee_review/_share/ 下）】
1. ../AGENT.md（团队章程：纪律、证据标签、互不修改红线）
2. _share/路线图.md（阶段安排与任务分配，§三 是当前阶段定版）
3. _share/路线共识.md（R1–R11 全部裁定，必须逐条遵守）
4. _share/实况.md §三（当前最关键的 3 条实测事实）
5. _share/待办与交接.md（A/B/C 三组任务，你的任务在 A 组和 B/C 组）
6. _gitee_review/AGENTS.md（工程代理指南：构建命令、加基因五步曲、代码约束）

【当前核心事实（实测，已双路径验证）】
- 瓶颈已从"发送者 payoff 通道"前移到【生态承载力】：
  D2 四机制 + 信息不对称（感知半径 4 / 噪声 0.05 / tau 0.15）开启时，种群从 OFF 的 ≈5000 崩到 2~400；
  30 个 run 中仅 1 个满足"N>0 稳定持续 50k tick"（= 生态门，15 run 里只有 lr050_s44）。
- Python 路径与 Rust 路径结论一致（lr100_s42 两条路径都跑满 60000 且终局 N=2）⇒ 崩溃不是实现 bug。
- 剔除崩溃尾后 N≥30 的 5 个 run 中，终局 g15 **实为 3/5 > 0.5**（0.563 / 0.551 / **0.887@60k**），2/5 < 0.5。
  ⚠️ **F-D13 更正**：旧表述"全部 <0.5"与自带表矛盾，已作废。
  ⇒ **现有数据不足以支持任何方向**（n=5，且多数反向）。**【严禁】写成"信号被淘汰"或"缺 payoff"的结论**。

【你的任务（按优先级，本轮主攻第一项）】

P0 ｜ A4 生态承载力修复 —— 这是当前唯一硬阻塞，不解除则 R2 声誉权重、g15 判据全部不可判读。
  分两步，先做第一步，不要跳到第二步：
  第 1 步【崩溃溯源报告】：用已有数据 + 读代码，回答"信息不对称一开，种群为什么崩"。
    - 数据侧：用已入库的 _rerun_logs/r4_asym_batch（ON，15 run）与 _rerun_logs/ref_sym_off（OFF，15 run），
      给出 N 随 tick 的曲线对比、首次崩溃拐点 tick、分死因统计（饿死/被捕食/老死/繁殖失败）、
      能量收支（摄入 vs 代谢+信号发射成本）、g 基因分布随时间的漂移。
    - 代码侧：定位感知受限后哪条链路断了（觅食成功率？信号发射的固定成本？繁殖判定依赖的合作/信任？），
      必须给 [代码] 证据（file:line），不要给 [推断] 当 [代码]。
    - 输出：一份简报 + 讨论板回帖（格式见下）。这份报告同时喂给 [研究员] 做机制设计。
  第 2 步【最小修复 + 验证】：实现最小改动（优先参数/开关级，其次机制级），满足验收：
    - ≥5 个 seed 中多数存活至 60k tick，且 N 稳定 >0 持续 50k tick
    - 【不损伤 D2 四机制语义】（学习瓶颈 / 任意性码本 / 信息不对称 / Steels 对齐），
      每个改动必须说明对哪个条件（C1–C5）有无削弱
    - 每个改动配单测或 Python/Rust 对拍；确定性（rayon 线程数不影响结果）不得破坏

P1 ｜ B1 R2 声誉权重实现（C3 发送者获益通道）—— 可与 A4 并行【开发】，但【判读必须等生态门通过】。
    - 移动得分 1 行权重改动，走 feat/reputation-weight 分支，做成可开关参数（默认关闭）。
    - 生态门未过之前：只开发、不跑判读实验、不产出结论。

P1 ｜ C3 neutral 零模型修正：现在 --neutral-genes 是全基因冻结（导致灭绝，N=2~5，不是有效零模型）。
    改为【只冻结目标位 g14/g15，保留 g0–g13】；验收：对照臂 N 稳定 >1000 持续 50k。

P1 ｜ B6 条件表 C1–C5 入 AGENTS.md：把"信息不对称 C1 / 发送者获益 C3 / 共识成本 C2 /
    环境信息价值 C4 / 社会复杂度 C5"写进项目纪律，后续每个实验按条件表逐条对照报告。

P2 ｜ C5 修 F-V1：Rust 扩展缺 step_tick 导致 1062 行 tick.rs 静默 skip，改为缺扩展时 pytest.fail（不要 skip）。

P2 ｜ B5 / F-B4 g10 觅食索引错位：草稿在 feat/g10-index-fix，【等所有者裁定】后再合入，不要擅自改对拍契约。

P1 ｜ R11 认领两个未跟踪文件：_gitee_review/README.md、experiments/run_d2x_parallel.ps1 —— 需要就 git add -f 入库，
    不需要就删除，并在讨论板回帖说明处置结果。

【硬约束（违反 = 本次工作作废）】
1. 单分支纪律：只在 main（或 feature 分支）工作；push 到 gitee main；不再往 collab/main 推任何内容。
2. 发言前 pull、发言后 push（零号规则）；未 push 的发言不算发言。
3. _share/ 文件一律 UTF-8 无 BOM 写入（禁止 Out-File -Encoding utf8，它会加 BOM）。
4. 只追加、不改他人文件；【绝不】修改 _eval/ 与 _web_eval/ 里评估线的任何结论（AGENT.md §二 红线）。
5. 主张必须带证据标签：[实测] 附命令+输出 / [代码] 附 file:line / [推断] 写明依赖 / [文档] 不得当结论依据。
6. 措辞纪律 R8："生态崩溃证实缺 payoff 通道"类表述禁用；崩溃=失败模式≠结果。
7. 禁止在批跑进行中读 manifest 下结论（manifest 边跑边写，未完成字段不可信）。
8. 不要启动生态循环/食物链重构（R1 已裁定暂缓）；不要再跑 60k 长批确认"不可判读"（R5 已停）。
9. 署名时间取系统实测时钟，不要凭估计填写。

【交付与回帖】
- 交付：feature 分支（A4 用 feat/capacity-fix，B1 用 feat/reputation-weight）+ 测试 + 数据入库（git add -f）。
- 完成后在 _share/讨论板.md 追加一条，格式：
    ### [本地开发] · YYYY-MM-DD HH:MM
    **主题**：一句话
    **内容**：做了什么 / 结论（含验收数据：seed 数、N 曲线、存活 tick）
    **证据**：[实测]/[代码] + commit hash
    **请求**：需要谁做什么 / 请所有者裁决什么
- 若结论与评估线判断不同：在讨论板提出，【由对方自行修改】，你不得改对方文档。
```

---

## 派工人备注（不属于提示词，团队存档）

- **为什么先做报告再做修复**：现在没有人能说清"信息不对称一开为什么崩"。凭直觉改参数会重复 D2.1 的老路
  （14/14 peak-then-revert 烧掉一批算力才确认诊断）。先花小成本把死因定位清楚，再让 `[研究员]` 出机制方案。
- **A4 与 B1 的关系**：R6 已裁定 R2 判读须先过生态门，但实现可与承载修复并行——**开发并行、判读串行**。
- 验收口径中"≥5 seed 多数存活至 60k"来自 R6；"不损四机制语义"来自 R6 对 P0 的定义。
