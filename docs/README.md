# docs/ — 项目文档索引

> **2026-09-13 整理**：原散落在仓库根目录的 31 个文档已按主题归入本目录（根目录只留 5 个必读文件）。
> **2026-09-17 整理（R111）**：`_share/` 资料类文件（评估/设计/预注册/派工等 40 个）全部移入本目录；`_share/` 只保留交流文件与花名册。

## 目录结构

| 目录 | 内容 | 文件数 |
|------|------|--------|
| **`评估-EVAL/`** | 评估报告 / 抽核 / 复核 / 审计 / 云端任务包 / 参数调优指南 / Prompt 模板 | 47 |
| **`设计文档/`** | L1–L6 技术汇总、L7 性能优化、L8–L10 扩展建议、快照机制、**规格 V1、R97 系列设计稿** | 8 |
| **`预注册/`**（2026-09-17 新建） | 实验预注册（⑤ ρ 口径 v1–v3、R97 增益校准档 v1/v1.1） | 5 |
| **`决策与评审/`** | 重大决策记录（A4 崩溃溯源、战略转向、生态承载力、**实况快照、分支治理、战略定位**） | 7 |
| **`审计/`** | 隐式选择压审计清单与中性化建议 | 2 |
| **`tasks/`** | 任务/派工（TASKS-CLOUD / DEV / SNAPSHOT、**TASKS-COLLAB、历史派工单 ×3**） | 7 |
| **`二次评估报告/`** | 外部/内部二次评估（元宝二审、全方位评估、对标报告） | 3 |
| （本目录根） | ARCHITECTURE、性能优化路线、项目状态与续跑指南、D3 扫描报告、L7d-Rayon 文档 | 5 |

## 不在 `docs/` 下的文档（各有专门位置）

| 位置 | 内容 | 说明 |
|------|------|------|
| **仓库根** | `AGENT.md`（协作章程）、`AGENTS.md`（代理指南）、`MODULES.md`、`PROJECT-DESCRIPTION.md`、`PROGRESS.md` | **必读**，被大量交叉引用，留在根 |
| **`_share/`** | 讨论板、路线共识、待办与交接、路线图、**花名册**、archive/、.locks/ | **协作层（只留交流文件）** |
| **`tools/`** | 协作工具（`share_lock.py` 写入锁等） | 2026-09-17 新建 |
| **`_eval/`** | 内部评估线工作区（CHARTER / GATE-CRITERIA / BASELINE / FINDINGS / 评审） | 评估线 |
| **`_web_eval/`** | 联网评估线工作区（CHARTER / FACTS / FINDINGS / 核查） | 评估线 |
| **`_roadmap/`** | 路线图 + 实况（索引版） | 路线 |
| **`_advice/`** | 外部会诊建议（**非项目结论**） | 参考 |
| **`_archive/`** | 历史归档（已废弃的路线文档等） | 归档 |
| **`_rerun_logs/`** | 实验日志与结果（数据，**不入主仓库**） | → `the-world-data` |

## 纪律

- 新文档请按主题放入 `docs/` 下对应子目录；**不要**堆在仓库根
- 根目录只保留"必读 / 强引用"的 5 个文件
- 实验数据（`.npz`/`.csv`/`.json`）**不入主仓库**（见 `AGENT.md` §十一）
- **`_share/` 与 `docs/` 边界**（R111）：**活的交流**（板/共识/待办/路线图/花名册）留 `_share/`；**资料与产出物**（评估/设计/预注册/派工/快照）进 `docs/`。历史帖子中的 `_share/评估-xxx` 旧引用 → 按 `_share/README.md` §一.2 对照表找新位置
