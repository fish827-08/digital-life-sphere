# AGENT.md — the-world 项目组协作章程

> 本文件说明**谁在做什么、谁不能做什么、大家在哪里交流**。
> 所有成员（人 + AI 代理）在动手前应读本文件。
> 生效日期：2026-09-12 | 主项目真身：`_gitee_review/`

---

## 一、团队构成与职责边界

| # | 角色 | 职责 | 工作区 | 产出物 |
|---|------|------|--------|--------|
| 1 | **项目所有者** | 最终决策；路线拍板；资源分配 | — | 决策 |
| 2 | **内部评估专员**（AI） | **实验 + 代码结构验证**：跑实验、复算数据、读源码、缺陷审计、Gate G1–G7 判定 | `_eval/` | `评审-R<n>-*.md`、`路线建议-R<n>-*.md`、`BASELINE.md`、`FINDINGS.md` |
| 3 | **评估专家·联网线**（AI） | **公开事实验证**：检索文献/既有工作，验证理论引用真实性、方向可行性、叙事强度 | `_web_eval/` | `核查-V<n>-*.md`、`FACTS.md`、`FINDINGS.md` |
| 4 | **本地项目开发** | 实现机制、写测试、修复缺陷、维护文档 | `_gitee_review/` | 代码提交、`PROGRESS.md` |
| 5 | **云端项目开发** | 与本地并行开发（feature 分支），不直接碰 main | 云端 + `gitee/*` | feature 分支提交 |
| 6 | **云端实验员** | 跑长程/批量实验，产出数据 | 云端 | `experiments/long_*.csv`、manifest |
| 7 | **研究员**（AI/外部模型） | **难题攻坚与算法优化**：本地与云端均难解的高难度任务（机制/算法设计、性能/优化攻坚）；可由 DeepSeek V4 Pro 等承担，经所有者调用或专项委派 | 本地调用 / 专项分支 | 设计文档、优化实现、攻坚报告 |

### 1.1 两条验证线互补（不重叠、不越界）

| 线 | 验证什么 | 负责方 | **不能**验证什么 |
|----|---------|--------|----------------|
| 实验/代码线 | 项目**内部数据是否有效**、代码是否**正确** | 内部评估专员 | 公开文献怎么说 |
| 联网事实线 | 项目**引用的公开事实是否真实**、方向是否有**既有支撑** | 评估专家·联网线 | 项目引擎里能否涌现 |

> **共同边界**：两条线都**不能**替所有者做决策，只能给出判定 + 证据 + 解除条件。

---

## 二、⚠️ 核心纪律：评审内容互不可修改（真实性红线）

**这是本团队最重要的一条规则。**

### 2.1 规则

1. **两位评审家不得修改对方的任何评审内容。** 包括但不限于：评审单正文、结论、判定、证据、账本条目、事实基线。
2. **不同意见只能以"独立新增"的方式表达**：在自己工作区新建文档，或在 `_share/` 中回复，**并在文中明确指向被评论的对象**。
3. **禁止**：删除、改写、软化、加注、覆盖对方的结论——**哪怕是"修正笔误"**。
4. **禁止**：在对方文档顶部加"修订说明"或"补充"。
5. 若发现对方文档确有笔误：**在 `_share/` 提出，由对方自行修改**，或由所有者裁定。

### 2.2 为什么

评审的价值**完全建立在"结论与其产出者绑定"之上**。一旦甲能改乙的结论：
- 无法判断某条判定出自谁、依据什么证据标准
- 事后无法审计"结论是否被悄悄改过"
- 两条独立验证线的**独立性**丧失 → 交叉验证失效

> 独立验证线的全部价值，来自**两个结论是各自独立得出的**。可互改 = 独立性归零。

### 2.3 如何提出异议（正确做法）

```markdown
# 在 _share/ 讨论板 或 自己工作区新建文件
> 提出人：<角色> | 日期 | 针对：<_web_eval/核查-V2-...md> §四 的判定

## 我的不同意见
<具体异议 + 我的证据>

## 我建议对方如何修改
<仅供参考，修改权归原作者>
```

**结论的最终归属永远属于原作者。**

---

## 三、`_share/` — 共享交流面板

所有成员在此交流、讨论、提建议、追踪路线。

| 文件 | 用途 | 谁写 |
|------|------|------|
| `_share/README.md` | 使用说明与当前议题索引 | 任何人 |
| `_share/讨论板.md` | **主交流面板**：按时间追加的对话 | 任何人 |
| `_share/路线共识.md` | 经讨论形成、待/已由所有者裁决的路线结论 | 任何人（结论需标注裁决状态） |
| `_share/待办与交接.md` | 跨角色任务分配与交接状态 | 任何人 |

### 3.1 交流规范

1. **每人有固定署名头**，格式：`### [角色] 姓名/代理 · YYYY-MM-DD HH:MM`（**时间精确到时分**；所在时区默认 Asia/Shanghai；**必须取系统实测时钟，不得凭估计填写**）
2. **提出主张必须带证据标签**（见第四节）
3. **追加而非改写**：讨论板按时间顺序追加，不回头修改历史发言（如必须更正，**新发一条更正**并说明）
4. **跨线引用用相对路径**，如 `_eval/评审-R1-...md`、`_web_eval/核查-V2-...md`
5. **争议不删帖**，由所有者裁定并在 `路线共识.md` 记录
6. **署名时间更正规则（R10 裁定）**：未 push 前可就地修正；**已 push 的一律不补改**（无论"缺失""不完整"还是"填错"），统一以新发更正帖登记偏差。唯一例外：所有者明确指示就地修正的占位符类错误

### 3.1.1 ★ 零号规则：发言前 pull，发言后 push（最高优先）

> **这是交流面板能被团队使用的唯一前提。违反此规则 = 你的发言不存在。**
> （所有者指示，2026-09-12；**同样适用于云端成员**）

**每次在讨论板发言，无例外，按此顺序：**

```bash
git fetch gitee && git pull --ff-only gitee main   # ① 发言前：先拉最新
#  追加你的发言（只追加，不改他人；UTF-8 无 BOM）
git add _share/讨论板.md && git commit -m "share: [角色] <主题>"
git push gitee HEAD:main                            # ② 发言后：立刻推送（R9 后唯一权威 = main）
```

| 不 pull 的后果 | 不 push 的后果 |
|---------------|---------------|
| 读到过期讨论，答非所问 | **发言别人看不到 = 没说过** |
| 提交时覆盖他人未拉取的发言 | 云端无法回应，讨论分叉 |
| 基于旧结论决策 | 权威版与本地版不一致 |

**三条判定**
1. **未 push 的发言不算发言** —— 权威版只有 `gitee/main`（§7.1，R9 裁定）
2. **本规则同样适用于云端成员** —— 云端也必须 pull→发言→push，否则本地看不到云端会话
3. **无法 push 时**（受限环境）：写入本地 + 末尾标注 **"待推送——请 `[本地开发]` 代为 push"**，**不得**声称已更新远端

> ⚠️ **真实案例（本规则的确立原因）**：2026-09-12 内评线在未拉取的情况下编辑，事后发现远端已有 3 个新提交（含 `731485d` 新增 **[研究员] 角色**与 **§3.3 归档规则**），且团队已在板上向内评线派活——内评线**只读板、未同步文件**，险些漏掉新增角色与 §3.5"执行前查板"规则。
> **只读讨论板 ≠ 已同步。** 必须 pull。

**发言前自检 4 问**：① pull 了吗？② 是追加而非覆盖吗？③ 带证据标签了吗？④ push 了吗？

### 3.2 讨论板固定结构

```markdown
### [角色] · YYYY-MM-DD HH:MM
**主题**：<一句话>
**内容**：<主张/建议/异议>
**证据**：[实测]/[代码]/[文献]/[网络]/[推断] + 出处
**请求**：<需要谁做什么 / 请所有者裁决什么>
```

### 3.3 讨论板维护与归档规则（2026-09-12 确立）

1. **大小上限 150KB**：`讨论板.md`（及任一 `_share/` 文件）接近/超过 **150KB** 时，当前会话责任人必须执行归档
2. **归档方式**：总结当前讨论板 → 将已落定结论记入 `路线共识.md`，未落定决策/问题**移植**（重述决策点、结论、未决问题、责任方）到新讨论板 → 原讨论板移入归档（如 `_share/archive/`）并标注原会话 id、日期、范围
3. **归档即知**：归档内容**默认不读**；仅当引用链明确指向归档会话或确需回溯历史决策时才读
4. **归档需说明**：归档操作必须在当次发言/回复中**说明归档事实与原因**
5. **执行前查板**：任何角色（云端/本地/研究员/评估线）**执行任务前必须先查看讨论板状态**（`_share/`）——讨论板用于直接收发任务；开工前检查新任务、相关讨论、待移植/待决策内容，确认上下文后再执行，避免漏任务或重复工作

---

## 四、统一证据标签（全员适用）

不同角色的标签体系**合并**为一张表，避免各说各话：

| 标签 | 含义 | 谁用 | 要求 |
|------|------|------|------|
| `[实测]` | 亲手跑出来的 | 内部评估 | 附可复现命令 + 原始输出 |
| `[代码]` | 从源码读出 | 内部评估、开发 | 附 `file:line` |
| `[文献]` | 公开学术文献 | 联网评估 | 作者+年份+出处+检索词 |
| `[网络]` | 权威网页/官方文档 | 联网评估 | URL + 检索日期 |
| `[文档]` | 项目文档自述 | 全员 | 注明出处；**不得作为 Gate 通过依据** |
| `[推断]` | 分析判断 | 全员 | 写明依赖的前置事实 |
| `[待核]` | 检索不到可靠来源 | 联网评估 | 明确标注"未找到公开来源"，不得当结论 |

> **共同底线**：`[文档]` 与 `[推断]` 不得冒充 `[实测]`/`[代码]`/`[文献]`。

---

## 五、联网线（评估专家）的特殊真实性要求

联网评估线**必须保持真实性**，具体要求：

1. **必须亲自检索到原文要点**，不得转引项目文档中的二手转述
2. **区分三件事**（`_web_eval/CHARTER.md` §三.3）：
   - 文献**支持**某命题为真 → 算证据
   - 文献**未反驳**某命题 → **不算证据**（是不明确）
   - 文献**反对**某命题 → 反证
3. **每条结论附检索路径**（关键词 + 引擎 + 日期），保证**任何人可复现**
4. **找不到来源就标 `[待核]`**，不得用推测填补

### 5.1 全员可验证（重要）

> 联网线的结论**必须设计成可被其他成员验证**。

| 谁 | 如何验证 |
|----|---------|
| 内部评估专员 | 核对文献结论是否与项目代码/数据一致（如 W-F3 要求核对 MDL 等价物） |
| 本地/云端开发 | 按检索路径自行复检关键文献 |
| 项目所有者 | 抽查任一 `[文献]` 条目，按出处自行检索 |

**若某条 `[文献]` 无法按所给路径复现，任何成员都可在 `_share/` 提出质疑**，联网线须回应或降级为 `[待核]`。

---

## 六、主项目目录与 ref 纪律（**2026-09-13 迁移后更正**）

| 项 | 值 |
|----|-----|
| **唯一主工作树（= 仓库真身）** | **`the-world/` 根**（`.git` 在根；分支 `main`） |
| 引擎 + 文档 | 同在 `the-world/` 根 —— **`AGENT.md`（本文件）与 `AGENTS.md` 两份现均在根，各一份、受版本控制**（迁移前分处两目录、靠手工同步，是"规则不一致"的根因，现已根治） |
| **实验数据仓库** | **`the-world/the-world-data/`**（嵌套独立仓库，主仓 `.gitignore` 已排除；数据不进主仓，见 §十一） |
| 待清理残留 | `digital-life-sphere/`（detached `b896cc1`，09-08）、`_gitee_review/`（原 worktree 物理残留）、`.git.corrupt-20260913/`、`_reclone_tmp/` |
| ⚠️ 仅作对照 | `digital_life/`（第一代封存，独立仓库） |
| 评估工作区 | `_eval/`、`_web_eval/`、`_roadmap/`、`_advice/` —— **必须入库**（曾误加 `.gitignore`，已纠正） |
| **远端** | **`gitee`** = `the-world`（唯一活跃，upstream）；`gitee-archive` = 旧仓库（**禁推**）；`origin`/`github` = 只读归档 |
| ❌ 已取消 | `collab/main`（R9 封存；分支已删，回溯用标签 `archive/collab-main-20260912`） |

**发言/评估前必做**：`git fetch gitee && git log --oneline -1 gitee/main` 确认状态；本地曾多次落后于 `gitee/main`。

---

## 七、交流分支 `collab/main`

- **用途**：交流汇报、评审反馈、路线讨论的**落盘载体**（不承载引擎代码改动）
- **内容**：`AGENT.md`、`_share/`
- **纪律**：
  1. 分支只做文档，**不改引擎代码**
  2. 每位成员**只追加自己的文件**，不改他人文件（同 §二）
  3. 定期由所有者或开发线合并关键结论回 `main` 的 `docs/决策与评审/`

### 7.1 ⚠️ 文件归属：单分支权威（2026-09-12 R9 裁定后）

本文件与 `_share/` 的归属：

| 位置 | 角色 | 说明 |
|------|------|------|
| 工作区根目录 `the-world/AGENT.md`、`the-world/_share/` | **工作副本** | 供本地直接阅读编辑；以远端为准同步 |
| **`gitee` 远端 `main` 分支** | **唯一权威** | `_share/` 只在 `main`；云端/本地**切 main 即可见** |
| `gitee` 远端 `collab/main` 分支 | **封存历史** | R9 裁定后**不再写入**；交流历史由 git 历史承担 |

**规则**：
1. 讨论发言以远端 `main` 上的版本为**唯一权威**
2. 本地编辑后需 **push 到 `main`** 才算正式发言
3. **禁止**只改本地不推远端，然后在讨论中引用"我改过了"——那样别人看不到
4. 若本地与远端不一致：**以远端为准**，本地需同步
5. **禁止**再向 `collab/main` 推送 `_share/` 更新（防再次双版本）

#### 7.1.1 ⚠️ 单分支纪律与写入规范（2026-09-12 R9 裁定）

**背景**：双分支镜像（`_share/` 同时在 `main` 与 `collab/main`）在启用后一小时内即发生 `AGENT.md` 被旧基点覆盖、丢失 18 行的事故。所有者裁定（R9，采纳 `[内评]` 方案 C）：**取消双分支镜像**，`_share/` 只保留在 `main`。

```bash
# ① 发言前
git fetch gitee && git pull --ff-only gitee main

# ② 改 _share/ 内容（UTF-8 无 BOM 写入）

# ③ 推送
git add _share/ && git commit -m "share: <主题>" && git push gitee HEAD:main
```

**写入规范（全员强制）**：
1. `_share/` 所有文件一律 **UTF-8 无 BOM**（Windows 的 `Out-File -Encoding utf8` 会自动加 BOM，禁用该方式写 `_share/`）
2. **禁止**在批跑进行中读取 manifest 下结论——manifest 边跑边写，未完成时字段不可信（2026-09-12 内评误判教训）
3. 提交前自检：`git status --short _share/` 确认只含自己应改的文件

> ⚠️ 本项目已多次因"多份副本"出问题（`digital-life-sphere/` 陈旧副本、`_share_远端讨论板.md` 散落副本、双分支镜像冲突）。
> **任何第 N 份拷贝都会变成陈旧副本。`_share/` 只有一个家：`gitee/main`。**
> 本节历史：旧版曾写"唯一权威 = `gitee/collab/main`"→ 2026-09-12 改为 `main` + 双分支镜像 → R9 取消镜像，`main` 单分支权威。
### 7.2 与 `AGENTS.md` 的区别（**两份文件，勿混淆**）

> **2026-09-13 更正**：迁移前两者分处 `the-world/` 根 与 `_gitee_review/`；**现两份同在仓库根，各一份、均受版本控制**。

| 文件 | 位置 | 作用 | 回答的问题 |
|------|------|------|-----------|
| **`AGENT.md`**（单数，本文件） | `the-world/AGENT.md` | **团队协作章程**：谁做什么、交流规则、评审红线、分支与仓库纪律 | **「团队怎么合作」** |
| **`AGENTS.md`**（复数） | `the-world/AGENTS.md` | **工程代理指南**：构建命令、技术栈、加基因五步曲、D2 开发约定、条件表 C1–C5 | **「代码怎么写」** |

两者**互不替代**，也**不互相覆盖**：
- 改协作规则（流程/纪律/裁决）→ `AGENT.md`
- 改工程约定（构建/基因/参数/机制开关）→ `AGENTS.md`
- 涉及两者的（如"实验数据不进主仓"）→ 在 `AGENT.md` 定纪律、在 `AGENTS.md` 记操作细节，并互相引用

**命名易混提示**：单数 `AGENT.md` = 给**人/角色**看的章程；复数 `AGENTS.md` = 给**编码代理**看的指南（沿袭社区 `AGENTS.md` 约定）。

### 7.3 仓库架构（R9 裁定后）

```
gitee 私有仓库  little-fishy/digital-life-sphere
├── main        ← 唯一活跃分支：引擎代码 + 实验数据 + AGENT.md + _share/
└── collab/main ← 封存（R9）：仅保留 2026-09-12 前的交流历史，不再写入
```

| 分支 | 内容 | 谁写 |
|------|------|------|
| `main` | 引擎代码、测试、实验数据（§7.4）、协作文档 `_share/` | 全员（代码归开发线，文档全员） |
| `collab/main` | 冻结的交流历史 | 无人（只读） |

---

## 7.4 仓库数据资产分类（不影响私有库定位，但须登记）

主仓 `main` **同时承载引擎代码与云端实验数据**，这是有意的：数据需在本地可读，供评估线与开发线复算。

### 7.4.1 已追踪的数据资产（`[实测]` 2026-09-12）

| 类别 | 路径 | 规模 | 用途 | 必须本地可读？ |
|------|------|------|------|--------------|
| **快照** | `data/snapshots/*.npz` | 22 个，约 83.6MB | 长实验中间态/终态；语言指标复算的输入 | ✅ **是**（`[实测]` MI 复算即依赖 `snapshot_small.npz`） |
| **实验产出** | `experiments/long_*.csv` | 多个，每个 10–70KB | 世代/基因/生态时间序列，评估线的原始证据 | ✅ **是** |
| **语言指标** | `experiments/*_lang_metrics*.json` | 数 KB | 语言维度指标归档 | ✅ **是**（且**须补** seed/指纹/commit 列，见 `_eval/FINDINGS.md` F-R2） |
| **汇总** | `results/ensemble/*/summary.{csv,json}` | 数 KB | 跨 seed 系综 mean±std | ✅ **是** |

> 判定：**这是云端实验数据，本地需要**——不是误入库，是刻意保留。

### 7.4.2 `.gitignore` 与实际追踪的矛盾（已知，按约定处理）

`.gitignore` 含 `*.csv` / `*.json` 忽略规则，但**具体实验数据已被追踪**。

**结论**：不是 bug，是**"通配忽略 + 显式追踪"的共存**——gitignore 只对**未追踪**文件生效，已追踪文件不受影响。

**约定（写入纪律，避免后人误判）**：
1. `.gitignore` 的 `*.csv`/`*.json`/`*.npz` 通配规则**保持不变**——防止临时/中间产物被误提交
2. **需要入库的实验数据用 `git add -f` 显式追踪**，并在提交信息注明"实验数据入库"
3. **禁止**为图省事删除 `.gitignore` 里的通配规则——那会让 `results/` 中间产物、`*.egg-info` 等一起涌入
4. 私有库定位下**体积不构成阻断**，但单文件 >10MB 应先在 `_share/` 讨论是否改用 LFS

> **对评估线的含义**：不得因"gitignore 说了不入库"就推断"这些数据不存在或不该存在"。**以 `git ls-files` 的实际结果为准。**

---

## 7.5 远端同步纪律

### 7.5.1 事实：评估线无法自主拉取

`[实测]` 2026-09-12：本环境的沙箱**阻断外部 HTTPS**（Windows 凭据存储不可访问 → schannel 无法完成 TLS）。
`git fetch` / `git push` / `Invoke-WebRequest` / `curl` **均失败**（TCP 可连，TLS 握手失败）。

**影响**：
- 评估线**不能**自主拉取 gitee 更新
- 评估线判断"远端最新状态"时，**只能依赖本地已有的 ref**

⚠️ **已发生过的真实事故**：本地 `main` 曾落后 `gitee/main` 5+ 提交，导致评估了陈旧状态。

### 7.5.2 因此的工作流（重要）

1. **每次会话开始，所有者或开发线先拉取**：`git fetch gitee && git pull`（或至少 `git fetch gitee`）
2. 评估线在开工前**必做**：
   ```
   git log --oneline -1 gitee/main      # 远端已知状态
   git log --oneline -1 main            # 本地状态
   git status -sb                       # 是否 ahead/behind
   ```
3. **评估单必须写明所依据的 commit**（本纪律已在 `_eval/` 执行）
4. 若本地落后于 `gitee/main`：**先报告落后，再决定是否继续** —— 不得默默评估旧状态
5. 若无法拉取：评估线在结论中**显式声明"基于本地快照 <sha>，未含远端 <sha> 之后的提交"**

### 7.5.3 无 push 权限时的发言方式（降级方案）

评估线在受限环境**可能无法 push**。此时：

| 方式 | 说明 |
|------|------|
| **本地 `_share/` 发言** | 写入 `the-world/_share/讨论板.md`，由有 push 能力的成员代为推送 |
| **显式标注** | 发言末尾注明"**待推送**——请 `[本地开发]` 代为 push 到 `collab/main`" |
| **禁止** | 声称"我已更新远端"而实际未 push |

> 权威版是 `gitee/main`（§7.1，R9 裁定）。本地发言**未经推送不构成正式记录**。

---

## 八、协作流程图

```
                    ┌─────────────────────┐
                    │   项目所有者（决策）  │
                    └──────────┬──────────┘
                               │ 裁决
          ┌────────────────────┼────────────────────┐
          ▼                    ▼                    ▼
   ┌─────────────┐     ┌─────────────┐     ┌─────────────┐
   │ 内部评估专员 │     │ 联网评估专家 │     │  开发线      │
   │  _eval/     │     │ _web_eval/  │     │ 本地 + 云端  │
   │ 实验+代码    │     │ 公开事实     │     │ 实现+实验    │
   └──────┬──────┘     └──────┬──────┘     └──────┬──────┘
          │                   │                   │
          │  互不修改对方结论（§二）                │
          │                   │                   │
          └─────────┬─────────┴─────────┬─────────┘
                    ▼                   ▼
            ┌───────────────┐   ┌───────────────┐
            │  _share/      │   │  gitee/main   │
            │  讨论面板      │◄──│  远端落盘      │
            └───────────────┘   └───────────────┘
```

---

## 九、当前开放议题（2026-09-12，R7–R11 裁定后）

| # | 议题 | 状态 | 相关文档 |
|---|------|------|---------|
| 1 | 是否全押生态重构（6–9 周） | ✅ 已裁定（R1）：暂缓，后续选项 | `_eval/评审-R1`、`_web_eval/核查-V2` |
| 2 | "五要素"→"条件表 C1–C5" | ✅ 已裁定（R2）：采纳 | `_eval/路线建议-R2` §二 |
| 3 | C1+C3 两周证伪 | ✅ 已由 R5/R6 细化：R2 声誉权重须先过生态门 | `_share/路线共识.md` R5/R6 |
| 4 | MDL 紧凑性偏差（W-F3） | ✅ 已核对：**实质缺失**（只实现瓶颈一半） | `_web_eval/FINDINGS.md` W-F3 |
| 5 | Steels 失败反馈（W-F4） | ✅ 已核对：**缺失败反馈** | `_web_eval/FINDINGS.md` W-F4 |
| 6 | **生态承载力修复**（info-asym ON 下 15 run 仅 1 个过生态门） | 🔴 **当前 P0，阻塞一切** | `_share/路线图.md` §三 |
| 7 | R4 主判据修订 | ✅ 已裁定（R7）：仅统计过生态门的 run + 漂变零模型超额量 | `_share/路线共识.md` R7 |
| 8 | 生态崩溃机制归因（为何 info-asym ON 崩溃） | 🔄 待《D2 生态稳定性报告》 | `_share/待办与交接.md` A5 |

> 详细任务分配见 `_share/待办与交接.md`；路线索引见 `_share/路线图.md`。

---

## 十、分支与仓库纪律（2026-09-12 增订，所有者裁定）

> 背景：本项目已因"多份副本 / 分支无管控"反复出事（陈旧副本被当现状、`AGENT.md` 被回退丢 18 行、`collab/main` 落后 100 提交）。本节为**机制性处置**，不是提醒。
> 完整诊断见 `_share/分支治理-20260912.md`。

### 10.1 唯一真身（2026-09-13 迁移后）

| 项 | 值 |
|----|-----|
| 仓库真身 | **`the-world/.git`**（2026-09-13 从 `digital-life-sphere/.git` **上移**至此） |
| **唯一活跃主工作树** | **`the-world/` 根**（分支 `main`）——`git status`/`git commit` 直接在根目录跑 |
| 孤立目录（原 worktree 物理文件） | `_gitee_review/`（**已从 worktree 登记 unlink**，含完整 main HEAD + 旧 `.venv`；**不要再当 worktree 用**，git 命令会失败） |
| ❶ 陈旧副本 | `digital-life-sphere/`（主工作树**已移交**，里头空 / 旧代码；**禁止当现状**） |
| ❷ 历史副本 | `digital_life/`（独立仓库，第一代，封存） |
| 唯一活跃远程 | **gitee**（upstream 已修正为 `gitee/main`） |
| 本地备份 | `.git.backup-20260912/`（473MB）、`_backup_full_20260912/`（1.9GB） |

> ⚠️ **迁移后变化**：the-world 根**直接就是主工作树**——不再需要 `cd _gitee_review` 才能跑 git。
> 原来"两份 AGENT.md 不同步"问题**已根治**：只有仓库里这一份，根目录直接受 git 管控。
> ⚠️ 但 `_gitee_review/` 里的 .git 文件指向**已失效**的路径；保留它的目的是 .venv 可用，不是 worktree。

### 10.2 开分支：必须有充分理由（事前审批）

开分支前**必须**在讨论板发帖，四项缺一不可：

```markdown
**开分支申请**
- 分支名：feat/<短名>
- 目的：
- 关联任务：（_share/待办与交接.md 的任务号）
- 预期合并时间：
```

- **未说明理由的分支一律删除**，不做事后追认
- 命名统一 **`feat/<短名>`**；❌ 禁用 `feature/`（历史分支保留）
- 一个分支 = **一个 worktree 目录**（`git worktree add ../_wt_<name> feat/<name>`），
  **不在同一工作树反复 checkout**——反复 checkout 是"陈旧副本"的制造机
- ❌ **全员禁用 `git worktree prune`**（2026-09-12 事故）
  > 它会把**被标记 `prunable` 但仍在使用**的工作树登记一并清掉。
  > 2026-09-12 实际事故：`prune` 连带删除 `.git/worktrees/_gitee_review/`，导致**唯一活跃工作区**失去 git 身份
  > （`fatal: not a git repository`）。修复靠手动重建 worktree 登记 + `git reset` 重建 index，**数据未丢但全员虚惊**。
  > 需要清理时：用 `git worktree remove <路径>` 指定删除；或先 `git worktree list` 确认哪些确已废弃再动手。

### 10.3 完成后：主动发起合并（事后审批）

1. **负责人主动发起**：任务完成后在讨论板发合并请求帖（分支名 + 完成内容 + 验收证据 + commit）
2. **由裁定者（所有者）审核后合并**——**不得自行合并进 `main`**
3. **合并后 24 小时内删除分支**（本地 + 远端）：
   ```bash
   git branch -d feat/<name>              # 本地（安全删除，未合并不给删）
   git push gitee --delete feat/<name>    # 远端
   ```
4. 合并前必须：`git fetch gitee && git rebase gitee/main`（保持线性，避免合并噪声）

### 10.4 协作文档不开分支

`_share/`、`AGENT.md` 的修改**在 `main` 上直接追加**，不开文档分支。
（历史教训：`collab/main` 双分支镜像启用一小时内即造成 `AGENT.md` 回退事故，R9 已取消镜像。）

### 10.5 提交纪律（补）

- **路径限定提交**：`git commit -m "..." -- <你的文件路径>`
  ❌ 禁用 `git add .` / `git add -A` —— 多人共用一个工作区时，会把别人的暂存文件一起提交（**已发生两次**）
- 新文件：先 `git add <文件>` 再 `git commit -- <文件>`
- 提交信息用约定式前缀：`feat:` `fix:` `docs:` `data:` `share:` `chore:` `refactor:` `test:`
- ❌ **禁止 force-push 共享分支**（`main`）；确需强推时用 `--force-with-lease`
- **并发编辑（2026-09-16 增订）**：同一 `_share/` 文件存在多线并发编辑时，提交前须 `git diff <文件>` 确认**待提交差异只含自己的行**；若含他人在途内容 ⇒ **先暂缓并板上知会**
- **提交后核对（2026-09-16 增订）**：路径限定提交后须 `git show --stat` 核对**文件数与路径**（防静默漏提）

### 10.6 复杂 git 操作必停确认（2026-09-12 事故后增订）

> 本节是**执行纪律**——比 §10.5 更严。违反者视同事故责任人。

**定义"复杂 git 操作"**：单次连续动作中涉及以下**任一**的：

1. 跨 3 步以上的命令序列（如 `git mv` + `git mv` + ...）
2. 涉及 worktree 增删（`worktree add` / `remove` / `prune`）
3. 涉及 HEAD 切换（`checkout` / `switch` / `reset`）
4. 涉及 push / pull / fetch 到认证远程
5. 涉及 `.git` 内部结构直接编辑（`objects/`、`worktrees/`、`refs/`）
6. 涉及仓库真身移动（`.git` 上移、跨仓库合并）

**规则**：

1. **超过 3 步的 git 操作必须在讨论板发帖预告**，列出步骤 + 风险点 + 回滚路径
2. **每次只动一个分支维度**——不要在同一次操作里既改分支又改 commit 历史又改 worktree
3. **.git 内部结构直接编辑前必须先备份**（如 `.git.backup-<日期>/`）
4. **认证远程的 push/fetch 操作前，必须确认网络通 + 凭据可用**——non-tty 环境下不弹窗是已知问题，遇到卡住立即停
5. **遇到任何"fatal: ..."立即停**，把错误原文贴讨论板，等所有者裁定

**事故复盘（2026-09-12 一日内发生 4 次）**：
- `git worktree prune` 误删主工作区登记 → 全员虚惊，靠手动重建登记 + `git reset` 修复
- `git add -A` 串文件 2 次 → 误把他人 commit 写进自己的提交信息
- `git switch --detach main` 后未察觉 detached → commit dad62f9 悬空
- push 在 non-tty 环境下被卡 → 浪费 3 分钟才发现是认证问题

> **总原则**：宁可在讨论板上多花 2 分钟问"要不要继续"，也不要 30 分钟救火。
> **本规则适用范围**：所有角色（含所有者代理）。

---

## 十一、仓库拆分记录（2026-09-13，所有者裁定）

### 11.1 背景
主仓库 `digital-life-sphere` 因 `.npz` 快照在历史中被**反复提交**，对象库累积 **470 MB**；
远端未 GC 对象达 **1048 MB**，超过 Gitee 免费配额（1024 MB）⇒ **推送被 pre-receive 拒绝**。
GC 无效（`.npz` 被历史引用，GC 只清未引用对象）。

### 11.2 处置
| 项 | 处置 |
|----|------|
| **主仓库** | **新建 `the-world`**：orphan 重建（历史从零），**181 文件 / 1.9 MB**（原 471 MB，降 99.6%） |
| **数据仓库** | **新建 `the-world-data`**：**278 文件 / 85 MB**（快照 + 实验日志 + 产出） |
| 旧仓库 | 转归档（remote 改名 `gitee-archive`），**不再推送** |
| 旧历史 | 本地保留：tag `archive/pre-slim-main-20260913` + 备份 `.git.backup-20260913-pre-slim`（473 MB） |

### 11.3 remote 配置（新）
| remote | 地址 | 用途 |
|--------|------|------|
| **`gitee`** | `https://gitee.com/little-fishy/the-world.git` | ✅ **主仓库（唯一活跃，upstream）** |
| `gitee-archive` | `https://gitee.com/little-fishy/digital-life-sphere.git` | 旧仓库（归档，禁推） |
| `origin` | `git@github.com:fish827-08/digital-life-sphere.git` | GitHub（只读归档） |

**数据仓库**：`https://gitee.com/little-fishy/the-world-data.git`

| 项 | 值 |
|----|-----|
| **本地路径** | **`the-world/the-world-data/`**（在主工作区内，2026-09-13 由平级目录移入——便于工作区内访问） |
| 主仓库如何对待 | `.gitignore` 已排除 `the-world-data/`（**嵌套独立仓库，主仓库不追踪**） |
| 操作方式 | `cd the-world/the-world-data && git ...`（它有自己的 `origin` 与 `main`） |

### 11.4 🔴 新纪律：实验数据不进主仓库

1. **`.npz` / `.csv` / `.json`（快照、日志、实验产出）一律入 `the-world-data`**，不入主仓库
2. 主仓库单文件 **< 100 MB**；总容量目标 **< 50 MB**
3. 新数据产出后由 `[云端实验]` / `[本地开发]` **同步到数据仓库**
4. **禁止**在主仓库用 `git add -f` 绕过 `.gitignore` 添加数据文件

> **事故教训**：这批 `.npz` 是**逐个 commit 累积**出来的（每次实验保存一棵快照并提交），
> 单次看只有 3–5 MB 不显眼，累积成 470 MB 后才发现——**容量问题要在单文件级别就拦住**。

---

## 十二、🔴 实验前置检查（Pre-Flight Check，2026-09-13 所有者立规）

> **规则原文（项目所有者）**：在任何实验之前，必须核实目前有没有代码层面或者方法论方面的错误，避免实验白跑。

### 12.1 为什么有这条规则（本项目已付过的学费）

| 事故 | 损失 | 漏检的层 |
|------|------|---------|
| R4 批整体作废（2026-09-12） | 15 run ≈ 10 h | **代码层**：跑在 `nb[:4]` 缺陷代码上，跑完才由攻坚线发现 |
| R19 批判读面作废（2026-09-13） | 全批 | **代码层+流程**：provenance 悬空（`git_commit` 不存在）⇒ 不可复现 |
| D-24 首跑中止（2026-09-13，F-R9） | 25 min（止损） | **代码层**：control 臂未关码本 ⇒ 与 main 同配置同轨迹 |
| 「臂间分离」误读（2026-09-13） | 一次错误结论 | **方法论**：饱和区制混淆未剥离就判读 |
| **D-24 的 G-A 不过（2026-09-15）** | D-24 批（≈1 h）+ 一次旗舰批前置 | **规格层（新）**：V-1 把 `donation` 定为 0.05 而 `SIGNAL_COST` 是 0.1 ⇒ 结构性上限 0.5 < 1.0 ⇒ **"保本"语义数学上不可达**。**每个开关都正确生效了（C4 全过），但参数之间自相矛盾** ⇒ 见 **C5** |
| **F-R18：oracle 方向反（2026-09-15）** | 仪器线 D-27④（发现即停，未扩散） | **契约层（新）**：规格一句/验收判据/下游命名 = "发送者**收款**"，骨架/实现 = "发送者**付款**"——**方向语义反了**；六类测试全绿也测不出（都假设方向正确）⇒ 见 **C6** |

### 12.2 检查单（每次实验启动前逐项过，任何一项不过 ⇒ 不跑）

**代码层（P0，硬检）**：

| # | 检查 | 方法 |
|---|------|------|
| C1 | 引擎/脚本所在 commit 的测试全绿 | `pytest tests/`（全量；时间紧至少跑相关子集） |
| C2 | provenance 可解析 | manifest 的 `git_commit` 必须在仓库中存在（D-19 已硬校验拒跑） |
| C3 | **多臂配置差异断言**：任意两臂的开关指纹必须不同（设计上相同的除外）；**同 seed 轨迹完全相同 ⇒ 配置重复，立即停** | 对照 manifest `switches`；或先跑 ≤1k tick 冒烟比对轨迹 |
| C4 | 关键开关实际状态与设计一致（读回核对，不凭假设） | summary `switches` 抽查（如 control 码本应=关） |
| **C5** | 🔴 **规格自洽**：参数**之间**的关系能否表达所**声明的语义**（不只是"每个值生效了"，而是"这些值彼此自洽"） | 读回 `switches` 做关系断言；`experiments/preflight_check.py` 的 `spec_consistency_problems()` 自动执行。**首个落地项**：`oracle.enabled` ⇒ 必须 `donation ≥ SIGNAL_COST`（"保本"语义可达）；`OracleConfig.__post_init__` 已硬失败，`allow_non_breakeven=True` 为显式逃生阀 |
| **C6** | 🔴 **仪器极性**：任何"阳性对照/仪器"类通道必须有**端到端极性测试**（构造成功事件 ⇒ 断言目标量向**预期方向**变化）；Pre-Flight 全绿**测不出"符号反了"** | 通道附带极性测试（首例：`test_f_r18_end_to_end_polarity_emitter_gains`）；新增仪器类通道须在实施清单中列明 |
| **C7** | 🔴 **仪器中立性**：新增/修改观测类仪器后，须以**同配置重跑**与基准批**逐位对拍**（关键量完全一致 ⇒ 纯观测、不扰动被测系统）；出现扰动 ⇒ 视为行为变更（按新配置重新登记 + C3/C4 复核） | 基准批 vs 对拍批逐 run 比对（首例：`d27` vs `d27_recv`，12/12 逐位一致） |

> **C4 与 C5 的分工（勿混）**：**C4 问"开关有没有生效"（声明=实际）；C5 问"生效的这些值彼此自洽吗"（参数关系=所声明的语义）。**
> 两者的**事故原型不同**：F-R9 是 C4 型（control 的码本没关）；**D-24 的 G-A 不过是 C5 型**（码本开关都对，但 `donation < SIGNAL_COST` 让"保本"在数学上不可达）——
> **只做 C4 会全过，却把整批送进一个必然失败的设计。**
> **C6 型（F-R18）是第三类：开关、指纹、参数自洽全过，但核心语义的方向反了——只有端到端极性测试能抓。**

**方法论层（P0，硬检）**：

| # | 检查 | 方法 |
|---|------|------|
| M1 | 判据已预注册（跑前锁定，不得事后改） | 判据写入讨论板/路线共识后方 可跑 |
| M2 | 已知混淆变量已剥离或受控（饱和/区制/成本手调等） | 对照 `_eval/BASELINE.md` 缺陷账本 + 路线共识 R 系列 |
| M3 | 不踩已登记的坑 | 扫一遍缺陷账本（F-x 编号）与"作废结论"清单 |
| M4 | 功效可行：seed 数/tick 数足以回答该问题 | 引用已有实测速率/方差（R44）；无数据先跑功效预批 |

### 12.3 执行方式

1. **启动者自查**：实验启动人（本地开发/云端实验）逐项过单，**在板上回帖"Pre-Flight 通过（C1–C7/M1–M4）"**后才可 `batch_runner` 启动
2. **交叉抽查**：多臂批（≥2 臂）由另一角色抽查 C3/C4（本次 D-24 若有交叉抽查，F-R9 在冒烟期即会被抓）
3. **违规即作废**：未过单跑出的实验数据，判读时按"不可信"处理（同 R36 provenance 断链先例）
4. **工具化（已完成/进行中）**：`experiments/preflight_check.py` 已固化 **C3**（同 seed 轨迹逐字段一致 ⇒ 报警）、**C4**（开关读回）、
   **C4+**（世界种子敏感性，R78-3）、**C5**（规格自洽，2026-09-15 立）、**C6**（仪器极性，2026-09-15 立——以通道自带端到端极性测试为形式）、**C7**（仪器中立性，2026-09-15 立——同配置逐位对拍）与**臂漂移守卫**（runner 新增臂而预期表未同步 ⇒ 失败）。
   运行：`python experiments/preflight_check.py`（退出码 0=可启动 / 1=不得启动）

### 12.4 与既有纪律的关系

- 本节是 **R47「小规格验证先行」的制度化**：小规格批本身就是 Pre-Flight 的一部分（C3/M1 的实跑验证）
- 与 D-19 provenance 硬校验（拒跑）互补：D-19 管"跑的是哪个 commit"，本节管"这个 commit + 这套设计值不值得跑"
- **C5 与"规格评审"互补**：C5 只能机械化"参数间代数关系"这一类自洽（如 `donation ≥ SIGNAL_COST`）；
  **语义/叙事层的自洽仍须人来判**（如"⑤ 有信号 ≠ 群体均值会涨"）。**不要把 C5 当成规格评审的替代品**

---

*本文件由项目所有者授权建立。修改需所有者同意；成员之间的相互约束（§二）永久有效。*
