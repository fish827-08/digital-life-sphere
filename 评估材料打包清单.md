# 外部评估材料打包（R2）

> 交付给第三方审校的材料索引。所有文件均在仓库内（`feature/dev-l7` 分支），
> 打包 = 按本清单收集 + 核对状态。引用文件为当前仓库路径。

---

## 一、材料清单与状态

| # | 材料 | 文件 | 状态 | 备注 |
|---|------|------|------|------|
| 1 | 项目描述（目标/边界） | PROJECT-DESCRIPTION.md | ✅ 完成 | 生态涌现目标 + 定位 |
| 2 | 模块文档（架构） | MODULES.md | ✅ 完成 | 模块一~五，模块三 Rust 核 |
| 3 | 技术汇总（L1~L6 特性） | L1-L6-技术汇总文档.md | ✅ 完成 | 特性实现细节 |
| 4 | 性能设计（L7 + T1~T4） | L7-性能优化详细设计文档.md | ✅ 完成 | T1~T4 已实现 |
| 5 | 扩展特性设计（L8~L10） | L8-L10-扩展特性建议文档.md | ✅ 完成 | 优先级已更新为 L9→L10→L8 |
| 6 | **审计清单（A1）** | 隐式选择压审计清单.md | ✅ 完成 | 逐参数：影响基因/偏置方向/敏感性建议 |
| 7 | **中性化建议（A4）** | 隐式选择压中性化建议.md | ✅ 完成 | 逐偏置：当前方向/是否刻意/方案 |
| 8 | 参数化结果（A2） | simulation/config.py（PredationConfig/CultureConfig/OrganismConfig/PleasureConfig） | ✅ 完成 | 魔法数收编，指纹含键 |
| 9 | **敏感性表（A3）** | results/sensitivity/summary.csv | ⏳ 云端执行中（C4） | 核心 4 参数 ±20%/±50% × 3 seed |
| 10 | 后续发展路线（R1） | 后续发展路线.md | ✅ 完成（草稿待评审） | 阶段分级 + L9→L10→L8 + L7d 触发条件 |
| 11 | 实验记录 | PROGRESS.md（30 万 tick 记录） | ✅ 完成 | 结果 experiments/long_run_l4l5_result.csv（gitignore） |
| 12 | 测试状态 | tests/（139 passed） | ✅ 完成 | 双路径对拍 + 快照 + 信号 + 愉悦度 + 基因注册表 |

## 二、交付顺序建议（给审校方）

1. **先读** #1 项目描述 + #2 模块文档 → 建立整体认知；
2. **再读** #6 审计清单 + #7 中性化建议 → 关注设计者意图 vs 实现偏差（评审核心）；
3. **对照** #9 敏感性表（A3 完成后）→ 验证 #7 的优先项（捕食 0.4 / 信任 −0.1）；
4. **最后** #10 路线文档 → 确认后续方向的合理性。

## 三、完整性缺口（评审时已知）

- #9 敏感性表依赖云端 C4 完成（预估下一轮返回）；
- #7 中性化建议中"未收编登记项"（繁殖门槛下界 0.25 / 传代投入下界 0.3 /
  信号可信度权重 0.5）待 A3 产出后统一补收编进 SimConfig；
- #8 A2 参数化默认值保持现状（不中性化）已声明，等待评估方确认或要求改默认。

## 四、打包操作（发布时执行）

```bash
# 在干净 checkout（feature/dev-l7 最新）下导出本清单涉及的 md 文件：
mkdir -p eval-package
cp {PROJECT-DESCRIPTION,MODULES,L1-L6-技术汇总文档,L7-性能优化详细设计文档,\
L8-L10-扩展特性建议文档,隐式选择压审计清单,隐式选择压中性化建议,后续发展路线,\
PROGRESS}.md eval-package/
cp TASKS-DEV.md TASKS-CLOUD.md eval-package/
```

> 说明：config.py 与测试代码随仓库即可；敏感性表（#9）产出后追加复制。