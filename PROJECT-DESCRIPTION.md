# digital-life-sphere 项目功能描述文档

> 版本：v0.7（架构决策已确认并全部实现；六模型外部评估完成，后续开发按 D0~D5 评估路线推进）
> 状态：D0 止血已全部合入 main（164 passed）；主实验形态调整为**千代 × 多 seed × 零模型对照**（详见评估-EVAL-汇总意见-20260909.md 与评估-EVAL-云端启动说明-20260909.md）

## 目录
1. [项目概述](#1-项目概述)
2. [整体架构](#2-整体架构)
3. [模块详解](#3-模块详解)
4. [模块间交互关系](#4-模块间交互关系)
5. [技术栈与依赖](#5-技术栈与依赖)
6. [外部服务与配置](#6-外部服务与配置)
7. [与旧版 digital_life 的关系](#7-与旧版-digital_life-的关系)
8. [已确认的架构决策](#8-已确认的架构决策)

---

## 1. 项目概述

**digital-life-sphere** 是数字生命模拟项目（旧版 `digital_life`）的第二代重构：
- **世界形态**：由平面矩形网格升级为 **经纬网格构成的球体**（等距圆柱投影）。极区格子面积 ∝ cos φ，小于赤道格子；极点处经度坍缩为一格。
- **环境维度**：引入 **光照 / 昼夜节律**（固定太阳 + 世界自转 → 昼夜扫掠，简化模型）。光照强度随纬度与昼夜相位变化，进一步映射出 **温度场**：极地温度低、夜晚略低。资源再生率受光照调制，温度影响个体的移动/发育速度与能量消耗（冷 → 慢、耗能多）→ 涌现赤道-极地梯度生态。
  - **注**：已随特性线落地捕食（g16，L4：饥饿驱动攻击、能量转移率 0.4）、植物化生态位（g19，L4：光合收益加成 + 移动率折减）、果实-种子传播（L10a 先行版）等机制。**按六模型评估，L8 地形 / L9 尸体能量守恒 / L10 完整种子传播明确推迟**：它们依赖"信号已有语义"，在 D2 信息结构修复前是成本不是收益。完整行为规则见 L1-L6-技术汇总文档.md 与 L8-L10-扩展特性建议文档.md。
- **性能架构（方案 4）**：Python 作为主语言与编排者（保留旧版全部资产：151 项测试、observatory 实验矩阵、persistence 落盘）；**Rust + PyO3** 将热路径数值运算编译为 `.pyd`/`.so` 扩展模块，被 Python 无感 import 调用。
- **可视化**：Web 前端（React + Three.js / react-globe.gl），内核经 WebSocket 推送世界快照，浏览器渲染可旋转球体、昼夜明暗、热力图。

**目标用户**：项目所有者本人（研究观察用），架构决策权归属所有者，AI 只做执行与文档草稿。

**独特问题**：在球形有限世界上，验证环境选择压（光照梯度、极区-赤道差异）是否驱动可解释、可重复的跨世代演化。

---

## 2. 整体架构

```
┌──────────────────────────────────────────────┐
│ ① 前端可视化层（浏览器 / React + globe.gl）     │
│    球面渲染 · 昼夜光照 · 热力图 · 交互旋转       │
└──────────────────┬───────────────────────────┘
                   │ WebSocket 快照流（单向推送）
┌──────────────────▼───────────────────────────┐
│ ② 数据桥 layer（Python 小服务）                │
│    快照序列化 + WS 推流（只读，不影响内核）     │
└──────────────────┬───────────────────────────┘
                   │
┌──────────────────▼───────────────────────────┐
│ ③ Python 编排层（主语言，全部沿用旧版）         │
│    VecEngine 编排 · observatory · persistence │
└──────────────────┬───────────────────────────┘
                   │ import 高频调用（PyO3）
┌──────────────────▼───────────────────────────┐
│ ④ Rust 加速核 Sim-core（.pyd / .so）           │
│    tick 向量运算 · 资源场批量更新 · rayon 并行  │
└──────────────────┬───────────────────────────┘
                   │ 批量字段读写
┌──────────────────▼───────────────────────────┐
│ ⑤ 球形世界 SphereWorld（本次重构的核心内容）    │
│    经纬网格 · 极点坍缩 · 光照/昼夜 · 资源        │
└──────────────────────────────────────────────┘
```

**数据流**：⑤ 世界状态 → ④ 批量计算 → ③ 统计/存档 → ② 快照 → ① 渲染。

---

## 3. 模块详解

### 3.1 SphereWorld 世界模块（核心重构，模块一）

**模块职责**
- 负责：球面网格拓扑（经纬度坐标系）、各格面积与邻居关系、极点坍缩规则、资源场（含光照调制再生）、**温度场（由光照映射）**、光照/昼夜相位、赋值/查询接口，以及为引擎提供"flat 索引"与邻接表。
- 不负责：任何生命个体逻辑（那是引擎的事）、任何统计口径（那是 observatory 的事）。

**对外接口**（草案）
- `class SphereWorld(config)`
  - 输入：`SimConfig`（含 纬度/经度格数、极点模式、光照参数）
  - 输出：世界实例
- `width / height`（属性）：经度格数 / 纬度格数，宽为各纬度最大经度格数
- `latitude_of(row) / longitude_of(col)`：行→纬度、列→经度的映射（含极点坍缩缩放）
- `cell_area(row)`：第 row 行的格子面积（极区更小）
- `neighbor_flat(flat_idx, diag=True) -> np.ndarray`：返回某格的 8 邻平铺索引（极点处自动邻居去重）
- `resources` 资源场：`regrow_tick(global_multiplier)`、`consume_at_flat(flat, amount)`
- `light_phase(tick)` / `temperature(tick) -> np.ndarray`：光照强度场（纬度 × 昼夜相位）与温度场（光照映射，极地冷、夜晚略冷）
- `walk_neighbor(flat, dir_idx) -> flat`：支持绕过极点环绕

**内部方法**（草案，待确认细粒度后才逐一实现）
- 极点坍缩映射表 `_pole_map`
- 平铺索引（y*width+x）与经纬网格间的双向转换
- 邻居掩码生成（`np.roll` 或预计算表）
- 温度场映射 `_light_to_temperature(light)`

**依赖关系**
- 依赖：`simulation.config.SimConfig`、`numpy`
- 被依赖：VecEngine / Sim-core / observatory

---

### 3.2 SimulationEngine / VecEngine（引擎层，模块二，大量复用）

**模块职责**
- 负责：tick 主循环编排、种群 SoA 数组维护、代谢/进食/移动/衰老/死亡/繁殖的**语义规则**（与旧版一致）。
- 不负责：批量数值的逐底实现（可能交给 Sim-core）、统计口径。

**对外接口**（与旧版完全对齐，duck-typed）
- `engine.tick() / step() / run()`
- `engine.finished / extinct`
- `engine.population.{alive_count, max_generation, total_energy, organisms()}`
- `engine.history`（有界）
- `engine.total_born / total_died / death_cause_totals()`

**内部方法**
- `_step_population()`：行动→死亡→繁殖→清理（与 3.3 或有重叠，待架构确认后决定归属）
- `_advance_one_tick()`：先资源再生，再种群行动（顺序契约不变）

**依赖关系**：依赖 3.1（世界）；被 3.4 观察台依赖。

---

### 3.3 Rust 加速核 Sim-core（模块三，本次新增）

**模块职责**
- 负责:VecEngine 中**真正发热的数值运算**的 Rust 实现：代谢/进食/移动/繁殖的批量矩阵运算、资源场再生与消耗。
- 不负责：引擎决策逻辑（出生判定、世代判定仍在 Python）。

**对外接口**（PyO3,草案）
- `sim_core.step_vectors(x, y, energy, genes, age, ...) -> (energy, parent_offsets, ...)`
- `sim_core.regrow(grid, rate)`
- 具体签名待架构确认后与 Python 侧对齐

**内部方法**（Rust）
- `rayon` 并行批次循环
- 无共享可变状态，函数输入输出均为借用数组

**依赖关系**：仅依赖 `numpy`（互转）与 `pyo3`。不依赖 Python 世界对象。

---

### 3.4 observatory 观察台（模块四，复用不变）

**模块职责**：实验矩阵调度（ExperimentRunner）、统计口径（EvolutionStatistics）、阶段归档（RESEARCH 追踪）、命令行入口。
**对外接口**：`run_single`、`build_plan`、`derive_seed`、`--vec/--sphere` 开关。
**依赖关系**：依赖 3.2 引擎的 duck-typed 表面。

---

### 3.5 可视化桥接层（模块五，新增）

**模块职责**
- 负责：订阅引擎 tick 快照、序列化为前端所需结构、通过 WebSocket 推送。
- 不负责：任何模拟计算。

**对外接口**
- `SnapshotBroker(engine)`：以 `interval` tick 节拍采集快照
- `serve(port)`：启动 WS 服务
- 快照结构：种群位置/能量/基因摘要、资源热力场、世界元数据（网格、极点、光照相位）

---

### 3.6 前端渲染层（模块六，新项目）

**模块职责**：浏览器中展示球体世界。React + three.js / react-globe.gl。
**对外接口**：浏览器打开即连 WS、渲染、交互；无业务接口。
**依赖关系**：WebSocket 桥（3.5）。

---

## 4. 模块间交互关系

| 调用方 | 被调用方 | 方式 |
|--------|----------|------|
| VecEngine (3.2) | SphereWorld (3.1) | 世界查询/写入，平铺索引 |
| VecEngine (3.2) | Sim-core (3.3) | PyO3 import，批量数值调用 |
| observatory (3.4) | VecEngine (3.2) | duck-typed 引擎表面 |
| 桥接层 (3.5) | VecEngine (3.2) | 快照订阅回调 |
| 前端 (3.6) | 桥接层 (3.5) | WebSocket 单向推送 |

依赖方向：单向向下（3.6→3.5→3.2→3.1, 3.3），observatory 从旁挂载。禁止反向依赖。

---

## 5. 技术栈与依赖

| 类别 | 选型 | 说明 |
|------|------|------|
| 主语言 | Python 3.14 + NumPy | 编排、统计、测试、可视化桥 |
| 高性能 | Rust + PyO3 + maturin + rayon | 数值热核编译为 .pyd/.so |
| 可视化 | React + Three.js / react-globe.gl | 浏览器球面渲染（WebGL） |
| 实时通道 | WebSocket（如 `websockets` 库） | 快照单向推送 |
| 测试 | pytest（旧版 151 项为基线） | 双引擎等价性、确定性、性能哨兵 |
| 构建 | maturin（Rust 扩展）、Cargo | |

---

## 6. 外部服务与配置

- 无强制外部服务。可视化可本地浏览器打开（localhost）。
- 可选：`--viz` 开启推流；端口配置可覆盖。
- 所有实验/结果本地落盘（results/），遵循旧版 `RESEARCH.md` 归档规范。

---

## 7. 与旧版 digital_life 的关系

- **不删除**旧版 `the-world/digital_life`（保留为参考基准与回归对照）。
- **复用**：引擎语义规则、观察台、统计口径、测试框架、文档体系（AI-CODE-DEVELOPMENT-RULES.md / PROGRESS.md / PROJECT-DESCRIPTION.md 已按新项目建立）。
- **重写**：世界模块（平面→球面）、新增 Rust 热核、新增可视化链。
- 迁移方式：一步步来，每模块经我确认后再进入下一模块（遵守 AI Code 规则一）。

---

## 8. 已确认的架构决策

> 以下决策已由项目所有者逐项确认（2026-09-06）。后续如需变更，按 AI Code 规则重新走确认流程。

| # | 决策点 | 结论 | 说明 |
|---|--------|------|------|
| 1 | 网格分辨率 | **纬度 60 × 经度 120** | 2° 格，共 7,200 格；height=60，width=120 |
| 2 | 邻居邻接 | **8 邻**（含对角） | 极点处邻居去重 |
| 3 | 光照模型 | 固定太阳 + 世界自转 → 昼夜扫掠 | 简化模型，不做复杂大气层 |
| 4 | 资源初始分布 | 均匀分布 | 光照/温度造成的差异靠再生与个体行为涌现 |
| 5 | 温度场 | 光照调制温度：极地冷、夜晚略冷 | 温度影响移动/发育速度与能量消耗（冷 → 慢、耗能多） |
| 6 | Rust 热核范围 | **B：数值 + 资源场** | Rust 承担数值运算 + 资源场/温度场更新；可分模块逐步迁移 |
| 7 | 快照推送频率 | **每 100 tick** | 后续按实际效果调整 |
| 8 | 开发顺序 | 先世界模块 → 再放生物 | 先让世界模块独立可运行/可视化，再接入生命 |

**开发顺序基本盘**：SphereWorld（模块一，重写）→ VecEngine 适配球面（模块二，复用+改写）→ Rust 热核（模块三，新增）→ observatory 适配（模块四，复用）→ 快照桥 + 前端（模块五/六，新增）。每完成一个模块，经所有者理解确认后再进入下一个。

---

*本文档随项目进展实时更新（遵守 AI Code 规则 → 文档不是一次性产物）。*